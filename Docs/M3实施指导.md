# M3 实施指导：xLua 集成

> 里程碑目标（手册 §五）：**Lua 跑起来、能调试、秒级迭代、注册表从真实表填充。**
> 前置阅读：《框架实施手册》§五/§9.0、《自研框架设计方案》§4.1-4.4（职责划分/LuaComponent/三座桥/目录约定）、§3.4（启动失败判定与注册表降级细则）、《M2实施指导》§4（流程与锚点）、§6.5。
> 状态：规划于 2026-09-10（M2 收尾后立项）。实施记录见文末。

---

## 0. 现状盘点（开工前核对）

**已就绪**：
- 流程链 Launch→Preload→Main 通；**M3 锚点已留**：`ProcedurePreload.cs:32`（"Lua 预载与注册表填充段"注释行，消费逻辑插在其下方）
- `Bridge.Data` C# 骨架（GetItem/GetUIForm + 按行缓存位，注释标明 M3 由生成器产出转换体）
- `LuaRegistry<T>` 机制（Core，M1 交付，8 用例）；注册表三件套表有数据（TbUIForm 行：LuaPath=UI.UIMain）
- `AssetService.LoadRawFileTextAsync/BytesAsync`（TextAsset 实现）；YooAsset 收集组 LiteGameConfig/LiteGameScenes
- Editor 面板（ConfigPanel）——M3 可选扩展 Lua 区
- xLua 插件在 `Assets/XLua`（含 Examples/GenConfig 示例），**`xLuaMain` asmdef 尚不存在**（LiteGame.Runtime 的引用已预写，建好后自动咬合）

**M3 交付**：LuaComponent（env+loader+tick+shutdown）、Lua 全量预载、main.lua 执行、Bridge 三门面绑定（data 可查表）、三注册表填充（含 `RegistryFillReport`）、事件桥机制、`DevReload`、`SafeCall`、LuaPanda、core/ 地道 Lua 风格基座。

**不在范围**：UI 壳消费注册表（M4）、实体/声音壳、BindCodeGen（M4）、玩法进 Lua（实体逻辑已在 C#，设计方案 §4.1）。

---

## 1. 前置决策（开工前定案）

### 1a. Lua 收集方案（**定案，2026-09-10：自定义 `IIgnoreRule`**）

`.lua` 是 Unity DefaultAsset，被包级 `NormalIgnoreRule` 拒绝收集（M2 实测："Default asset cannot be packed"）。**定案：自定义 `IIgnoreRule`（~15 行）**——按路径放行：仅 `/LiteGame/Lua/` 下的 DefaultAsset 允许收集，其余沿用 `DefaultAssetIgnoreRule` 全部逻辑；包级 `IgnoreRuleName` 切换为它。**不改后缀**，保持 §4.4 目录约定（main.lua/UIMain.lua）不动。影响面核对：其余收集目录（Config=.bytes、Scenes=.unity、GameMain/Configs）无 DefaultAsset，放行无副作用——实施时复核一遍各收集目录。

### 1b. 预载清单来源：tag 枚举 vs 目录枚举

手册 §4.2 定死"收集器打 tag `lua` → `GetAssetInfosByTag` 枚举"。**M2 教训：YooAsset 3.0.5 模拟模式 API 与文档有出入**（RawFileObject 事件），tag 枚举在 EditorSimulateMode 下的实际 API 需实测——fallback 是经收集清单按 CollectPath 枚举。**先实测 tag 路径，不通再用 fallback，两条路都只写一处**（`LuaPreloader` 内部）。

### 1c. LuaKeys 生成（**定案，2026-09-10：做生成**——用户推翻"推迟手写"建议）

注册表 Lua路径 → **`LuaKeys.g.cs` 生成常量**，纳入 M3 交付。设计方案 §277"已落地先例"由本里程碑兑现。

**生成方案**：
- **来源**：注册表三件套 xlsx（TbUIForm/TbContentEntry/TbStrategy）的 `Lua路径` 列
- **工具**：`Luban/gen_lua_keys.py`（openpyxl，按 Luban 表头约定定位字段名行；无该列/空表跳过）
- **接线**：`gen.bat` 追加第三步（bat 注释 ASCII 纪律不变）
- **产物**：`Assets/LiteGame/Scripts/Runtime/Bridge/Generated/LuaKeys.g.cs`（namespace `LiteGame`，归属 LiteGame.Runtime 程序集；文件头标 `// generated`）

```csharp
// 生成形态（按根分段，段内按名字排序，幂等覆盖）：
public static class LuaKeys
{
    public static class UI         { public const string UIMain = "UI.UIMain"; }
    public static class Content    { /* …行驱动… */ }
    public static class Strategies { /* …行驱动… */ }
}
```

- **约束**：路径约定两段式（`根.名字`，§4.4 三个固定根）——未知根 **生成器直接抛**（fail the build，不静默落平铺）；重名即抛；**排序输出保证 diff 干净**
- **消费点**：RegistryFiller（填充与校验）、事件桥映射表、DevReload 校验——**禁止散落字符串路径**（收口模式判据，设计方案 §277）
- 编码 UTF-8；生成物入库（与 Luban 产物同策略）

### 1d. cfg/*.lua 不消费（重申）

`Bridge.data` 内部把 C# 强类型行转 LuaTable（xLua 生成代码），Lua **不触碰 Luban 类型**（`Luban.Tables` 不进白名单）；Luban 的 lua 输出维持"生成备用"。

---

## 2. C# 侧步骤

### 2.0 LuaKeys 生成（gen.bat 第三步，§1c 定案）

- `Luban/gen_lua_keys.py`（~50 行 python）+ `gen.bat` 追加第三步：读三件套 xlsx 的 `Lua路径` 列 → 产出 `Bridge/Generated/LuaKeys.g.cs`
- 实测点：Luban xlsx 表头约定（字段名行的定位）；空表/无该列的健壮跳过
- 验收：改 xlsx 加一行 → gen → 常量出现且排序稳定；未知根 → 生成器抛错

### 2.1 xLua asmdef 化 + 导出配置（手册步骤 1）——**已交付（2026-09-10）**

- **asmdef 已就位**（核对实证：vendor 自带 `Src/xLua.asmdef`，其 `"name"` 字段已是 **`xLuaMain`**——文件名旧、程序集名对，LiteGame.Runtime 的引用早已咬合；名字避开原生 `xlua.dll` 的纪律满足）。新增 `Src/Editor/xLuaMain.Editor.asmdef`（Editor-only，引用 xLuaMain）——把生成器（5 个 cs）从运行时程序集剥离
- **服务桥形态修正（决策记录⑤）**：**Bridge 不进生成白名单**——生成代码落在 xLuaMain 内，包 wrapper 需要业务类型就得让 xLuaMain 引 LiteGame.Runtime，而 Runtime（LuaComponent）引 xLuaMain → **asmdef 循环**（禁环）。替代：服务桥走 **CSharpCallLua 委托注册**（LuaComponent 把适配函数 Set 进全局表；查表属低频入口，动态转换符合热路径纪律 §4.3）；Bridge 拆独立底层程序集后（M4+ 视频率）再评估生成路线
- **`GenConfig` 重写**（LiteGame/Scripts/Editor/GenConfig/，asmdef 补 xLuaMain 引用）：CSharpCallLua only（`Action` / `Action<string>` / `Func<int, LuaTable>`——Bridge 查表适配用）；**Luban.Tables / 业务类型 / 示例类型永不进**
- **Examples/Tutorial 处置（实证决策）**：示例脚本自带 gen 属性标注，asmdef 边界下 Generate Code 必然产出"引用 Assembly-CSharp 类型"的代码 → xLuaMain 内 CS0246（手册"留在 Assembly-CSharp 自动引用"的预设被 asmdef 化击穿）。处置：**Examples 整体移出 Assets**（`XLuaSamples/Examples`），Tutorial 的 5 个属性标注脚本内容停用（原文件保留 + 内容备份 `XLuaSamples/TutorialSrc/`）
- `HIDE_REFLECTION` 宏：17 个现役平台组已配（PlayerSettings API；废弃平台组报错忽略）
- Generate Code：**实测跑通**（5 个生成文件、零示例污染、Unity 编译零错误）；生成的 `Func<int, LuaTable>` 桥供 §2.5 服务桥注册使用

### 2.2 Lua 全量预载（手册步骤 3）——**代码交付，Play 验证因 Unity 重导入未完成（待续）**

- 收集组 `LiteGameLua`（CollectPath: Assets/LiteGame/Lua，PackDirectory + 1a 的 ignore rule）+ 收集器 **`AssetTags: lua`（字符串字段，分号分隔——写成 YAML 序列反序列化必空，实测坑）**
- `LuaPreloader`（业务件，`Runtime/Lua/LuaPreloader.cs`）：Preload 锚点处全量 `LoadRawFileBytesAsync` → `Dictionary<string, byte[]>`（key = require 路径，如 `ui/UIMain`）；DevReload 重入 = 先 Clear
- **loader 是同步签名，任何"运行时再异步加载"的念头都是错的**——一切靠启动期预载；未命中 = 校验漏项直接抛（带路径）
- **本轮实证的三个坑（均已修复/落位）**：
  1. **清单 API**：3.0.5 无 `GetAssetInfosByTag`、无目录枚举——`GetAssetInfos(string)` 即 **tag 查询重载**（§1b fallback 路线不存在，tag 是唯一清单来源）
  2. **`.lua` 是 DefaultAsset**：`LoadAssetAsync<TextAsset>` 恒 null → 新增 `LuaTextAssetImporter`（ScriptedImporter，把 .lua 主对象声明为 TextAsset，EditorSimulateMode 与真机形态一致）
  3. **空文件 = 构建失败**：`main.lua` 曾为 0 字节 → PackDirectory 包体积为零 → `ErrorCode403 Bundle temporary size is zero` → 整个模拟构建失败停机。**Lua 目录禁止空文件**（已写入真实 main.lua）
- 待验证：Play 下 `LuaPreloader.PreloadAllAsync` 的 count/keys（ScriptedImporter 触发全工程 .lua 重导入期间 MCP 会话掉线，未完成）

### 2.3 LuaComponent（手册步骤 2，框架唯一新增核心模块）——**已交付（2026-09-10，Play 实测通过）**

- `Runtime/Lua/LuaComponent.cs`（~150 行）：MonoBehaviour 挂 GameEntry 同 GameObject；四件职责
  ① `LuaEnv` + 自定义 loader（同步签名，只从 LuaPreloader 预载缓存取；require 点分转斜杠 + 原样双试；未命中抛带路径明确错误——实测 `require('No.Such')` → "Lua 预载缓存未命中:No.Such"）
  ② tick 派发开关（`TickEnabled` → `env.Tick()`，Lua 不需要时不调）
  ③ `DoMain()` 执行 main.lua（重复执行抛——重跑走 DevReload §2.7）；诊断口 `DoString(chunk)`
  ④ `Shutdown()` 幂等（env.Dispose；OnDestroy 兜底）
- **`log` 全局表绑定**（§4.3 日志收口白名单首批成员）：info/warning/error → LiteFramework.Log，经 `Action<string>` 委托（CSharpCallLua 已生成）
- IModuleStats（HUD）：Files / MainExecuted / TickEnabled / EnvAlive
- **Play 实测**：预载 8 → DoMain 成功 → `UI.UIMain` 全局表 check=`table/true`（require 链通）→ main.lua 的 log.info 经桥到达 C# Log → tick 开 3 帧无异常 → 未命中 require 报错带路径 → Shutdown 幂等 + 账目清零 + 二次 DoMain 被拒
- 实施备注：新增诊断口 `DoString(chunk, chunkName)`（公开低频入口）

### 2.4 启动接线（手册步骤 4，Preload 锚点五步序）——**已交付（2026-09-12，Play 实测通过）**

```
ProcedurePreload（锚点下方按序）：
① LuaPreloader.PreloadAllAsync(ct)        — 全量预载
② luaComponent.DoMain()                   — 执行 main.lua
③ RegistryFiller.FillAll(ct)              — 读三件套逐行 require/校验/包适配器 → Fill → RegistryFillReport
④ report.HasFailures → Fail()（整批统一判定，不遇错即停）
```

- **注册表实例装配在 ProcedureLaunch**（唯一受信装配点，追加三行 `RegisterInstance<ILuaRegistry<...>>`），填充器 `RegistryFiller` 构造注入（依赖 config/tables + LuaComponent）
- **路径常量一律走 `LuaKeys` 生成物**（§2.0）——填充、校验、事件桥映射、DevReload 禁止散落字符串路径（收口模式判据，设计方案 §277）
- **`RegistryFillReport`**（设计方案 §3.4 降级细则，~40 行）：逐项 try/catch；单项失败记入继续；整批跑完统一判定；`Failures` 非空 → 阻止进 Main、逐条 `Log.Error`；**错误信息区分"未注册"（配置漏配）与"注册失败"（lua 炸了）**

### 2.5 服务桥绑定（手册步骤 5，形态已按 §2.1 修正）——**已交付（2026-09-12，Play 实测通过）**

- **绑定形态 = CSharpCallLua 委托注册**（§2.1 决策：Bridge 不走生成 wrapper）：LuaComponent 在 env 初始化后 `Global.Set("bridge_getItem", (Func<int, LuaTable>)(...))` 等适配函数（转换器在 Runtime 层手工展开字段，不用生成代码），再组装 `Bridge = { getItem = ..., getUIForm = ... }` 全局表——"启动时把容器服务显式绑定成 Lua 全局表"（§4.3）字面满足
- **只导出这三个门面**；`ILuaRegistry`/玩法系统一律不导出；禁止 `CS.` 直引业务类型（HIDE_REFLECTION 下未生成代码本就不可达）
- `Bridge.Data` 增 Lua 侧入口：`GetItemLua(id)` → 手工展开 `LuaTable` + 按行缓存（M2 预留的同构位）；**DevReload 时缓存必须清空**（旧 env 的 LuaTable 全失效，见 §2.7）
- `Bridge.ui/content`：M3 交付**骨架门面**（注册表查询入口，方法最小集）——真实语义等 M4 UI 壳，不过度设计

### 2.6 事件桥（手册步骤 6）——**已交付（2026-09-12，Play 实测通过；SafeCall 前置件随之提前交付）**

- C# 事件 → 逐回调 pcall（经 `SafeCall`）→ Lua 回调表；Lua 端 `events.on(name, fn)` **返回注销委托**（闭包配对，core/eventer.lua 同款语义）
- **映射规则由事件桥认领**：桥内维护 string 事件名 → C# 事件类型的**显式注册表**（新增桥接事件 = 一行注册），两头不各自发明命名
- M3 桥接事件最少集：1 个验证事件即可（无真实玩法事件——机制验收优先，别造假事件）

### 2.7 DevReload（手册步骤 7）+ SafeCall

- `SafeCall`（手册 §9.0，~20 行）：**放 Core**（引擎无关的"调用即防护"），`Core/Util/SafeCall.cs` + xLua 侧薄封装；补 xUnit 用例（正常/抛/返回值）
- 菜单 `LiteGame/Lua/Reload Env %&r`：**清 Bridge 缓存 → 重填注册表 → env.Dispose 重建 → 重跑 main.lua**——**顺序钉死**：env 重建后旧 LuaTable 引用全失效，Bridge/注册表缓存不清 = 静默用旧对象
- 重填走 §2.4 同一条填充路径（③），不写第二份

### 2.8 HUD 与 LuaPanda（手册步骤 8/9）

- HUD **已交付**（LiteGame.DevHUD——**全平台编译 + 三宏 #if 剥离 + DevHUDBoot 自创建，场景不挂**；Editor-only asmdef 实测剥离 play 场景组件，见决策记录⑨）——核对项：Log Recent/错误计数/stats 可见；可选给 LuaComponent 加 IModuleStats（已载文件数/tick 开关）
- LuaPanda：`LuaPanda.lua` 随 Lua 目录分发（收集进预载即可被 require）；**仅 `UNITY_EDITOR` 下 `require("LuaPanda").start()`**（宏隔离，打包关闭——hook 忘隔离 = 真机莫名掉帧，手册点名的坑）；VSCode 插件 + attach 启动配置写进文档

---

## 3. Lua 侧交付（core/ 地道 Lua 风格，设计方案 §4.4）

- `core/class.lua`（元表 OOP 基类）、`core/eventer.lua`（闭包事件器，`on` 返回注销委托）
- `main.lua`（require/定义，按约定路径暴露逻辑表）
- `ui/UIMain.lua`（示例逻辑表：OnShow/OnClick 壳——M4 消费；M3 只要求"表存在、可寻址、可 require"）
- `cfg/`（Luban 生成备用，不消费）
- **学习口径**：core/ 必须是地道 Lua——元表 OOP、闭包注销配对、协程流程；**不许把 Lua 写成"C# 的注释版"**（§4.4）

---

## 4. 测试与自测策略

xLua 有引擎依赖，xUnit 双轨覆盖不到——**权威断言 = Play 冒烟**（手册自测五条）+ 面板扩展：

- `ConfigPanel` 可选加 Lua 区：env 状态 / 已载文件数 / 三注册表行数（复用反射发现模式）
- 冒烟清单（对应验收线）：
  1. 嵌套 require 链可用（main → core/class → ui/UIMain）
  2. Lua 调 `Bridge.data.GetItem(1001)` == "道具1"
  3. 注册表某行 Lua 路径不存在 → 启动期报错且**信息区分未注册/注册失败**；阻止进 Main
  4. 改 .lua → Ctrl+Alt+R 秒级生效（DevReload）
  5. VSCode 断点命中 main.lua
  6. 事件桥 on → 触发 → 注销 → 不再触发；Lua 回调抛异常 → pcall 隔离、C# 日志可见、派发链存活
- dotnet 基线 85 绿（Core 仅增 SafeCall 用例）

---

## 5. 完成判定

- [x] `xLuaMain` asmdef + Generate Code + `HIDE_REFLECTION`（**已交付**：asmdef vendor 已带 name=xLuaMain；HIDE_REFLECTION 配 17 平台组；GenConfig 修订为 CSharpCallLua only——Bridge 走委托注册，见 §2.1 决策记录⑤）
- [x] **LuaKeys 生成链路**：**已交付**——2 条路径产出（UI.UIMain / Content.DemoEntry）、未知根抛错实测、排序稳定；gen.bat 第三步接线
- [x] **Lua 收集 + 全量预载 Play 实测通过（2026-09-10）**：`PreloadAllAsync` 8/8 文件（Cfg×4 / Core×2 / main / UI/UIMain）、key 剥前缀后缀正确、6 帧完成；配置链路回归正常（ignore rule 切换无副作用）
- [x] **Lua 目录禁止空文件**（0 字节 → ErrorCode403 构建失败，实测坑；main.lua 已写入真实内容）
- [x] **main.lua 执行 + 嵌套 require 链（2.3 交付）**：DoMain 成功、`UI.UIMain` 全局表 table/true、log 桥生效、未命中 require 报错带路径、Shutdown 幂等
- [x] Bridge.data Lua 查表成功（GetItem(1001) = 道具1）+ 按行缓存（**2026-09-12 Play 实测**：道具1|Count=10、rawequal 同实例）
- [x] **三注册表填充 + RegistryFillReport（2026-09-12 已交付 + Play 实测通过）**：未注册/注册失败区分、缺项整批判定后阻止进 Main；RegistryFillReport 落 Core（xUnit 4 用例，dotnet 基线 89 绿）；Play 冒烟全通（见实施记录）
- [x] 事件桥 on/off/注销配对 + 逐回调 pcall 隔离 + 显式映射注册表（**2026-09-12 Play 实测**，见实施记录）
- [ ] DevReload（Ctrl+Alt+R）秒级迭代；Bridge 缓存清空顺序钉死
- [ ] LuaPanda 断点命中 main.lua；hook 打包隔离
- [x] SafeCall 进 Core + 用例（**2026-09-12 随事件桥提前交付**：Core/Util/SafeCall.cs，xUnit 4 用例，基线 89→93 绿）
- [ ] HUD 可见 Lua 运行状态
- [ ] dotnet 基线 ≥85 绿；行数对账记录在案

---

## 6. 预算（软约束，超 20% 写明原因）

| 件 | 预算 |
| --- | --- |
| LuaComponent | ~120 |
| LuaPreloader + 收集/ignore rule | ~80 |
| RegistryFiller + RegistryFillReport | ~120 |
| Bridge Lua 侧入口（ToLuaTable 转换 + 缓存） | ~80 |
| 事件桥 | ~80 |
| DevReload + SafeCall | ~50 |
| **LuaKeys.g.cs（生成物）+ 消费** | ~25 |
| Lua 侧（core/ + main.lua + ui/UIMain.lua） | ~120 行 Lua |
| gen_lua_keys.py（python，工具不计 C# 账） | ~50 行 python |
| **合计** | **~555 C# + ~120 Lua** |

---

## 7. 决策记录

- 2026-09-10（规划定案）：**①Lua 收集 = 自定义 `IIgnoreRule`**（按路径放行 Lua 目录 DefaultAsset，不改后缀——保持 §4.4 目录约定；M2 挂账关闭）。**②LuaKeys 生成 = 纳入 M3**（用户推翻"推迟手写"建议：gen.bat 第三步读三件套 Lua路径 列 → `LuaKeys.g.cs`；设计方案 §277"已落地先例"由本里程碑兑现；两段式路径约定外的未知根生成器直接抛）。③预载清单来源先实测 tag 枚举（M2 教训：模拟 API 与文档有出入），fallback 目录枚举。④cfg/*.lua 不消费（重申）。
- 2026-09-10（执行 2.0/2.1 定案）：
  - **⑤服务桥不走生成 wrapper**：生成代码落在 xLuaMain 内，包 Bridge wrapper 需 xLuaMain → LiteGame.Runtime，而 Runtime（LuaComponent）→ xLuaMain，**asmdef 禁环**。改走 CSharpCallLua 委托注册（`Func<int, LuaTable>` 适配函数由 LuaComponent Set 进全局表；查表低频，符合热路径纪律）；Bridge 拆独立底层程序集后（M4+ 视频率）再评估生成路线。GenConfig 随之修订为 CSharpCallLua only。
  - **⑥Examples 移出 Assets、Tutorial 脚本停用**：示例脚本自带 gen 属性标注，asmdef 边界下 Generate Code 必然产出引用 Assembly-CSharp 类型的代码（xLuaMain 不可见 → CS0246）。手册"Examples/Tutorial 留在 Assembly-CSharp 自动引用"预设被 asmdef 化击穿。Examples/Tutorial 目录备份至 `XLuaSamples/`；Tutorial 5 个属性标注脚本内容停用（备份 `XLuaSamples/TutorialSrc/`）。
  - **⑦asmdef 实证**：`Src/xLua.asmdef` vendor 已带且 name=xLuaMain（"从零建"修正为"核对就位"）；新增 `Src/Editor/xLuaMain.Editor.asmdef`（生成器 Editor-only，不进玩家包）。
  - **⑧HIDE_REFLECTION**：PlayerSettings API 配 17 个现役平台组（废弃平台组 API 报错忽略）。
  - **⑨DevHUD 形态回归（2026-09-10 复验定案）**：Editor-only asmdef（分层审查时的修正）**实测剥离 play 模式场景组件**——场景加载报 "class named 'LiteGame.DevHUD' is not derived from MonoBehaviour"、运行时 FindObjectsByType 含 Inactive 均为 0、AddComponent 返回 null（类型本身健康：BaseType=MonoBehaviour、程序集已加载、无同名类冲突）。**回归为三宏 `#if` 剥离 + `DevHUDBoot`（RuntimeInitializeOnLoadMethod AfterSceneLoad 幂等自创建，DontDestroyOnLoad）+ 场景不挂组件**——同 DebugTuner/M0SelfTestRunner 手法。Play 复验：hudFound=1、boot 7 帧、预载 8/8 回归、零错误。教训：**Editor-only asmdef 的 MonoBehaviour 不能用于场景挂载**（"Editor 常开、打包剥离"对场景组件必须用 #if + 代码创建实现）。

## 附：实施记录

- 2026-09-10（步骤 2.0 + 2.1 交付）：
  - **2.0 LuaKeys 生成**：`Luban/gen_lua_keys.py`（~90 行：`##var` 表头定位、路径列按文件映射——uiform/strategy=lua_path、contententry=entry、未知根抛、重名抛、排序输出、幂等覆盖）+ gen.bat 第三步。实测：2 条路径产出（UI.UIMain / Content.DemoEntry，strategy 空路径正确跳过）、未知根 `Foo.Bar` 抛错、`Strategies` 空根不产出。产物 `Bridge/Generated/LuaKeys.g.cs`。
  - **2.1 xLua asmdef + Generate Code**：核对（xLuaMain 已就位）→ xLuaMain.Editor 拆分 → GenConfig 重写（CSharpCallLua only）→ HIDE_REFLECTION 17 组 → Examples/Tutorial 处置 → Generate Code 三轮实测（第一轮暴露 asmdef 边界 CS0246 → 处置 → 清 Gen 死循环破局：编译错误滞留旧程序集导致生成器扫描到旧属性标注，park 全部 Gen 后域重载再生成）→ **最终 Gen 5 文件、零污染、Unity 编译零错误**。
  - 过程插曲：Bash/PowerShell 工具外壳持续损坏（`unknown option '--codebuddy-windows-job-launcher'`），全部 IO 与命令经 Unity MCP `execute_code` 完成；python openpyxl 临时补装。
- 2026-09-10（步骤 2.2 交付 + Play 实测）：
  - **交付**：`LiteGameIgnoreRule`（复刻 NormalIgnoreRule、Lua 目录 DefaultAsset 放行，包级切换）+ 收集器 `AssetTags: lua`（**string 字段坑**：写成 YAML 序列反序列化必空）+ `LuaTextAssetImporter`（ScriptedImporter：.lua → TextAsset，**DefaultAsset 无法直接加载**的实证修复）+ `LuaPreloader`（tag 查询 `GetAssetInfos("lua")`——**3.0.5 无目录枚举 API、该重载即 tag 查询**；key 剥前缀后缀；重入 Clear）+ `main.lua`/`Core/class.lua`/`Core/eventer.lua`/`UI/UIMain.lua` 最小交付。
  - **Play 实测通过**：预载 8/8 文件、6 帧完成、key 全对；配置链路回归正常（ignore rule 切换零副作用，item(1001).Count=10）。
  - **实证坑位**：① **Lua 目录禁止 0 字节文件**——main.lua 曾为空文件 → PackDirectory 包体积零 → `ErrorCode403 Bundle temporary size is zero` → 模拟构建失败停机（启动链全程失效的根因）。② YooAsset 3.0.5 的 `GetAssetInfos(string)` 是 **tag 查询重载**（非目录枚举），fallback 目录枚举路线不存在。③ `Collector.AssetTags` 是 string（分号分隔多 tag），YAML 序列写法静默失败。
  - **已知问题（待查）**：Test.unity 的 DevHUD 组件在场景加载时被判"not derived from MonoBehaviour"而未实例化（FindObjectsByType 含 Inactive 均为 0）——**guid 唯一、MonoScript 解析正常（GetClass=LiteGame.DevHUD、基类 MonoBehaviour）、无同名类冲突**，已强制重导入 Test.unity（ImportAsset ForceUpdate），复验待 Unity 空闲；HUD 缺失不影响 M3 链路验收（仅调试可见性）。
- 2026-09-10（硬编码巡检 + 单源化）：全扫自研两程序集——无平台 API 散落、无真文件系统路径（Assets/ 前缀均为 YooAsset location，跨平台恒定）。顺手单源化两处：LuaPreloader.LuaDir 公共常量化（Editor 的 LiteGameIgnoreRule 改为引用，删重复字面量）；ConfigService.DataDir 常量化（原内插前缀）。维持不动：SettingService.FilePath="settings.json"（机制约定）、DefaultPackageName（已是 const）、Editor 工具内路径（豁免）、EditorSimulateMode 硬约束（M6 边界，#else 抛出带指引——真机运行的显式阻塞点）。
- 2026-09-10（步骤 2.3 交付 + Play 实测）：LuaComponent（~150 行）四件职责全通——env+同步 loader（点分转斜杠+原样双试、未命中抛带路径）、tick 开关（env.Tick）、DoMain（重复执行抛）、Shutdown 幂等（OnDestroy 兜底）。附加：log 全局表绑定（info/warning/error → Log，§4.3 日志收口首批成员）+ 公开诊断口 DoString + IModuleStats。Play 实测：预载 8/8 → DoMain → UI.UIMain=table/true（require 链通）→ main.lua 的 log.info 经桥到达 C# Log → tick 3 帧无异常 → 未命中 require 报错带路径 → Shutdown 两次幂等 + 账目清零 + 二次 DoMain 被拒（未初始化）。接线（GameEntry AddComponent + Launch 装配 + Preload 注入）留 2.4。
- 2026-09-12（步骤 2.4 交付）：
  - **RegistryFillReport 落 Core**（`Core/DI/RegistryFillReport.cs`，设计方案 §3.4 原形态：Total/Filled/Failed + Failures 元组清单 + HasFailures；RecordFailure 同 key+kind 去重——同因不双报）。落 Core 换来 xUnit 双轨 4 用例，dotnet 基线 85 → **89 绿**。
  - **RegistryFiller**（`Runtime/Lua/RegistryFiller.cs`）：行驱动填充（Tbuiform.LuaPath / Tbcontententry.Entry / Tbstrategy.LuaPath）→ 两段式与根一致性校验 → `HasCached` 预检 → `DoString("return require('…')")` → 非表判定 → `registry.Fill(全路径, 逻辑表)`；逐行 try/catch 记入继续；错误语义三分（路径非法 / **未注册**=配置漏配 / **注册失败**=lua 炸了）；收尾 **LuaKeys 常量校验**（已知常量必须可注册，漏配当场暴露——§2.0 消费点落地）。策略行空路径跳过不计（§4.4）。
  - **三注册表类型化**：`LuaRegistries.cs` 三个标记接口（IUILuaRegistry/IContentLuaRegistry/IStrategyLuaRegistry，元素同为 LuaTable，容器按 Type 键控重复注册抛，以标记接口区分）+ 三个空子类。**Core 的 `LuaRegistry<T>` 由 sealed 放开继承**（唯一改动，子类仅构造传 kind，禁重写行为）。
  - **接线五步序落位**：GameEntry.Awake 步骤 4.5 `AddComponent<LuaComponent>`（不进 DI）→ CreateFsm 构造三注册表实例 + RegistryFiller（依赖 config + lua）构造注入两流程 → ProcedureLaunch 追加三行 RegisterInstance（Seal 前）→ ProcedurePreload 锚点下方 ①预载 → Init → ②DoMain（+TickEnabled=true）→ ③FillAll → ④HasFailures 逐条 Log.Error + Fail 阻断进 Error 流程 → ⑤放行。
  - **LuaComponent 增诊断口 `HasCached(requirePath)`**（区分未注册/注册失败用，免 require 抛错后字符串匹配）。
  - **补齐 `Content/DemoEntry.lua`**：TbContentEntry 行 Entry=Content.DemoEntry 已在表（gen_lua_keys 同源），Lua 侧文件缺失 → 填充必失败阻断启动；按 UIMain 同款 class 风格补最小处理器（M4 解释器消费，OnEnter/OnExit 壳）。
  - 验证口径：游戏程序集 dotnet 编译 **0 错误**（IDE 生成 csproj 为显式清单，临时补入新文件仅作语法检查用，入库面不受影响）；Core 测试 89 绿。**Play 冒烟（预载 9 文件→注册表填充 2 行→放行进 Main；删行/改路径报错区分）待 Unity 重导入后执行**。
  - **Play 冒烟实测通过（2026-09-12，Unity MCP）**：编译零错误 → Play → 控制台链全通（`Lua 全量预载完成:9 个文件`〔8→9，DemoEntry 入列〕→ `main.lua loaded: UI.UIMain = true` → `main.lua 执行完成`，全程零 Error）→ 反射取证：**FSM=ProcedureMain**（⑤放行达成，即 ④HasFailures=false）、LuaComponent Main=True/Tick=True、容器解析三注册表——UI.Has(UI.UIMain)=true、Content.Has(Content.DemoEntry)=true（补齐的 Lua 文件生效）、Strategies Generation=0（空路径全跳过）、`ui.Get("UI.UIMain")` 返回真 **LuaTable**。错误语义抽查：①`require('No.Such.Module')` → `Lua 预载缓存未命中:No.Such.Module`（filler 据此归**未注册**）②`error('boom')` → 纯 lua 报错无未命中标记（归**注册失败**）③抽查后 MainExecuted 仍 true，宿主无损。整批 2/2 填充、0 失败，验收线 1/2/3 关闭。
- 2026-09-12（步骤 2.5 + 2.6 交付 + Play 实测）：
  - **2.5 服务桥绑定**：`Bridge.Data` 增 Lua 侧入口 `GetItemLua/GetUIFormLua`（行字段手工展开为 LuaTable + 按行缓存，M2 预留同构位兑现；C# 侧冗余行缓存删除——Luban DataMap 已自缓存）+ `GetContentEntry`；`Bridge.BindRegistries`（ProcedureLaunch Seal 前注入 ui/content 注册表）+ `Bridge.Ui.GetLogic(id)` / `Bridge.Content.GetProcessor(id)` **骨架门面**（id → 行路径列 → 注册表查逻辑表，方法最小集）；`ClearLuaCaches`（§2.7 编排调用）。**门面全走白名单委托 `Func<int, LuaTable>`（2.1 生成代码已含）——零再生成**；只导出三门面，注册表/玩法系统零出口。LuaComponent.BindBridge 在 Init 内组装全局表。
  - **2.6 事件桥**：`EventBridge`（Runtime/Lua/）——`events.on` 为 **Lua 闭包**（诊断口装配 shim，捕获 C# 回调表后清场；返回注销委托，core/eventer.lua 同款语义，零白名单新增）；桥内显式映射注册表（`Map<TEvent>(luaEvent, pack)` 一行注册）；派发 = C# 订阅 → 按映射名逐回调 **SafeCall**。M3 最少集：1 个机制验证事件 `BridgeProbeEvent`（嵌套公开件，M5 起被真实玩法事件替代——"别造假事件"即不再扩展此词汇）。生命周期：Init 创建、**Shutdown 先于 env.Dispose 释放**（退订 C# 事件）。
  - **SafeCall 提前交付**（原列 2.7）：`Core/Util/SafeCall.cs`（Invoke Action/Func 两形态，异常落地 Log.Error，Func 塌陷返回 fallback）+ xUnit 4 用例——**dotnet 基线 89→93 绿**。用例教训：Log.ErrorCount 是静态累计值，断言须取增量。
  - **Play 实测通过（验收线 2/6 关闭）**：服务桥——`Bridge.data.GetItem(1001)` = **道具1**|Count=10（精确命中）、rawequal 证实同 id 恒同一 LuaTable 实例（按行缓存生效）、GetUIForm 字段展开正确（LuaPath=UI.UIMain）、`ui.GetLogic(id)`/`content.GetProcessor(id)` 返回注册表逻辑表、`Bridge.registry/Bridge.ui.Get` = nil（**门面收口**）。事件桥——`events.on` 返回 function（注销委托）；C# `Publish(BridgeProbeEvent)` → 抛异常回调被 SafeCall 隔离（`[SafeCall:event[bridge.probe]#1] LuaException` 落 C# 日志）且**回调②照常收到 ping1（派发链存活）**；`off()` 幂等双调后 ping2 仅回调②收到、ping3 零回调（**注销配对生效**）；全程 Error 台账仅 ping1 一条隔离记录。
