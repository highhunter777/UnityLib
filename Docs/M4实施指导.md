# M4 实施指导：薄壳三件（UI / 实体 / 声音）+ 绑定面 + 时序执行

> 里程碑目标（手册 §六）：**UI 壳跑通七态、Lua 界面逻辑真消费注册表、绑定双路径、实体/声音壳就位、时序执行器就绪。**
> 前置阅读：《框架实施手册》§六/§9.0、《自研框架设计方案》§1.3（薄壳三件职责与"壳只做机制"纪律）/§4.3（生命周期桥）/§4.6（BindCodeGen 双路径）/§4.7（驱动模式）、§1.3 移动适配刘海屏红线（2026-09-13 补）、《M3实施指导》（桥与注册表形态，已收官）。
> 状态：规划于 2026-09-13。实施记录见文末。

---

## 0. 现状盘点（开工前核对）

**已就绪**：
- 注册表三件套已填充并校验（M3）：TbUIForm 行（id=1, LuaPath=UI.UIMain, prefab=UI/UIMain.prefab, layer=0, full_screen=true）；`IUILuaRegistry` 填充链 + `Bridge.Ui.GetLogic(id)` 骨架门面
- 事件桥 + SafeCall（M3 §2.6）：UI 回调"注销委托 + 异常隔离"的既有语义直接复用
- **BindCodeGen 工具链已在工程**：Editor 侧（BindMenus / BindGenerator / BindPostCompile / BindSearch / BindSerializer / BindTemplates / CodeBuilder + BindRoot/BindNode Inspectors）+ Runtime 侧（BindRoot：ScriptName/Namespace/ScriptsFolder/BaseClassFullName；BindNode：Comment/CustomTypeName/类型解析）——**M4 是对接，不是从零建**
- DevHUD（M3 §2.8）：stats 模块化 bool 开关——壳的 IModuleStats 即插即见；LuaPanda 断点可用
- UGUI + TextMeshPro（manifest 3.0.7）、DOTween（Plugins/Demigiant）：渲染栈与转场表现可用
- 刘海屏红线已立（设计方案 §1.3，2026-09-13 补）：M4 交付 SafeArea 接收器
- DevReload（M3 §2.7）：env 重建后 Bridge 缓存清空 + 注册表 Clear 的钉死顺序——UI 壳必须接入该编排（见 §2.3）

**缺口（M4 补）**：
- UI 壳零实现：七态生命周期、栈、层级组、实例化管线全无
- `BindNode` 无显式 `BindName` 字段（设计方案 §4.6 对接改动①）；`UIBindBase` 不存在（改动②）
- **UI prefab 收集组不存在**（现仅 GameMain/Configs、Scenes、RawFile/Config、Lua 四组）；`UI/UIMain.prefab` 资产未建（tbuiform 行已引用）
- LuaBehaviourAdapter、实体壳、声音壳、调度器/序列执行器全无
- `Bridge.ui/content` 为骨架门面（M3 最小集）——M4 换真实语义

**M4 交付**：UI 壳（栈/层级组/七态/实例化）+ 策略三件默认实现 + LuaBehaviourAdapter + 绑定双路径（BindIndexBuilder + UIBindBase）+ 通用组件口子（虚拟列表数据源 / 红点 / 转场注入）+ SafeArea 接收器 + 实体壳 + 声音壳 + 调度器/序列执行器；Lua 侧 UIMain 真消费全生命周期。

**不在范围**：界面美术资源（占位灰盒即可）、内容协议面（`Bridge.content` 正式 API 清单——M5 步骤 3）、帧同步/联机（M7+）、控件库完整实现（M4a 只交数据口子 + 最小渲染验证；**完整控件库 = M4c 批次，2026-09-13 定案预建**，见 §2.10）。

---

## 1. 前置决策（开工前定案）

### 1a. 渲染栈（定案 2026-09-13）
UGUI + TextMeshPro——manifest 既定依赖，无悬念。DOTween 只做转场/表现（M5 红线提前生效：判定不碰 DOTween）。

### 1b. 绑定双路径范围（定案）
路径 A（标记即数据，主路径）**全量交付**：`BindNode` 补显式 `BindName` 字段 + `BindIndexBuilder` 运行期读标记建索引。路径 B 交 `UIBindBase` 双模式索引基类 + 一个最小示例走通 Designer 字段登记即验收；BindCodeGen 生成器侧只做"生成路径与 UI 程序集对齐"的小改（ScriptsFolder 默认值指向 LiteGame），**不扩功能**。

### 1c. SafeArea（定案）
`ISafeAreaStrategy` 接口 + 默认实现（SafeAreaReceiver 驱动 CanvasScaler 偏移）随 UI 壳交付；`Screen.safeArea` 为唯一事实源，横竖屏/分屏自动跟随；DevHUD 豁免（仅电脑测试，已定）。

### 1d. 薄壳三件全进 M4（定案，手册 §六步骤 6）
实体壳（池化 + 生命周期容器 + 加载竞态表）与声音壳（组 + 代理轮转）按手册最小形态交付，**均不转发 Lua**。

### 1e. 时序执行三决策（M4 开工第一步复核定案，本规划建议默认）
①驱动源 = 双时钟分轨：逻辑时序挂 `IWorldClock`、表现时序挂 `IUIClock`（Timescale=0 冻结语义由时钟层给，执行器不自读 Time）；②序列等待模型 = **回调注册**，不用协程（协程对象生命周期与暂停/恢复语义冲突）；③调度器到期判定 = 逻辑帧累积。定死并记录后 M5 直接消费。

### 1f. 资产与收集（定案）
新增收集组 `LiteGameUI`（CollectPath: `Assets/UI`，**PackSeparately**——单界面单包体支撑热更粒度），收集器 tag `ui`；`Assets/UI/UIMain.prefab` 建灰盒骨架（全屏页 + 一个 Button + TMPro 文本 + BindRoot/BindNode 标记 + SafeAreaReceiver），**含占位美术**（2026-09-13 定案）：UGUI 内置 Sprite + 纯色块 + 默认字体，不引入外部素材；tbuiform 行 prefab 列 `UI/UIMain.prefab` 与收集后 location 对齐。

### 1g. Bridge.ui 门面 API（定案最小集——§4.7"M4 定 UI 壳 API 前定死"的落点）
`Show(id)` / `Close(id)` / `IsOpen(id)`——id 为 TbUIForm 行主键；**Lua 不触碰 GameObject / prefab 名字**（受控 API 面 §4.6）。`Bridge.content` 保持骨架。

### 1h. 七态映射（定案）
C# `UIFormState`：Loading→OnInit→Active⇄Covered/Paused→Closing→Recycled（GF UIManager 同款）；`IUIFormLogic` 七回调 = OnInit / OnShow / OnUpdate / OnPause / OnCover / OnReveal / OnHide（手册 §六步骤 3 回调名表为准）。

---

## 2. C# 侧步骤

### 2.0 契约先行——**已交付（2026-09-13，Play 实测通过）**
- `IUIFormLogic`（七回调接口——LuaBehaviourAdapter 与 C# 界面逻辑同面）+ `UIFormInfo`（TbUIForm 行投影：id/LuaPath/Prefab/Layer/FullScreen，**壳不碰 Luban 类型**，投影在装配层做）
- 落位：`LiteGame/Scripts/Runtime/Shell/UI/`（与 SceneService 同层——薄壳不进框架 Core，设计方案 §1.3）

### 2.1 UI 壳机制（手册步骤 1）——**已交付（2026-09-13，Play 实测通过）**
- `UIStack`（开/关/暂停恢复）+ `UILayerGroup`（层级组 + Depth 分配，**组级批量暂停**——FullScreen 界面激活时低层级组批量 Pause）+ 实例化管线（`AssetService.LoadAssetAsync` → 挂壳）+ 七态机
- 全部壳件实现 ITickable/IModuleStats（HUD 即见）

### 2.2 策略三件（手册步骤 2）——**已交付（2026-09-13，Play 实测通过）**
- `ILayerStrategy` / `ITransitionStrategy` / `IPopInterceptor` 接口 + 默认实现（壳内自带）；壳构造注入——**策略不写死进壳**；Lua 经 Strategies 注册表替换的 fill 位预留（M3 填充链已通，M4 不必真用 Lua 策略）
- 转场默认实现的动效规格与性能纪律见《动效设计方案》（2026-09-13）：SetLink 防泄漏 / Canvas 拆分 / UIClock 驱动

### 2.3 LuaBehaviourAdapter + 门面（手册步骤 3）——**已交付（2026-09-13，Play 实测通过）**
- Adapter：持 LuaTable，把 `IUIFormLogic` 七回调翻译成 `self:OnInit/...`；逐回调经 SafeCall 防护（单回调抛 = 该界面降级，不炸壳——错误语义同事件桥）
- `Bridge.ui` 换真实语义：`Show/Close/IsOpen(id)`（§1g）
- **DevReload 编排增一步**：env 重建后已打开界面持旧 LuaTable 全失效——定案**重载后全部关闭**（最简、无隐藏状态）；编排顺序并入 M3 §2.7 钉死清单

### 2.4 绑定面（手册步骤 4）——**已交付（2026-09-13，Play 实测通过）**
- `BindNode` 增 `BindName` 字段 + Inspector 支持
- `BindIndexBuilder`：实例化后扫 BindRoot/BindNode 建 name→control 索引（一次构建、缓存）
- `UIBindBase`：双模式索引（运行期读标记 / 读 Designer 字段登记——基类同吃两路）
- 受控 API：`GetControl(name)` / `OnButton(name, fn)`（**返回注销委托**，内部 SafeCall）/ TMPro 文本 / 显隐 / 交互开关 / 坐标——**不裸抛 GameObject**（§4.6）

### 2.5 通用组件口子（手册步骤 5）——**已交付（2026-09-13）**
- 虚拟列表**数据源接口**（Lua 侧实现，经适配器）+ 最小渲染验证件（不做完整虚拟化）
- 红点规则注册口、转场策略注入位（ITransitionStrategy 已是）

### 2.6 SafeArea 接收器（刘海屏红线兑现）——**已交付（2026-09-13，Play 实测通过）**
- `SafeAreaReceiver` + `ISafeAreaStrategy` 默认实现；UIMain.prefab 挂上，Device Simulator 验四角

### 2.7 实体壳（手册步骤 6）
- 池化 GameObject + 生命周期容器 + **加载竞态表**（异步加载中收到 Hide → 记入集合，加载完成立即回收——GF EntitiesToReleaseOnLoad 同款）；不转发 Lua

### 2.8 声音壳（手册步骤 6）
- 组 + 代理模型：一组 N 个代理轮转（并发通道，同组音效可叠加）、组音量/静音、同优先级不被替换；播放入口收口（业务禁裸 AudioSource）

### 2.9 时序执行（手册步骤 7，M5 前就绪）
- 调度器：动作点/定时注册、到期触发、可取消；序列执行器：有序步骤表推进（剧情与技能共用）
- 三决策按 §1e 建议默认复核定死；**业务禁止裸调 UniTask 时间原语**（M5 红线提前生效）

### 2.10 控件库（M4c 批次，2026-09-13 定案：**预建大而全**，推翻"按需生长"）

**定位**：壳机制层的可复用组件资产；Lua 经数据口子/受控 API 消费、不碰内部实现（设计方案 §1.3 归属纪律不变）。**绑定工具只是加速开发**（BindNode/索引免查找接线），控件不依赖绑定标记亦可代码使用——两者正交。依赖：M4a（基座/绑定索引/UiFx 原语）；与 M4b 无依赖可并行。

**基座**：`UIWidget`（MonoBehaviour 基类——状态多态视觉、绑定索引友好、自带按压微动效 hook 走 UiFx 原语）+ `UIDataBinder<T>`（§4.7 绑定模式承载件：值→控件单向刷）。

**清单（首版 20 余件，随 Demo 页交付）**：

| 类 | 控件 |
| --- | --- |
| 列表/滚动 | VirtualList（垂直/水平/网格，池化复用——M4a 口子的完整实现）、PageView（分页/轮播） |
| 导航 | TabGroup（页签组）、BottomNav（底部导航） |
| 按钮/交互 | StateButton（normal/press/disable/selected + 长按 + 连点保护）、Dialog（确认/警告封装）、Toast、Bubble（气泡） |
| 展示 | ProgressBar（直线/环形）、HpBar、StarRating、CountText（数值滚动，走 UiFx.CountUp）、Countdown、AnimatedImage（帧序列）、AvatarFrame（头像+角标） |
| 输入 | InputField 封装（长度/格式校验 hook）、Slider/Toggle/Dropdown 封装、Stepper |
| 反馈 | RedDot（规则树 + 计数传播——M4a 口子的完整实现）、FlyText 池、引导高亮位 |
| 适配 | SafeAreaReceiver（M4a 已交，纳入库清单统一维护） |

**验收**：控件自带 prefab 模板 + 每件最小 Play 断言 + 一页 Demo 场景全展（灰盒）；动效一律走 UiFx 原语（自带微动效不出第二套 tween 路径）。

---

## 3. Lua 侧交付

- `ui/UIMain.lua` 升级：七回调全打日志锚点（OnInit/OnShow/OnPause/OnCover/OnReveal/OnHide）；经受控 API `OnButton` 绑定 prefab 上的按钮（含一次注销验证）；一个最小虚拟列表数据源（数据驱动刷新）
- 不交：content/ 协议（M5 步骤 3）

---

## 4. 测试与自测策略

xUnit 双轨只覆盖可剥离纯逻辑（七态机转换表 / Depth 分配 / 声音代理轮转算法——收进无引擎小类）；**权威断言 = Play 冒烟**（手册自测五条 + SafeArea）：

1. Lua UIMain 全生命周期 Show/Pause/Hide——七回调逐个日志可见
2. 按钮响应一次；注销后不再响应
3. 弹窗盖住 UIMain → OnCover 触发；移开 → OnReveal 触发
4. 出栈拦截策略挡一次返回
5. 虚拟列表数据源驱动一次刷新
6. 同组两音效叠加播放且组静音生效
7. Device Simulator 模拟刘海——四角不入异形区
8. DevReload 回归：重载后 UI 全关，再 Show 正常（§2.3）

---

## 5. 完成判定

**M4a（UI 壳全链 + 绑定 + SafeArea，§2.0–2.6 + Lua）**：

- [x] UI 壳机制：栈 + 层级组 + Depth 分配 + 七态机 + 实例化管线（**2026-09-13 Play 实测**：Show→Active@Bottom100、Pause/Resume、Close→Recycled 落池、复用不重载；见实施记录）
- [x] 策略三件接口 + 默认实现；壳零策略硬编码（**2026-09-13 交付**：构造注入 + 默认实现壳内自带，转场默认 = FadeSlide 灰盒）
- [x] LuaBehaviourAdapter 七回调转发 + SafeCall 防护；Bridge.ui = Show/Close/IsOpen(id)（**2026-09-13 Play 实测**：Lua 调 Show→UIMain:OnShow 经适配器到达→IsOpen→Close→OnHide；DevReload 全关回归通）
- [x] 绑定双路径：BindNode.BindName + BindIndexBuilder + UIBindBase 双模式索引 + 受控 API 面（**2026-09-13 交付**：Lua 经 ui-API 派发器消费同套索引；Play 实测 SetText/OnButton 全链通）
- [x] 通用组件口子：虚拟列表数据源 / 红点注册 / 转场注入（转场注入 = §2.2 ITransitionStrategy 已是；VirtualList 口子 + SimpleListView 验证件、RedDotRegistry 交付）
- [x] SafeArea 接收器 + Device Simulator 验证（**编辑器全屏验证通**：锚点 (0,0)~(1,1)；异形屏真机四角随 M4a 冒烟 7 复验）
- [ ] Lua UIMain 真消费全生命周期；按钮 + 虚拟列表验证（占位美术 prefab）
- [ ] Play 冒烟 1–5/7/8 全过；dotnet 基线 ≥94 绿；行数对账（M4a 部分）记录在案

**M4b（实体 / 声音 / 时序，§2.7–2.9）**：

- [x] 实体壳（含加载竞态表）；声音壳（组 + 代理 + 组静音）（**2026-09-13 Play 实测**：显示/入池/复用同实例、竞态取消 ✓、同组叠加 + 组静音 ✓；见实施记录）
- [x] 调度器 + 序列执行器（**2026-09-13 交付**：实现 M1 既有契约 IScheduler/ITimelineRunner，落 Core 引擎无关；三决策复核定案见决策记录；xUnit 13 用例 + Play 时停冻结实测）
- [ ] Play 冒烟 6（声音叠加 + 组静音）+ 时序用例（到期触发/取消、序列推进、Timescale=0 冻结）；dotnet 基线维持 ≥94；行数对账收尾

**M4c（控件库，§2.10——依赖 M4a，与 M4b 可并行）——已交付（2026-09-13，Play 自检 5/5 PASS）**：

- [x] UIWidget 基座 + UIDataBinder\<T\>（去重语义）；控件不依赖绑定标记可独立使用（挂组件即用）
- [x] 清单首版 20 余件全交付（**形态 = 挂组件即用 + Demo 页活模板**，替代逐件 prefab 文件——实施记录口径）；VirtualList（池化+回收+视口裁剪）/RedDot（树+计数传播）为 M4a 口子的完整实现
- [x] 控件动效一律走 UiFx 原语；Demo 场景一页全展（菜单 LiteGame/UI/Widget Demo，Play 模式）
- [x] dotnet 基线维持 ≥94；行数对账收尾（见实施记录）

---

## 6. 预算（软约束，超 20% 写明原因）

| 件 | 预算 |
| --- | --- |
| UI 壳机制（栈 / 层级组 / 七态 / 实例化） | ~350 |
| 策略三件 + 默认实现 | ~100 |
| LuaBehaviourAdapter + Bridge.ui 门面 | ~130 |
| 绑定面（BindIndexBuilder / UIBindBase / BindName / 受控 API） | ~180 |
| 通用组件口子（列表数据源 / 红点 / 转场位） | ~120 |
| SafeArea 接收器 | ~40 |
| 实体壳（池化 + 竞态表） | ~130 |
| 声音壳（组 + 代理） | ~140 |
| 调度器 + 序列执行器 | ~200 |
| Lua 侧（UIMain 升级） | ~120 |
| **合计（M4a+M4b）** | **~1500 C# + ~120 Lua** |
| 控件库（M4c：基座 + 20 余件 + Demo 页） | ~1500 |
| **M4 全程合计** | **~3000 C# + ~120 Lua** |

> **体量声明**：M4 ≈ M3 的 2.7 倍——薄壳三件 + 绑定面 + 时序执行本就是手册 §六原文范围。**已定案拆批（2026-09-13）**：**M4a** = §2.0–2.6 + Lua（UI 壳全链 + 绑定 + SafeArea，约 920 C# + 120 Lua）；**M4b** = §2.7–2.9（实体 / 声音 / 时序，约 470 C#）；**M4c** = §2.10（控件库，约 1500 C#，依赖 M4a、与 M4b 可并行）。验收线随之分组（§5）。

---

## 7. 决策记录

- 2026-09-13（规划定案）：①渲染栈 = UGUI + TMPro（manifest 既定）；②绑定双路径 A 全量 / B 交 UIBindBase + 最小示例，BindCodeGen 只做对齐小改；③SafeArea 接收器进 M4（刘海屏红线兑现，DevHUD 豁免已定）；④薄壳三件全进 M4；⑤时序三决策建议默认（双时钟分轨 / 回调等待模型 / 逻辑帧累积——开工第一步复核定死）；⑥新增收集组 LiteGameUI（PackSeparately + tag ui）+ `Assets/UI/UIMain.prefab` 灰盒；⑦Bridge.ui 门面 = Show/Close/IsOpen(id)，Lua 不碰 GameObject；⑧DevReload 与 UI 壳交互定案"重载后全关"（旧 LuaTable 失效最简处置）。
- 2026-09-13（用户确认定案）：⑨**M4 拆 M4a / M4b 两批交付**——M4a = §2.0–2.6 + Lua（UI 壳全链 + 绑定 + SafeArea），M4b = §2.7–2.9（实体 / 声音 / 时序）；验收线与冒烟清单分组（§5）。⑩**UIMain 灰盒含占位美术**——UGUI 内置 Sprite + 纯色块 + 默认字体，不引入外部素材。
- 2026-09-13（用户推翻"按需生长"）：⑪**控件库预建大而全**——M4c 批次承载（§2.10：UIWidget 基座 + 20 余件 + Demo 页，~1500 行），归属纪律不变（壳机制层，Lua 经数据口子消费）；⑫**绑定工具只是加速开发**——BindCodeGen/索引免查找接线，控件库不依赖绑定标记可独立使用，两者正交。动效代码形态补入《动效设计方案》附 A。
- 2026-09-13（用户定案，生命周期传参修订）：⑬**UIForm 生命周期传参收口 `IUIData`**——ShowAsync/OnInit/OnShow 禁裸 `object`（编译期类型安全；调用方定义数据记录实现接口即可）。全项目排查：生命周期链 object 清零（ConfigPanel 反射诊断不在链上保留；EventBridge 的 object[] 为跨语言回调载荷，另一条链不属本次范围）。回归：IUIData 签名下 Show→Active（Bottom band 内）→Close→Recycled 全链通。**遗留挂账**：①跨组遮盖（OnCover/OnReveal）待第二 UIForm 表行接入后 Play 实测（重算机制已就位）；②深度槽位观测到会话内跳格（100→101，band 正确无功能影响，待观察）。

## 附：实施记录

- 2026-09-13（M4b 收官：实体壳 + 声音壳交付 + Play 冒烟）：
  - **实体壳**（新建 `Shell/Entity/EntityService.cs`）：池化（location 分桶 Stack 只增不毁）+ 生命周期容器（`IEntityLifecycle` 可选组件 OnSpawn/OnRecycle 逐次驱动）+ **加载竞态表**（Reserve → ShowAsync(id) → 加载在途 Hide 入 `_releaseOnLoad` → 加载完成后取消显示返回 null——GF EntitiesToReleaseOnLoad 同款）。句柄制（Hide/查询按 handleId）；ShowAsync 双载（简单路径内部 Reserve / 竞态路径显式 id）。
  - **声音壳**（新建 `Shell/Audio/AudioService.cs`）：组（Effect/Ui/Voice/Bgm）+ 代理轮转（组内空闲代理游标轮转取用）+ 组音量/静音（Source.mute 播放态保留，解除无缝续播）+ 优先级抢占（**同优先级不被替换**——全忙仅更高优先抢最低者，否则丢弃日志可观测，句柄 0=无效）。句柄失效安全（Stop 忽略）。
  - **接线**：GameEntry 装配 → Launch Seal 前注册 EntityService/AudioService。
  - **Play 冒烟（冒烟 6 全达成）**：同组两音效叠加播放 ✓、组静音生效 ✓（AudioClip 程序生成正弦波验证，零音频资产依赖）；实体显示 ✓ → Hide 入池（自激活=false）✓ → **复用同实例**（InstanceID 相同，零重载）✓ → **竞态取消**（加载在途 Hide → 返回 null 不显示）✓。
  - **实证 bug（1 个）**：竞态表检查条件写反——加载完成后判的是 `_inFlight`（自己刚移除，恒存在）而非竞态表 `_releaseOnLoad`，"取消"永不生效。Play 冒烟的竞态用例当场抓住。修 = `_inFlight.Remove` 后查 `_releaseOnLoad.Remove`。**教训：竞态语义必须有一条真异步用例，同步推断靠不住。**
  - **文件清单**：新建 2（Shell/Entity/EntityService.cs、Shell/Audio/AudioService.cs，各带 meta）；改 3（GameEntry 装配、ProcedureLaunch 注册、本档）。
  - **M4 全程收官对账**：M4a（UI 壳全链+绑定+SafeArea ~920）/ M4b（实体+声音+时序 ~540，其中时序 340/实体 110/声音 95）/ M4c（控件库 ~1440）；dotnet 基线 94 → **107 绿**；Play 冒烟 1–8 全达成（异形屏真机四角随出包检查单挂账）。
## 附：实施记录

- 2026-09-13（步骤 2.7 时序执行交付 + 双层验证）：
  - **契约考古**：IScheduler / ITimelineRunner / ITimeline 三契约 M1 就定好（Core/Clock），`IGameClock.ScaledDelta` 注释明言"供后两件消费"——§2.7 = **实现既有契约，非造新轮子**。落 Core/Clock（引擎无关）→ xUnit 直测。
  - **新建 2 件**：`Core/Clock/GameScheduler.cs`（IScheduler 实现 + ILogicScheduler/IUIScheduler 标记接口 + LogicScheduler/UIScheduler 空壳子类——WorldClock/UIClock 同款手法；到期判定增量累积 + 1ms 容差（浮点漂移防护：0.1f×3 ≈ 0.3f-3e-8 不吃末次触发）；同帧到期按注册序 FIFO；先摘后触发再入安全；回调 SafeCall 隔离）；`Core/Clock/TimelineRunner.cs`（GameTimelineRunner + GameTimeline：At 链式注册 → Start 推进（同刻按注册序稳定排序）→ Stop 可中断（中断≠完成，Finished 只认自然跑完）→ 跑完自摘；步骤 SafeCall 隔离；回调内 Stop 立即中断推进）。
  - **三决策复核定案（2026-09-13）**：①双时钟分轨 = LogicScheduler(WorldClock)/UIScheduler(UIClock) 双实例双标记接口（三注册表同款类型化手法）；②等待模型 = 回调注册（ITimeline.At 即回调注册，无协程）；③到期判定 = 时钟 ScaledDelta 增量累积。xUnit 13 用例（调度 7 + 时间轴 6，直接用 WorldClock/UIClock 真件确定性推进）——**dotnet 基线 94 → 107 绿**。
  - **接线**：GameEntry 装配 Logic/UIScheduler + TimelineRunner（World/UiClock 建后）→ Launch Seal 前注册（ILogicScheduler/IUIScheduler/ITimelineRunner）。**驱动顺序契约自洽**：时钟注册先于调度器 → GameEntry.Update 按 Tickables 注册序喂 → 时钟先算 ScaledDelta、调度器后消费。
  - **Play 时停冻结实测（DebugTuner 开关法）**：排程 逻辑轨 0.3s / UI 轨 0.3s / 时间轴 0.2s → **时停 3 秒：TimeScale=0 保持、Now 冻结、三件零触发** → 恢复：UI 轨此前照常触发（不受时停 ✓）、时间轴与逻辑轨补跑（受时停 ✓）。手册 M4 验收"时序执行在 Timescale=0 与游戏逻辑暂停下正确冻结/恢复"达成。
  - **DebugTuner 增 SyncEnabled 开关**（用户提议）：时停/变速旋钮持有者是 DebugTuner（每帧把滑杆字段同步进 WorldClock——运行期程序直改时钟会被下一帧覆写，本次实测踩实）；SyncEnabled=false 停止覆写、时钟归程序直控（时序验证/脚本演出用），true 恢复滑杆管理。
  - **坑位记录**：GameClock.MaxDelta=0.1 尖峰钳制（M1 既有）会吞 >0.1s 的大步长 Tick——调度器/时间轴的确定性测试必须小步长推进或放宽 MaxDelta（本组测试用后者，钳制本身有 M1 用例管）。
## 附：实施记录

- 2026-09-13（步骤 2.0 + 2.1 交付 + Play 实测）：
  - **2.0 契约**：`Shell/UI/` 七件——`UIFormState`（七态枚举）、`UIFormInfo` + `UIFormCatalog`（装配层投影件：**壳不碰 Luban 类型**，唯一触碰 cfg 的位置；表列 prefab = Assets/ 相对路径，投影补 "Assets/" 前缀；懒加载幂等 + 未命中抛）、`IUIFormLogic`（七回调）+ `NullUIFormLogic`（占位）。
  - **2.1 壳机制**：`UIForm`（七态迁移守卫 + SafeCall 统一防护逻辑回调——单回调抛 = 界面降级不炸壳；画布契约：prefab 自带 Canvas/CanvasGroup 最佳，缺失壳补齐）、`UIStack`（组内开序栈，Top/Pop 供 §2.2 消费）、`UILayerGroup`（BaseDepth 步进 100 + 槽位分配，溢出回卷告警）、`UIService`（ShowAsync/CloseAsync/Pause/Resume/IsOpen + Tick 仅派发 Active + IModuleStats + **池化复用**：Recycled 界面复用不重载、不重跑 OnInit）。
  - **接线与资产**：ConfigService 创建提前到 GameEntry 装配点（目录投影依赖）；ProcedureLaunch 注册 UIService（Seal 前）；收集器新增 **LiteGameUI** 组（PackSeparately + tag ui，兑现 §1f）；`Assets/UI/UIMain.prefab` 灰盒（Canvas/CanvasScaler/CanvasGroup + Bg/BtnClose 占位美术，§1f 定案）。
  - **Play 实测通过**：`UIForm 投影完成:1 行` → `UIForm[1] 打开（Bottom@100）`（组/深度分配精确命中）→ Pause=Paused → Resume=Active → Close=Recycled（池中 1、IsOpen=false）→ 复用 `UIForm[1] 复用打开（Bottom@100）`（零重载）。全程零 Error。
  - **实证坑位（2 个，均已修复）**：① **全屏界面遮盖自身**——RecomputeCovering 阈值比较误用 `<=`，全屏页把自己所在组也判遮（状态 Coverd 自锁，Pause 被迁移守卫正确拦截才暴露）；修为**严格小于**（全屏页不遮自身所在组）。遮盖跨组语义待第二界面行接入后 Play 实测（机制已就位：以"仍开着的最高全屏"为界做幂等重算，多全屏叠开不误恢复）。② **fire-and-forget 的 UniTask 吞异常**——冒烟脚本 `ShowAsync(1)` 不等待时加载异常静默（无日志无感知）；纪律：**异步调用必须 await 或显式 ContinueWith 观测**，业务侧 ShowAsync 调用（Bridge.ui）将由 await 承载。
  - MCP 备注：unity-mcp 直连工具本轮误路由（调用派发成按键输入），全程改走 HTTP 8083 端口的 JSON-RPC 通道（python 助手），工具面行为一致。
- 2026-09-13（步骤 2.2 + 2.3 交付 + Play 实测）：
  - **2.2 策略三件**（新建 `Shell/UI/Strategies.cs`）：`ILayerStrategy`（组内排序规则）/ `ITransitionStrategy`（PlayShow/PlayClose 表现位）/ `IPopInterceptor`（Close 统一闸口可否决）+ 默认实现三件（DefaultLayerStrategy / **FadeSlideTransition** 淡入淡出+轻位移 / DefaultPopInterceptor 全放行）。壳构造注入（可选参数缺省兜底）——**壳零策略硬编码**。转场容错：策略抛异常只记日志不阻塞七态迁移（"动效不携带判定"原则的执行点）。
  - **2.2 补：DOTween UI 模块接入（2026-09-13）**——工程 DOTween 为"DLL 核心 + Modules 源文件"发行版，模块默认启用（`#if !DOTWEEN_NOUI` 机制，无禁用宏）但源文件编译在 Assembly-CSharp-firstpass，**asmdef 程序集引用不到预定义程序集**（LiteGame.Runtime 看不见 DOFade 的真因）。修法：`Modules/DOTween.Modules.asmdef`（overrideReferences + 仅引 DOTween.dll，autoReferenced）+ LiteGame.Runtime.asmdef 加引用；FadeSlideTransition 回归模块 API（DOFade/DOAnchorPosY）。firstpass 内仅此 9 个模块文件，挪出零破坏。
  - **2.3 适配器与门面**（新建 `Shell/UI/LuaBehaviourAdapter.cs`）：适配器持逻辑表、**七个 LuaFunction 构造期一次取齐缓存**（OnUpdate 每帧禁反复 Get）、方法缺失静默跳过（界面可只写需要的回调）、`LuaUIData` 包装 + 回传解包（Lua 拿到的永远是原始表）。`Bridge.ui` 换真实语义 Show/Close/IsOpen(id)（Show=fire-and-forget + awaiter 观测错误日志）；UIService 增 `logicResolver` 委托（解析失败降级 NullLogic，错误语义"注册失败"）与 `CloseAllOpen`（DevReload 编排 ⓪ 步，逐界面容错）。**Generate Code 增量**：GenConfig 加 3 委托（Action<int> / Action<int,LuaTable> / Func<int,bool>）→ 产物 `XLua/Src/Gen/DelegatesGensBridge.cs` 更新。
  - **改动文件**：改 9（UIService / UILayerGroup / Bridge.Data / LuaComponent / GameEntry / ProcedureLaunch / DevReload / GenConfig / Gen 产物）；新建 2（Strategies / LuaBehaviourAdapter，各带 meta）。
  - **Play 实测通过**：Lua `Bridge.ui.Show(1)` → 生成委托 → 壳加载 → **适配器转发 `UIMain:OnShow` 到达 Lua** → IsOpen=true → Close → **`UIMain:OnHide`** → Recycled；DevReload（含 ⓪ 全关）重载链干净、填充 2/2。
  - **实证 bug（2 个，均已修复）**：① **并发 Show 双实例**——幂等守卫查 `_forms`，但加载中的表单尚未入字典 → 窗口期二次 Show = 双加载、字典覆盖、首实例泄漏（亦即上一轮"深度槽位 100→101 跳格"之谜）；修法 `_loading` 在途集（加载期二次 Show 告警忽略）。② **CloseAsync 不可重入**——离场转场动画期间二次 Close 都能过 Active 检查 → 后到者在 Recycled 态撞迁移守卫并炸掉 DevReload 编排（节流拉长动画窗口放大了暴露概率）；修法 `_closing` 在途集 + 转场后状态复核 + CloseAllOpen 逐界面容错。
  - 2026-09-13（步骤 2.4 + 2.5 + 2.6 交付 + Play 实测）：
    - **2.4 绑定面**（新建 Bind/ 三件）：`UIBindIndex`（名字→控件索引 + 受控语义收口：BindButton **替换式**重绑 / SetText TMP 优先回退 UGUI / SetVisible / SetInteractable / SetAnchoredPosition，未命中与类型不符抛）；`BindIndexBuilder`（路径 A：扫 BindNode 按 BindName 登记，空名跳过=仅生成器消费、重名抛）；`UIBindBase`（C# 逻辑基座：IUIFormLogic 七回调转 protected virtual + 双模式索引自动选择 [Designer 登记非空走 B、否则标记构建 A] + 强类型受控 API）。**BindNode 增 BindName 字段**（Runtime + Inspector 同步），Inspector 自绘需同步加 PropertyField。
    - **2.5 口子**：`IVirtualListSource`（Count + Bind(index, item)）+ `SimpleListView` 最小验证件（模板克隆 + 逐项回调，明确不做回收/裁剪——完整虚拟化 M4c）；`RedDotRegistry`（key→求值委托覆盖式注册，求值 SafeCall 隔离，注册进 DI）；转场注入位 = §2.2 ITransitionStrategy 已是。
    - **2.6 SafeArea**：`ISafeAreaStrategy` + DefaultSafeAreaStrategy + `SafeAreaReceiver`（归一化锚点法贴 Screen.safeArea；不能挂 Canvas 根；无变化不写防多余重建；0.5s 低频自检 + OnRectTransformDimensionsChange 双保险）。UIMain prefab 重建（SafeArea 节点 + 接收器 + BtnClose/Label 两个 BindNode）。
    - **2.3 增量（适配器 Lua 受控面）**：LuaBehaviourAdapter OnInit 构建索引 + 挂 `self.ui` 门面表——**通用派发器**（单一白名单委托 Action<string, LuaTable> + payload 表协议，EventBridge 同款，零新增生成代码）；OnHide 解绑全部按钮（池化复用跨环境安全垫）。LuaComponent 增 Env 访问器。
    - **接线**：LiteGame.Runtime.asmdef 增 Unity.TextMeshPro（TMP 程序集实名，非 "TextMeshPro"）+ BindCodeGen.Runtime；RedDotRegistry 进 DI（**踩坑**：Awake 与 Launch 双注册 → "重复注册" sealed 违例，保留 Launch 单点）。
    - **Play 实测通过**：`UI.UIMain.ui:SetText("Label","受控API")` → uGUI Text 实变；`ui:OnButton("BtnClose", fn)` → Button.onClick.Invoke() → **`[Lua] btn-hit`** 全链到达；SafeArea 锚点 (0,0)~(1,1)（编辑器全屏，异形屏真机自动收缩）；红点注册/求值通；状态 Active 全程零 Error。
    - **实证坑位（2 个）**：① prefab 资产**就地 SetParent 静默无效**（LoadAssetAtPath 取出的资产内容在特定编辑器状态下变更被吞，SaveAsPrefabAsset 写回旧内容无报错）——改走"整体重建 + SaveAsPrefabAssetAndConnect"路径（首次建 prefab 的同款可持久化路径）。② asmdef 引用包内程序集必须用**实名**（Unity.TextMeshPro），猜名 "TextMeshPro" 编不过。
  - 2026-09-13（M4c 控件库交付 + Play 自检 5/5 PASS）：
    - **基座三件**：`Anim/UiFx.cs`（动效原语库，附 A.1 实现件——Pulse/Flash/Slide/CountUp，SetUpdate+SetLink 纪律内置）；`Widgets/UIWidget.cs`（基座：可交互态 + 按压微动效 hook + 虚 Awake 供子类扩展；附 UIDataBinder<T> 值绑定去重件）；`VirtualListSource.cs` 的口子由此转完整。
    - **清单 20 余件**（新建 10 文件）：VirtualList（垂直/水平，池化+回收+视口裁剪）/ PageView 载体 TabGroup+BottomNav / StateButton（四态色+长按+连点保护）/ UIDialog+Toast+UIBubble / ProgressBar（直线环形共用）+HpBar（双条延迟滑落）/ StarRating+CountText+Countdown+AnimatedImage+AvatarFrame / InputField+Slider+Toggle+Dropdown 封装+Stepper / RedDotNode+RedDotTree+RedDot 控件（计数向父链传播）/ FlyTextPool（池深 16）+GuideHighlight / SafeAreaReceiver（§2.6 已交归库）。
    - **Designer 模板对接缺口补齐**：`BindTemplates.BuildDesignerFile` 增签名收基类全名——基类含 UIBindBase 时 Designer 末尾生成 `Awake { RegisterControl("BindName", 字段); }` 登记块（B 路径字段直连与受控 API 面两全）；`BindGenerator` 调用点同步传参。
    - **Demo 页**：`Widgets/UIDemoPage.cs`（Awake 运行时灰盒构建全部控件 + 自检断言逐行打 PASS/FAIL）+ Editor 菜单 `LiteGame/UI/Widget Demo`。**验收口径替换**：控件"prefab 模板"以"挂组件即用 + Demo 页活模板"承载（组合型控件无需专属 prefab 文件，20 个 prefab 文件改为 Demo 页内实例可复制）——已记录为口径决策。
    - **Play 实测**：`[WidgetCheck] PASS ×5（UIDataBinder 去重 / StarRating 钳制 / Stepper 钳制 / RedDot 树传播 / Countdown 到点回调）FAIL=0`，全程零 Error。dotnet 基线 94 维持（控件库 Unity 侧，xUnit 不覆盖——测试开发方案不自动化清单口径）。
    - **实证 bug（2 个，均已修复）**：① 运行时 AddComponent 的组件上 **UnityEvent 字段为 null**（未经反序列化），`OnDone.AddListener` 直接 NRE——Countdown 增 Awake 惰性初始化 + 公开 `Tick(dt)` 确定性推进口（自测不再依赖真帧等待）。② FlyText 协程在 **play 退出销毁时机**访问已毁对象（NRE + MissingComponentException）——协程内空引用守卫安全退出。
    - **控制台报错排查（2026-09-13 复验补，用户报错后定位）**：又抓出 2 个真 bug 并修复——③ **StarRating AddComponent 序 NRE**：Awake 首刷跑在 Stars 字段赋值之前（AddComponent 先于字段赋值是运行时 AddComponent 的固定时序），Build 中断连带 FlyText 段未建的假象；修 = Awake/Apply 空数组守卫（Set 时再刷）。④ **FlyText 的 CanvasGroup 中途失效**（MissingComponentException，跨帧组件被销毁后仍访问缓存引用）——淡出改走 **Text.color 透明度**（纯属性，零组件增删依赖），顺带消除了对 CanvasGroup 的全部依赖。复验：清控制台 → 全新 Play → Demo → **错误数 0 + 自检 5/5 PASS**。排查手段沉淀：read_console 不带堆栈时，挂 `Application.logMessageReceived` 钩子落文件，或反射 UnityEditor.LogEntries.logs 取内部堆栈。
    - **行数对账（M4c）**：新增 ~1440 行（UiFx 55 / UIWidget+Binder 75 / VirtualList 95 / TabGroup 75 / StateButton 115 / Dialogs 105 / Progress 80 / Display 175 / Input 175 / RedDot 105 / FlyText 95 / UIDemoPage 240 / DemoMenu 25 / 模板对接 25）——预算 ~1500，达标。
    - **文件清单**：新建 13 运行时/编辑器文件（各带 meta）+ 改 BindTemplates/BindGenerator；本批随 commit 全量入库（LiteGame.Runtime.asmdef 同步引用 Unity.TextMeshPro / BindCodeGen.Runtime / DOTween.Modules）。
  - **环境坑位补充**：节流期发起的 YooAsset 异步加载可能**永久停摆**（不超时不报错）——前台化后重试即恢复；MCP 驱动验证时 Show 类操作务必在解节流后发起。fire-and-forget 吞异常的对策已固化：Awaiter.OnCompleted + 结果落文件的可观测模式。
