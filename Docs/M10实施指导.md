# M10 实施指导：网络层（状态同步 · kcp2k 双通道 · 服务器权威）

> 依据：《状态同步实施方案》**§4.1–4.6**（传输选型 / 双通道 / 拓扑 / 定帧追帧 / RoomServer 结构与协议矩阵 / 服务器选型与可替换性）、**§3.2**（定帧公式与带宽估算）、**§3.4.1**（命中延迟补偿 = 服务器回溯）、**§5.6**（重连——M11 编排，M10 交付服务器能力）、**§7**（INetworkService 契约与三点接法）、**§8.3**（BattleContext 生命周期）、**§9 M10 行**（验收定义）、**§10 红线**（1/3/4/6/7/9/11）、**§11-2/6/7**（开工检查）；《服务端架构设计》**§10**（战斗服收口 + **E1 背压降级 / E3 抗放大——"M10 起带"**、§10.1/10.2 MVP 不拆分 / 单循环）。
> 前置：**M9 已交付**（`SnapshotRing` 容量参数化 / `RollbackSim` 预测回滚 / `InputHistory` / 同运行时结论——服务器 .NET 权威重放自洽）；M8 收口批实测结论（Mono FMA 1-ULP 跨运行时底噪）作为和解率预期基线。
> 定位：**联机骨架**——RoomServer 权威模拟 + 双通道传输 + 增量快照 + 客户端预测/和解 + 服务器回溯。M11 可玩 demo 的网络地基；顺序纪律 M9→M10 已过门（快照回滚先于联网验收）。
> 工期估算：**5–7 天**；预算 ~2100 行（含测试 ~600）。

---

## 0. 现状盘点（开工前核对）

| 项 | 状态 |
| --- | --- |
| `LiteSim.Core` | ✅ M8/M9 全量：`Step`/`SimChecksum`/`FrameDriver`/`SnapshotRing`/`RollbackSim`/`InputHistory`；.NET 8 原生可跑（`noEngineReferences` 兑现） |
| 回滚机制 | ✅ M9：深度 8 / 越界退化 / 上限 2 / 防共享用例全绿（L1 现 320） |
| `SnapshotRing` 参数化 | ✅ 服务端回溯环直接 `new SnapshotRing(LagCompHistory)`（16），零改动 |
| 跨运行时结论 | ⚠️ M8 实测 Mono FMA 1-ULP → **M10 验收全部在 .NET 侧闭环**（无头客户端）；Unity 客户端的和解率基线观测留 M11 demo |
| `INetworkService` 契约 | ✅ **批①已落**：`Assets/LiteFramework/Scripts/Unity/Net/INetworkService.cs`（`ITickable + IModuleStats`；枚举在 Core） |
| kcp2k | ✅ **批①已 vendor**：`Assets/LiteNet/Vendor/kcp2k`（MIT 源码入库） |
| protobuf | ✅ **批①已接**：`.proto` 单源 + `scripts/gen-proto.ps1` + `Vendor/Google.Protobuf` + `link.xml` 段；另补 vendor `System.Runtime.CompilerServices.Unsafe 4.0.4.1`（修引用闭包加载失败，提交 `c0be0dd`） |
| `LiteNet` / 测试工程 | ✅ **批①已建**：`Assets/LiteNet/`（`Protocol/` 纯函数 + `Transport/` 双通道 + 双轨 `LiteNet.csproj` + asmdef `noEngineReferences`）、`Tests/LiteNet.Tests/`（Packer/Codec/Snapshot/TransportLoopback 4 件）——**均已并入 `Tests.slnx`（L1 可见）** |
| `RoomServer` | ✅ **批②③ 已交付、批④ 待做**：`ServerHost/Session/SessionManager/Room/InputGate`（批②，`eb8b9e2`）+ **`Differ` 差分广播 / `AoiFilter` / `LagCompensator` 回溯 / `ServerLoop` 定拍 / `ReconnectService` / `Ops`**（批③）；权威循环（FrameAggregator 并入 `Room`）60Hz 定拍、30Hz 广播；无头对跑 harness 属批④ |
| 批②③ 前置件（**已提前交付**） | ✅ `Protocol/SnapshotSource.cs`（`ISnapshotSource` 解耦口——决策 7 的落地形态：批② 全量占位跑通验收，批③ 换增量时**循环结构零改动**，实际实现为两段式 `BeginFrame`/`BuildFor`）；✅ `Protocol/BuildHash.g.cs` + `scripts/gen-build-hash.py`（覆盖 Sim+Proto+Protocol 源码、行尾归一化、确定性排序）+ `Tests/LiteNet.Tests/BuildHashTests.cs` 拦"忘重跑" |
| HybridCLR 排除 | ⚠️ `LiteSim.Core` 已排除；**`LiteNet` 新程序集需同步加入排除清单**（§11-2，启动期断言）——清单不在版本库内，**本机未核对** |
| 客户端接缝 | ⚠️ **服务器侧镜像已备**：`SnapshotReassembler`（增量重建 + 分配器修复）+ 重连响应（快照 + 输入历史）已交付；`RoomClient`、`RollbackSim.OnAuthoritativeSnapshot`、`KcpNetworkService` 桥接属批④ |
| 输入面 | ✅ **Aim 方向改造已收尾**（《角色状态与动作实现设计》§7-1 实施记录）：`SimInputFrame.Yaw → AimX/AimZ` + `InputHistory` 补漏 + `battle.proto` 重生成 + 基线重录。批②③ 已在实际消费输入（含 `ButtonFireFlag` 回溯补判标记位与按键位白名单） |

> **对账（2026-09-19，批③ 后刷新）**：本表已按批① / 批② / **批③** 交付实物刷新。**M10 实际进度 = 批① ✅ + 批② ✅（`eb8b9e2`）+ 批③ ✅（快照差分/AOI/回溯/E1·E3/Ops/定拍循环）+ 批④ 待做**，与「附：实施记录」一致。L1 实测 **320 绿**（LiteFramework 203 / LiteSim 75 / LiteNet **42**）；L2 编译 0 错误 + EditMode 4/4。

---

## 1. 前置决策（开工前定案）

| # | 决策 | 理由 |
| --- | --- | --- |
| 1 | **`LiteNet` 程序集形态**：`Assets/LiteNet/`（asmdef `noEngineReferences: true`，references 仅 `Google.Protobuf` 预编译 DLL）+ 双轨 `LiteNet.csproj`（`.dotnet/` 产物重定向，同 `LiteSim.Core` 模式）；**禁 UniTask**——异步只用 BCL `Task`（Server .NET 控制台共用，UniTask 依赖 PlayerLoop） | §1 分层表 + §7.1；`.gitignore` 白名单补 `!Assets/LiteNet/*.csproj` |
| 2 | **kcp2k vendor**：源码入库 `Assets/LiteNet/Vendor/kcp2k/`（MIT 可再分发，与 xLua 同策略；版本锁定记录在本文件）；`Google.Protobuf` DLL vendor 进 `Assets/LiteNet/Vendor/Google.Protobuf/` + `link.xml` 追加段 | §4.1；生成物入库 → 运行期无需 protoc/网络 |
| 3 | **协议 = protobuf 代码生成**：`.proto` 单源 `Assets/LiteNet/Proto/battle.proto`；生成脚本 `scripts/gen-proto.ps1`（protoc 版本固定写进脚本注释）；产出 C# 入库 `Assets/LiteNet/Proto/Generated/`，两端共用。消息面 = §4.5 协议矩阵 9 个包（Join/JoinAck/StartGame/Input/StateSnapshot/MismatchReport/心跳/Leave/ReconnectReq/ReconnectResp） | §4.5-7 protobuf 定案；schema 单源 > 体积（+25% 已接受） |
| 4 | **`LiteNet` 内部分层**：`Protocol/`（打包解包纯函数，零 IO 零状态零 kcp2k 依赖）+ `Transport/`（kcp2k 收发、双通道、冗余、Session——唯一允许碰 kcp2k）；房间组件依赖窄端口 `IRoomTransport`（Broadcast/SendTo/OnData 3 方法，~20 行） | §4.6 可替换性两刀；换传输框架时 `Protocol/` 原封不动 |
| 5 | **RoomServer 落位**：仓库根 `RoomServer/`（.NET 8 控制台工程；csproj 经 `Compile Include` 引 `Assets/LiteNet` + `Assets/LiteSim/Core` 源码——双轨同款；`.gitignore` 白名单补行）。**MVP 形态：单进程多房间、每房间单线程 Sim、单循环（无 I/O 线程）、不拆 Gate/Sim** | §4.5；《服务端架构》§10.1/10.2（形态已定，MVP 不拆/单循环）；拆分时 Sim 代码零改动（窄端口已保证） |
| 6 | **权威循环**：`FrameAggregator` = `PeriodicTimer` 60Hz 裸循环：到点 → 聚合本帧输入（缺席者沿用空输入）→ `AuthSim.Step` → SnapshotDiffer → 广播。迟到输入进下一帧；快照尽力广播，客户端绝不阻塞等快照 | §4.5 关键机制 1/3 |
| 7 | **增量快照**：`SnapshotDiffer` 对比上一广播帧副本提取变化槽位（protobuf `SlotDelta`：zigzag + 量化整型），**带 frameNo + 全量 checksum + ackInput**；全量兜底：客户端 ack 落后 >N 帧或每 60 帧定期发全量。30Hz 起步（`SimConfig.SnapshotHz` 旋钮） | §4.5-7；checksum 供客户端和解判定（4 B 成本） |
| 8 | **命中延迟补偿 = 服务器回溯**：客户端 `Input` 包带 `viewFrame`（= ackSnapshot 推导，§3.4.1）；服务器收到开火输入 → `SnapshotRing(LagCompHistory=16).TryRestore(viewFrame)` → 在历史态上执行 `ShootingSystem.Run`（单系统执行，不推进 Frame）→ 恢复当前态。**不改 SimStep 顺序**——回溯判定是服务器单侧动作，不触 Sim 纯函数红线；`LagCompHistory=0` 关闭退路保留 | §3.4.1；M9 环复用；客户端不预判补偿结果（开火本就不预测） |
| 9 | **客户端预测形态 = 全量预测（GGPO 式，机制复用 M9）**：客户端 `RollbackSim` 跑全量 Sim——本地玩家零延迟预测、远端玩家沿用预测（M9 §5.3 策略原样）；服务器确认不逐帧下发远端输入，**和解统一走快照级**：`RollbackSim` 新增 `OnAuthoritativeSnapshot(frame, state, checksum)`——快照到 → 取本地环内同帧预测 checksum 比对 → 不符 → Restore 权威态 → 重放 frame+1..now（本地历史）→ 计数上报 `MismatchReport`。混合形态（远端走快照镜像）留调优，和解率不可接受时再议 | §5 章头"回退源=权威快照、重放范围=本地输入"；M9 零结构改动、全量复用；远端误差每 33ms 被快照覆盖 |
| 10 | **带宽预算锚点（首次实测前红线，实施记录校准）**：输入上行 ≤ 6 KB/s/客户端（16 B×60 + KCP/UDP 头 52 ≈ 4 KB/s 理论值）；快照下行 ≤ 32 KB/s/客户端（8 人满负荷全图 AOI、30Hz 增量，理论 ~18 KB/s + 冗余与头）；全量兜底帧不计常规预算（触发即计数观测） | §3.2 估算方法延续；"≤ 预算"验收需要预算先存在 |
| 11 | **E1 背压降级（M10 起带）**：per-client 发送队列上限 → 超限逐级降级（降快照率 → 收缩 AOI → 丢低优先级实体） | 《服务端架构》§10-E1：基线无背压，一个慢客户端会拖垮房间 |
| 12 | **E3 抗放大加固（M10 起带）**：连接 cookie（防 UDP 放大）+ per-IP 限速 + 重连**一次性票据** | 《服务端架构》§10-E3 |
| 13 | **AOI（MVP 验证等价性）**：网格过滤旋钮 `AoiCellSize/AoiRadius` 进 `SimConfig`，只影响广播、不影响判定与回滚重放；验收 = 开/关 AOI 结果一致 + 带宽差可观测 | §4.5-7；E2 优先级调度明确 **M10 之后**（不做） |
| 14 | **验收 harness = 全 .NET 无头对跑**：`RoomServer` 进程 + 2 个无头客户端进程（脚本输入 + `RoomClient` + `RollbackSim`）+ **网络损伤模拟器**（`LoopbackDamageSim`：延迟/丢包/乱序注入，测试 harness 专用，不进生产 `Transport/`）。Unity 侧本批只交付 `KcpNetworkService : INetworkService` 桥接（可编译），视觉装配留 M11 | 同运行时红线（M8 FMA 结论）；"同机两客户端对跑 5 分钟"用无头形态自动化 |
| 15 | **测试工程**：`Tests/LiteNet.Tests/`（第三个 dotnet 测试工程并入 `Tests.slnx`，`.gitignore` 白名单补行）——Protocol 纯函数 / SnapshotDiffer / 损伤模拟 / 无头对跑（子进程或同进程内嵌 Acceptor）全在其内 | L1 门禁延续；服务器件全可 dotnet 侧测 |

---

## 2. 分步实施（逐件施工图）

**批次划分**（四批，层叠推进）：

| 批 | 内容 | 目的 |
| --- | --- | --- |
| **第一批：LiteNet 骨架与协议** | 2.1 vendor（kcp2k/protobuf）+ asmdef/csproj/白名单 + 2.2 `.proto` 与生成 + 2.3 `Protocol/` 纯函数 + 2.4 `INetworkService` 契约 + `Transport/`（KcpPeer 收发/双通道/冗余） + 用例 | 传输与协议单源先冻结 |
| **第二批：RoomServer 权威循环** | 2.5 Acceptor/Session/Room（cookie/限速/buildHash）+ 2.6 FrameAggregator + AuthSim（缺席沿用）+ 输入两层校验 + E1 背压 + Ops 指标 + 用例 | 服务器能独立跑 60Hz 权威局 |
| **第三批：快照与回溯** | 2.7 SnapshotDiffer（增量/全量兜底/checksum/ackInput）+ AOI 网格过滤 + 2.8 服务器快照环 + 回溯判定（§3.4.1）+ ReconnectResp 能力 + 用例 | 广播与延迟补偿成形 |
| **第四批：客户端接缝与验收对跑** | 2.9 `RoomClient`（LiteNet）+ `RollbackSim.OnAuthoritativeSnapshot` 和解扩展 + `KcpNetworkService` 桥接（LiteGame）+ 2.10 损伤模拟器 + 无头双客户端 + §9 M10 验收全组 | 拿满里程碑验收 |

### 2.1 vendor 与工程骨架（~0 代码，纯装配）

`Assets/LiteNet/Vendor/{kcp2k, Google.Protobuf}`；`LiteNet.asmdef`（`noEngineReferences: true`）；`Assets/LiteNet/LiteNet.csproj`（双轨，`Directory.Build.props` 重定向 `.dotnet/`）；`RoomServer/RoomServer.csproj` + `Tests/LiteNet.Tests/LiteNet.Tests.csproj`；`.gitignore` 白名单三行；HybridCLR 排除清单登记 `LiteNet`。

### 2.2 协议定义与生成（`Proto/`，~120 行 .proto + 生成物）

消息面按 §4.5 协议矩阵；`Input(frameNo, wire[≤4 帧冗余], ackSnapshot, viewFrame)`；`StateSnapshot(frameNo, slotDeltas[] 或 fullState, checksum, ackInput, isFull)`。`scripts/gen-proto.ps1` 固定 protoc 版本；生成物入库。

### 2.3 `Protocol/`（~250 行）：打包解包纯函数

InputWire 打包（最近 4 帧冗余）、快照解包、信令序列化——零 IO 零状态零 kcp2k 依赖（决策 #4）。**用例**：往返序列化逐字节一致；冗余解包取最新有效帧；坏包丢弃不抛。

### 2.4 `Transport/` + 契约（~350 行）

kcp2k `KcpPeer` 收发、双通道分流、冗余发送、Session（心跳/超时）；`INetworkService`（§7.1 原文 ~30 行，接口在 `LiteFramework.Unity`，枚举 `NetState/NetChannel` 在 Core）。

### 2.5 RoomServer 会话与房间（~300 行）

Acceptor（握手→cookie→token/buildHash 校验→Session）、SessionManager（心跳/超时踢除/断线标记）、RoomManager + Room（成员表 playerId↔session）。E3 加固 + E1 队列上限骨架。
**批内必做（2026-09-17 盘点登记）**：①**假传输/假会话测试驱动**（无头客户端在第四批，本批用例必须靠 `IRoomTransport` 的测试实现 + 脚本输入注入，否则"60Hz 权威局"无从验收）；②**E1 数值落定**（队列上限、三档降级阈值——§10-E1 只给了顺序）+ **Ops 指标输出形式**（控制台周期打印？频率？指标集？）。

### 2.6 权威循环（~200 行）

`FrameAggregator`（`PeriodicTimer` 60Hz）：聚合（缺席沿用空输入）→ `SimStep.Step` → 服务端快照环 `Capture`（`LagCompHistory`）→ SnapshotDiffer → Broadcast。输入传输层校验（每帧≤1 条/玩家、ackSnapshot 单调且 ≤ 已广播帧号、非法帧号丢弃）——**Clamp 口径**：非法只丢弃不改写（拒绝会引发预测分叉，见《角色状态与动作实现设计》§4.2⑤）。Ops 控制台指标（帧号/房间数/快照均长/丢包率/和解率汇总，输出形式见 §2.5 登记）。
**广播用 `ISnapshotSource`**（本批注入 `FullSnapshotSource` 全量占位；第三批换 `SnapshotDiffer` 增量，循环零改动）；**`SimConfig.SnapshotHz=30`** 控制抽帧节拍。

### 2.7 SnapshotDiffer + AOI（~300 行）

上一广播帧副本对比 → 变化槽位 → 量化 zigzag；全量兜底（每 60 帧或 ack 落后）；AOI 网格过滤（旋钮）。**用例**：增量→全量往返一致；静止场景增量趋零；AOI 开/关判定结果一致。

### 2.8 回溯判定（~150 行）

开火输入带 `viewFrame` → 环回溯 → `ShootingSystem.Run` 单系统执行（历史态）→ 命令与帧事件**丢弃**（回溯只判命中，不结算当帧——结算在权威当前帧消费该判定）→ 恢复当前态。**用例**：150ms 模拟下"命中开火者所见位置"（§9 验收用例）；`LagCompHistory=0` 退化。

### 2.9 客户端接缝（~400 行）

`RoomClient`（连接/收发/冗余/ack 上报）；`RollbackSim.OnAuthoritativeSnapshot`（~120 行：环内同帧 checksum 比对 → 不符 → Restore 权威 → 重放本地历史 → `OnReconcile` 计数）；`MismatchReport` 上报；`KcpNetworkService : INetworkService`（LiteGame 桥接，本批只求编译通过）。**用例**：注入快照不一致 → 和解后与真值一致；快照超前（halt 态）→ 直接覆盖续跑。

### 2.10 损伤模拟与无头对跑（~250 行 harness + 验收组）

`LoopbackDamageSim`（延迟/丢包/乱序注入）；无头客户端（脚本输入 + RollbackSim）；验收组见 §3。

---

## 3. 测试与自测策略

| 组 | 用例 |
| --- | --- |
| **协议往返** | 打包→解包逐字节一致；4 帧冗余解包取最新；截断/坏包丢弃 |
| **权威循环** | 60Hz 定拍下输入聚合顺序确定；缺席沿用；迟到输入进下一帧 |
| **SnapshotDiffer** | 增量+全量重建 == 全量直拷；静止增量趋零；AOI 开/关一致；ackInput 正确 |
| **回溯判定** | 150ms 模拟延迟：命中"开火时刻目标位置"而非当前位置；深度 16 窗口外退化为当前帧判定 |
| **和解** | 快照与预测不符 → 重放后与后续快照收敛；halt 态快照超前覆盖续跑；`MismatchReport` 计数正确 |
| **E1 背压** | 队列超限 → 快照率降档 → AOI 收缩，房间其余客户端不受慢客户端拖累 |
| **E3 加固** | 无 cookie 握手拒答；超速丢包；重连票据一次性 |
| **对跑验收（§9 M10 行）** | 同机 2 无头客户端 + RoomServer 对跑 **5 分钟**：帧号稳定推进无累积落后、无死锁；**10% 丢包**不卡死（和解率上升但收敛）；人为注入不确定性（客户端篡改本地态）→ 快照覆盖兜底不发散；30Hz 带宽 ≤ §1-#10 预算（实测记录）；和解触发率全程可观测（Ops 输出） |
| **纪律** | 扫描器 0 违规（LiteNet 守 R5——禁裸 UNITY_EDITOR，双端编译红线；R1–R4 属 Sim 确定性纪律不适用；Vendor/ 第三方源码全局排除）；L1 全绿（207 + 新增） |

---

## 4. 完成判定（对账 §9 M10 验收）

- [ ] **同机两客户端对跑 5 分钟无卡顿累积**（无头 harness 自动化）——**批④**
- [ ] **模拟 10% 丢包下不卡死**（恢复不发散）——**批④**
- [x] **和解触发率可观测**（客户端上报 + 服务器 Ops 汇总）——**批③ 服务器侧已交付**：`MismatchReport` 只计数汇总（`ServerHost.HandleMismatch` → `Ops.MismatchReports`），控制台 5s 周期打印（`Ops.Format`：帧号/快照/输入/和解/回溯/背压一行式）。客户端上报侧（`RollbackSim.OnAuthoritativeSnapshot` 计数后上报）属批④；**M8 FMA 1-ULP 底噪**预期基线：无头验收（全 .NET）无此底噪，Unity 客户端观察值留 M11
- [ ] **人为注入不确定性 → 服务器快照覆盖兜底不发散**——**批④**
- [ ] **30Hz 快照下带宽 ≤ 预算**（§1-#10 锚点）——**批④** 实测记录（批③ 已备好观测点：`SnapshotSent`/`SnapshotFullSent`/`Differ.LastDeltaCount`）
- [x] **延迟补偿用例：高 ping（模拟 150ms）开火命中"客户端看到的那个目标位置"** —— **批③ 已交付**：`Tests/LiteNet.Tests/LagCompensationTests.高延迟开火_命中客户端所见位置_而非目标当前位置`（20 帧推进 + 9 帧延迟，断言 `LastTargetFrame` = 客户端所见帧、回溯帧目标距离 < 当前帧、且伤害命令落在当前帧缓冲；含窗口外退化、失效射手、不推进帧/不消费随机数三例）
- [x] 协议单源（`Protocol/` 零 kcp2k 依赖）+ `IRoomTransport` 窄端口就位（可替换性两刀）——**批①/③ 保持**（批③ 新增的 `SnapshotDiffer`/`AoiFilter`/`SnapshotReassembler`/`ProtocolConstants` 全部零 kcp2k 依赖）
- [x] 输入两层校验（传输层挡非法 + Sim 内 clamp）+ buildHash 版本拒绝 —— **批② 交付**；批③ 补 **按键位白名单**（`InputGate.AllowedButtons`：未定义位丢弃，防伪造 `ButtonFireFlag`）与 **ackSnapshot 记账**（`Session.LastAckSnapshot`）
- [x] E1 背压降级 + E3 抗放大（M10 起带项）交付 —— **批② 骨架 + 批③ 落地**：E1 = per-client 水位记账（`Session.SendQueueBytes/AckedBytes`）→ 三档降级（抽帧 / 收缩 AOI / 裁最远实体）+ 滞回恢复，`ProtocolConstants` 数值定案；E3 = kcp2k 连接 cookie（内建）+ per-IP 限速（批②）+ **重连一次性票据**（`ReconnectService`：一次性 + 5 分钟时效 + 绑定席位）。**实测观测点**：`RoomBroadcastTests.背压超限_该客户端降档抽帧_其余客户端不受拖累`（其余客户端零降档）
- [ ] （**待 HybridCLR 接线**，2026-09-17 口径修正）`LiteNet` 排除 HybridCLR 热更 + 启动期断言；`buildHash` 握手校验的**服务器侧**在本批落地（生成器与常量已备）——批③ 已把 `ServerBuildHash` 从字面量串切到 `BuildHash.Value`（源码内容哈希）
- [x] 纪律 0 违规；L1 全绿；RoomServer/无头客户端在 .NET 8 一键可跑（脚本留档）—— **批③ 实测**：L1 **323 绿**（LiteFramework 203 / LiteSim 75 / LiteNet **45**）；L2 编译 0 错误 + EditMode 4/4；`dotnet run --project RoomServer -- --duration <ms>` 一键可跑（`ServerLoop` 整数锚定 60Hz + 有界追赶，实测 3000ms = 188 tick、掉时债 0/放弃追帧 0）
- [x] 行数对账（超 20% 写明原因）—— **批③ 自研 +2185 −104**（预估 §2.7/2.8「~450」+ 客户端接缝拆分后的服务器侧件），超预算的主因见实施记录「批③ 交付」段

---

## 5. 预算

| 件 | 预算（行） |
| --- | --- |
| vendor 装配 + 工程/白名单/生成脚本 | ~50（不含第三方与生成物） |
| `Proto/` 定义 + 生成物 | ~120（+ 生成物入库） |
| `Protocol/` 纯函数 | ~250 |
| `Transport/`（kcp2k 收发/双通道/冗余/Session） | ~350 |
| `INetworkService` 契约 + `KcpNetworkService` 桥接 | ~100 |
| RoomServer（Acceptor/Session/Room/Aggregator/校验/E1/E3/Ops） | ~600（§4.6 明细） |
| SnapshotDiffer + AOI + 回溯判定 | ~450 |
| `RoomClient` + `RollbackSim` 和解扩展 | ~400 |
| 损伤模拟 + 无头客户端 harness | ~250 |
| `Tests/LiteNet.Tests` 用例 | ~600 |
| **合计** | **~3170（自研部分 ~2100 + 测试）** |

**工期**：**5–7 天**；批次划分见 §2 开头（骨架→服务器→快照回溯→对跑验收）。

---

## 6. 决策记录（本轮定案）

| # | 决策 | 备注 |
| --- | --- | --- |
| ① | kcp2k vendor 源码入库（MIT），不自研 KCP；Google.Protobuf DLL vendor + link.xml | §4.1 |
| ② | 双通道：信令 Reliable / 逐帧输入 Unreliable+4 帧冗余，单通道全 KCP 仅留退化路径 | §4.2 |
| ③ | 服务器自研 ~600 行，不用 Fantasy——协议单源价值高于框架功能 | §4.6 |
| ④ | `Protocol/`（纯函数）与 `Transport/` 分层 + `IRoomTransport` 窄端口 | 换传输框架 `Protocol/` 不动 |
| ⑤ | MVP：单进程多房间、单循环、不拆 Gate/Sim | 《服务端架构》§10.1/10.2 |
| ⑥ | 客户端**全量预测**（M9 机制原样复用），和解统一走快照级 `OnAuthoritativeSnapshot`；混合形态留调优 | 远端误差由 30Hz 快照覆盖 |
| ⑦ | 回溯判定 = 环回溯 + `ShootingSystem.Run` 单系统执行 + 恢复，不改 SimStep 顺序 | 服务器单侧动作，不触纯函数红线 |
| ⑧ | 验收全 .NET 无头闭环（同运行时红线）；Unity 桥接只求编译，装配留 M11 | M8 FMA 结论 |
| ⑨ | E1 背压 + E3 抗放大随 M10 交付；E2 优先级调度 M10 之后 | 《服务端架构》§10 |
| ⑩ | 重连：协议 + 服务器下发能力交付，客户端流程编排留 M11（§5.6） | MatchMaker 亦省（固定房间号） |

---

## 7. 边界与风险

- **不越界**：不做 Unity 视觉装配（M11：`SimView`/插值接线/`PlayerController`/场景）、不做重连客户端流程与断线重进体验（M11，服务器能力已备）、不做 MatchMaker（MVP 固定房间号/好友码）、不做 E2 快照优先级（M10 后调优）、不做 Gate/Sim 拆分与 I/O 线程（触发条件未命中）、不做帧同步预案（判据未触发）
- **风险 1：和解率底噪**——M8 实测 Mono/.NET 1-ULP 差异意味着 Unity 客户端预测与 .NET 服务器**必然周期性和解**；无头验收（全 .NET）无此底噪，M11 demo 需重新观测并记基线。**对策**：和解率是质量指标非正确性前提（快照覆盖兜底）；预期写进 Ops 文档，勿在 M11 把底噪当 bug 排查
- **风险 2：kcp2k 版本漂移**——vendor 版本锁定并记录；升级按 vendor 目录整体替换 + 回归对跑
- **风险 3：protoc 工具链依赖**——生成物入库后运行期零依赖；生成脚本注明 protoc 版本，避免本机环境差异
- **风险 4：双轨 csproj 维护**——`RoomServer`/`LiteNet.Tests` 的 `Compile Include` 随新文件漂移。**对策**：按既有模式用通配 Include + 纪律测试钉死（缺文件即编译失败）
- **风险 5：`PeriodicTimer` 节拍漂移**——60Hz 长跑累积误差。**对策**：绝对时间锚定（nextTick = start + n×dt，不 tick+=interval）；对跑验收 5 分钟帧号线性度即检验
- **风险 6：回溯判定与当前帧结算的双路径**——回溯只出命中结论，结算必须回到当前帧，两路径不一致会产生"幽灵命中/漏判"。**对策**：回溯产物只进当前帧的命令缓冲，用例钉死（150ms 用例 + 0ms 等价用例）
- **风险 7：协议单源红线回潮**——任何"服务器侧另写一个消息结构"的诱惑即违规。**对策**：RoomServer csproj 直接 Compile Include `LiteNet/Protocol`，物理上双写不可能

---

## 附：实施记录（对账 2026-09-18）

### 批次实况

| 批 | 状态 | 交付 / 缺口 |
| --- | --- | --- |
| **批① LiteNet 骨架与协议** | ✅ **已交付**（`86d6b7b`；补丁 `c0be0dd` Unsafe vendor、`a33bd39` 许可文本）；其后**追加批②③ 前置件**（未提交） | `Assets/LiteNet/`：asmdef（`noEngineReferences: true`、`overrideReferences` 只放 `Google.Protobuf.dll`、references `LiteSim.Core`）+ 双轨 `LiteNet.csproj`（net8.0、LangVersion 9.0、通配 `Protocol`/`Transport`/`Proto/Generated`/`Vendor/kcp2k`）；`Protocol/` 六件纯函数（`PacketType`/`PacketCodec`/`SnapshotCodec`/`InputPacker`/**`SnapshotSource`**/**`BuildHash.g.cs`**，零状态零 IO 零 kcp2k 依赖）；`Transport/` 三件（`IRoomTransport` 窄端口 + `KcpTransportClient/Server` 双通道）；`.proto` 单源 + `scripts/gen-proto.ps1`（Aim 改造后已重生成）+ `scripts/gen-build-hash.py` + 生成物入库；vendor `kcp2k` / `Google.Protobuf` / `System.Runtime.CompilerServices.Unsafe 4.0.4.1` + `link.xml` 段；`Tests/LiteNet.Tests/` **6 件 16 用例**（Packer/Codec/Snapshot/**SnapshotSource**/**BuildHash**/TransportLoopback）；`.gitignore` 白名单 5 行；`RoomServer/` 骨架桩入库；`INetworkService` 契约落 `LiteFramework.Unity` |
| **批② RoomServer 权威循环** | ✅ **已交付（`eb8b9e2`，2026-09-18）** | 实况：`RoomServer/` = `ServerHost`（Acceptor/连接与房间装配 168 行）+ `Session`/`SessionManager`（连接 cookie、per-IP 限速） + `Room`（房间生命周期 + buildHash 校验 + 60Hz 权威循环接 `AuthSim` 缺席沿用） + `InputGate`（输入两层校验 + **E1 背压**，108 行） + `Program`（Ops 指标启动）；用例 `Tests/LiteNet.Tests/RoomServerTests.cs`（204 行）→ LiteNet 用例 16 → **21**。全量广播走批① 交付的 `FullSnapshotSource` 解耦口，**增量替换（批③）时循环零改动** |
| **批③ 快照与回溯** | ✅ **已交付（2026-09-19）** | 待做项全部落地：`SnapshotDiffer`（增量 / 全量兜底 / ackInput / 活体变化 + 全局状态探针）+ AOI 网格过滤（`AoiFilter`，**实例持有**）+ 服务器 `SnapshotRing(LagCompHistory=16)` 回溯判定（`LagCompensator`）+ E1 三档降级与 E3 重连票据 + `Ops`/`ServerLoop`/`Program --duration`。用例 LiteNet 21 → **42** |
| **批④ 客户端接缝与验收对跑** | ❌ **未开工** | 待做：`RoomClient` + `RollbackSim.OnAuthoritativeSnapshot` + `KcpNetworkService` 桥（LiteGame）+ `LoopbackDamageSim` + 无头双客户端 + §9 M10 验收全组（5 分钟对跑 / 10% 丢包 / 带宽实测 / 和解率实测） |

### 2026-09-18 — 批② 交付（对账刷新）

**交付**（`eb8b9e2`；9 文件 / +18,246 −22 —— 其中 17,528 行为误入库的 `RoomServer/msbuild_preprocessed.xml`，已于同日清理）：

| 件 | 行数 | 说明 |
| --- | --- | --- |
| `RoomServer/ServerHost.cs` | 168 | Acceptor + 连接装配 + 房间管理入口 |
| `RoomServer/Room.cs` | 143 | 房间生命周期 + buildHash 校验 + **60Hz 权威循环**（聚合输入 → `AuthSim.Step` 缺席沿用 → 广播） |
| `RoomServer/InputGate.cs` | 108 | 输入两层校验 + **E1 背压** |
| `RoomServer/Session.cs` / `SessionManager.cs` | 47 / 33 | 连接 cookie（E3）+ per-IP 限速 + 重连票据 |
| `RoomServer/Program.cs` | 36 | Ops 指标 + 启动 |
| `Tests/LiteNet.Tests/RoomServerTests.cs` | 204 | 会话/房间/输入闸门用例 |

**实测**：L1 **287 绿**（LiteFramework 198 / LiteSim 68 / LiteNet **21**，0 失败）；L2 门禁 0 错误。

**坑位**：`msbuild_preprocessed.xml`（MSBuild 预处理输出 1.5 MB）随批② 误入库 → 已删除并加 `.gitignore` 规则；同批清理：目录 meta 补入库（`Editor/Style.meta`）、`AgentScripts/` 与 `TempProtoReflection.cs` 转忽略、`SnapshotSourceTests` 一条 xUnit2013 警告改 `Assert.Single`。

### 2026-09-19 — 批③ 交付（快照与回溯）

**交付**（38 文件 / 自研 **+2185 −104**，生成物 `Battle.cs` +264 −12 单列）：

| 件 | 行数 | 说明 |
| --- | --- | --- |
| `Assets/LiteNet/Protocol/SnapshotDiffer.cs` | 189 | 两段式差分器：`BeginFrame`（每广播帧一次：算变化集/判全量/推进金标）+ `BuildFor`（每客户端一次：按 AOI 纯读过滤） |
| `Assets/LiteNet/Protocol/AoiFilter.cs` | 104 | 网格过滤（**实例持有**网格 + (世界,帧号) 键失效；整格可见 = 可能多发半格、永不漏发） |
| `Assets/LiteNet/Protocol/SnapshotReassembler.cs` | 49 | 客户端镜像重建（全量 = 缺席即死亡；增量 = 覆盖槽位；分配器版本由 `Id>>16` 重建） |
| `Assets/LiteNet/Protocol/ProtocolConstants.cs` | 42 | 广播/背压常量集中地（全量周期 60 帧、ack 落后 30 帧、队列上限 4KB、三档降级数值、恢复 40%/2s） |
| `Assets/LiteSim/Core/Scripts/SimWorldStateSnapshot.cs` | 95 | 轻量金标摘要（逐字段位级比对；不含 Frame/RngState 之外的非逻辑字段） |
| `Assets/LiteSim/Core/Scripts/FramePump.cs` | 30 | 定次帧泵（与 FrameDriver 的分工：网络对跑 1:1 vs 渲染帧追帧） |
| `RoomServer/LagCompensator.cs` | 185 | §3.4.1 服务器回溯：环 Restore → 单系统 `ShootingSystem.Run` → 命令/事件落当前帧 → 还原（含 RngState 回滚、窗口 clamp、退化路径） |
| `RoomServer/ServerLoop.cs` | 120 | 整数锚定 60Hz 宿主（`nextTickMs = start + n×16ms`，**有界追赶 5 帧 + 丢债记账**，两形态同一套规则）+ `Run(ms)` 供验收脚本 + 单帧体注入点（过载用例驱动） |
| `RoomServer/ReconnectService.cs` | 63 | E3 重连一次性票据（一次性 + 5 分钟时效 + 绑定席位） |
| `RoomServer/Ops.cs` | 56 | 周期汇总行（帧号/快照/输入丢弃分因/和解/回溯/背压） |
| Room 广播重构 / InputGate / ServerHost / Session / Program | — | 两段式广播（差分一次 + 每客户端 AOI）、按键位白名单、ack 记账、buildHash 接生成器、重连响应、`--duration/--quiet` |
| `Tests/LiteNet.Tests/`：LagCompensation / AoiFilter / RoomBroadcast / ServerLoop / SnapshotSource 重写 | +740 | LiteNet 用例 **21 → 45** |

**实测**：L1 **323 绿**（LiteFramework 203 / LiteSim 75 / LiteNet **45**，0 失败）；L2 = 编译 0 错误 + EditMode **4/4**；RoomServer `--duration 3000` = 188 tick、掉时债 0、放弃追帧 0；真机两客户端对跑实测（临时 harness）**80 帧内仅 2 次全量**（= 60 帧周期兜底 + 首帧，符合设计）。

**坑位（本批踩到并已修，均有用例钉死）**：
1. **`FrameDriver` 不能用于网络对跑**——它是渲染帧驱动（按真实 dt 追帧/丢帧），无头客户端与服务器用它会帧号漂移（实测：服务器第 k 帧用的输入，客户端是第 k+1 帧用的）。**修**：新增 `FramePump`（定次 1:1），服务器/无头客户端一律用它。
2. **AOI 网格静态缓存串味**——只按帧号失效 → 多房间/多测试世界在同一帧号互相污染（实测：并发用例可见集合混入别的世界的实体）。**修**：网格归 `AoiFilter` 实例所有（每房间一份），键 = (世界实例, 帧号)。
3. **多客户端共用一个差分器**——每客户端各调一次 `Build` 会推进金标，第二个客户端恒发空增量（实测 116/144 次退化为全量）。**修**：两段式 API（`BeginFrame` 每帧一次 / `BuildFor` 每客户端纯读）。
4. **"全局状态变了吗"不能用 checksum 直接比**——checksum 含 **Frame**，帧号每步都变 → 每帧都判"变了"（实测表现：永远全量）。**修**：探针改比 `RngState` + Globals + CustomData（摘要外的量），且基准**每帧**推进（含全量帧，否则首帧后基准停在初始值，次帧必误报）。
5. **`viewFrame` 语义**——按《状态实施方案》§3.4.1 原文 = **客户端所见的权威帧号**（ackSnapshot 对应权威帧 + 插值帧，已是服务器帧轴的值）。服务器只做两件事：减插值量、clamp 到 `[当前帧 − LagCompHistory, 当前帧]`。曾实现为"用服务器当前帧 − ack 作偏移再对齐"，多减了一层（实测回溯帧恒等于当前帧 → 全部退化）。
6. **测试辅助函数的 EntityId 缺省 0** —— `SimInputFrame.EntityId` 缺省 0 会被 `TryResolve` 判失效实体**静默丢弃**，导致多条用例空转（"位移 0 也判过"）。**修**：所有合成输入一律显式带 `EntityId`，并在 helper 注释里写明。
7. **`gen-build-hash.py` 收录了 Editor 目录**——`Scripts/Editor/**` 不在双端共同编译源集（csproj 显式 Exclude），喂进 hash 会让"给菜单加一行注释 = 全体握手被拒"。**修**：生成器与 `BuildHashTests` 的跳过列表加 `Editor`。
8. **`.gitignore` 缺 `__pycache__/`**（跑生成器产生）——已补规则。
9. **`ServerLoop` 常驻模式缺追赶上限**（复盘发现）——`LoopBody` 落后时无界连跑 = 过载即持续满核。**修**：两形态统一"**有界追赶 + 丢债记账**"（超 <see cref="MaxCatchUp"/> 即把债从锚点时间轴上减去，计入 `DroppedTimeMs`/`CatchUpAbandoned`，Ops 行可见），并加注入点让"过载 → 有界"可被用例驱动（`ServerLoopTests.过载时_追赶有上界并记账丢债_不会无界满核`）。
10. **锚点换算走 double**（同批复盘）——`tick * 1000.0 / 60` 每帧一次浮点乘法，长期运行（数天）后 tick 很大时有累积舍入风险。**修**：整数锚点 `TickPeriodMs = 1000/60 = 16`（锚点纯整数运算），丢掉的 0.67ms/帧由**有界追上**补回——帧率长期精确、代价可观测。

**已知项（批④/M11 处理，非遗漏）**：
- **AOI 移出补发**：槽位移出可见集时客户端保留旧值 → 残影/陈旧位置，由 1s 周期性全量兜底纠正。批④ 对跑可观测（如需精确，加"移出即补发一次"）。
- **全量帧是整帧属性**：某客户端触发全量（重连/ack 落后）时，本帧对房间内所有客户端都发全量（差分金标需一义）。1s 周期兜底本就会发生，代价可接受。
- **客户端→服务器的 `ack_input`** 用"最近接受的输入帧号"（`InputGate.LastAcceptedFrame`）——语义是"你的输入到了"，不是"已消费帧"；回滚判定以真实输入帧号为准。

**待批④**：`RoomClient` / `RollbackSim.OnAuthoritativeSnapshot` / `KcpNetworkService` 桥 / `LoopbackDamageSim` / 无头双客户端 5 分钟对跑 / 10% 丢包 / 带宽与和解率实测。

### 开工前决策（§1）兑现情况

| 决策 | 状态 | 实证 |
| --- | --- | --- |
| 1 `LiteNet` 程序集形态（零引擎 + 双轨 csproj + 禁 UniTask） | ✅ | asmdef `noEngineReferences: true`；`LiteNet.csproj` net8.0 / LangVersion 9.0（注释写明"与 Unity 侧能力对齐，防双轨漂移"） |
| 2 kcp2k + Google.Protobuf vendor | ✅ | `Assets/LiteNet/Vendor/{kcp2k,Google.Protobuf}`（另补 Unsafe，来自 `c0be0dd`） |
| 3 协议 = protobuf 代码生成（单源 + 生成物入库） | ✅ | `Proto/battle.proto` + `scripts/gen-proto.ps1`（protoc 版本写进脚本头） |
| 4 `Protocol/` 与 `Transport/` 分层（唯一碰 kcp2k 的地方） | ✅ | `Protocol/*.cs` 仅 `using System/Google.Protobuf/LiteSim`，零 kcp2k 引用 |
| 5 RoomServer 落仓库根 + 引源码 | ✅（仅落地） | `RoomServer/RoomServer.csproj` 已入库；**权威循环本体未写** |
| 6–15（增量快照 / 回溯 / 预测和解 / 带宽 / AOI / 无头对跑 / 测试工程） | ⏳ | 属批③④（**权威循环已在批② 交付** ✅） |

### 当前状态（2026-09-18）

**输入面 Aim 改造已收尾**（曾一度打断编译：《角色状态与动作实现设计》§7-1 实施记录）——`InputHistory` 逐字段比较补齐、`battle.proto` 重生成、`SimChecksum.txt` 重录。
**L1 实测全绿 282**：LiteFramework **198** / LiteSim **68** / LiteNet **16**，0 失败。
**批②③ 前置件已备齐并已入库**（`ISnapshotSource` 全量占位 + `BuildHash` 握手锚点）；**批② 权威循环已交付（`eb8b9e2`）**。
**剩余动作 = 批③**（`SnapshotDiffer` 增量 / AOI 网格 / 服务器快照环 + 回溯判定），随后批④（客户端接缝 + 无头对跑拿验收）。

### 第二批开工前置（2026-09-17，本会话交付）

| 前置 | 交付 | 验收 |
|---|---|---|
| **Aim 方向改造**（决策落地：`SimInputFrame.Yaw → AimX/AimZ`） | `SimInputFrame` / `InputSystem`（`SimTrig.Atan2` 派生 `EntitySlot.Yaw`）/ `ShootingSystem`（射线直接用 Aim）/ `InputHistory.Differs` / `battle.proto` 的 `InputFrame`（`aim_x=4/aim_z=5/buttons=6`，字段重编号已记）/ 重跑 protoc 3.36.1 / `InputPacker` / 各测试合成输入 / **`Baselines/SimChecksum.txt` 重录** / `SimSandbox`（鼠标射线 → Aim，长度 ≤1 归一化） | L1 全绿；`预测正确_零回滚` 用例的"零输入 == 冷启动模板"前提恢复（曾因把脚本 Aim 设成 (1,0) 而每帧判预测错） |
| **protoc 版本与脚本** | 本机原无对齐版 protoc（只有 Luban 自带的 libprotoc 26.1，与 vendor 运行时 **Google.Protobuf 3.36.1** 不匹配）→ 按脚本注释从 nuget 取 `Google.Protobuf.Tools 3.36.1`（libprotoc 36.1）；**`scripts/gen-proto.ps1` 补 UTF-8 BOM**（原来 PS 5.1 读中文注释会解析失败） | 生成物差异仅 InputFrame 相关（无无关漂移） |
| **`SimConfig` 广播/AOI 旋钮** | `SnapshotHz=30` / `AoiCellSize=10f` / `AoiRadius=30f`（初始值，实施时校准；AOI 只影响广播） | 常量集中纪律（§11-3） |
| **快照载波窄口** | `Protocol/SnapshotSource.cs`：`ISnapshotSource` + `FullSnapshotSource`（复用第一批 `SnapshotCodec.PackFull`） | 3 条用例（字段齐备 / 只含活体且可解回 / 无状态可多房间共享） |
| **buildHash 机制** | `scripts/gen-build-hash.py` → `Protocol/BuildHash.g.cs`（覆盖 Sim/Core/Scripts + Proto + Protocol，**行尾归一化**保证跨机器一致）；RoomServer 启动打印 | 生成器幂等；`BuildHashTests`（格式 + **按同规则复算比对**，抓"改了源码忘重跑"） |
| **文档口径** | HybridCLR 勾选项降级为"待接线"；`§2.5/2.6` 登记批内必做（假传输驱动 / E1 数值 / Ops 输出形式） | — |

**验收**：L1 **282 绿**（LiteFramework 198 / LiteSim 68 / LiteNet **16** = 既有 11 + 快照载波 3 + buildHash 2）；RoomServer `dotnet run` 打印 `联机版本 hash = b8be78b64fdb42b9`。

**未提交前已完成**（本会话顺带）：框架状态机家族的 ARPG 形态扩展 + 能力分层 + Reset/RejectReason（见《状态机ARPG形态扩展施工图》《通用流程状态机施工图》§10）。

### 批④ 交付（2026-09-19，本会话：客户端接缝 + 无头对跑验收）

| 交付 | 内容 | 验证 |
|---|---|---|
| **`RollbackSim.OnAuthoritativeSnapshot`**（§2.9 和解入口） | 快照超前/环外 = 权威直接覆盖；环内 = 本地预测 checksum vs 权威 checksum 位级比对 → 不符则 Restore 权威 + 重放本地历史（全体输入：本地真实 + 远端沿用——远端误差由下一快照再纠，决策⑥ 预期内）+ `OnReconcile`/`ReconcileCount`（MismatchReport 上报接缝）；比对行 lint-allow R3（uint 位级判等） | 用例：和解触发（覆盖+重放收敛）/ 预测正确零和解（静止零输入 == 冷启动沿用）/ **快照先于 StartGame 到达的 NRE 修复**（Unreliable 快照可先于 Reliable 信令——Sim 未建即丢弃，下快照恢复） |
| **`RoomClient`**（LiteNet/Client/） | 协议矩阵 C→S 侧封装：SendJoin/SendInput（4 帧冗余+ackSnapshot+viewFrame）/SendMismatch + JoinAck/StartGame/StateSnapshot 事件化；`KcpTransportServer` 补 `Start(port)`（**服务器从未监听的根因**——ctor 未调 Start） | 回环冒烟（握手+cookie+双通道收发+广播+断开）沿用批①用例 |
| **无头对跑 harness** | `HeadlessClient`（RoomClient + RollbackSim 组合；StartGame.Seed 下发后**惰性建 Sim**——与服务器同 seed 同构世界；快照重建走直接槽位赋值保 Id 不走分配器；LocalEntityId 按快照 Slot==PlayerId 对齐）+ `HeadlessRunTests` | **短对跑 30s**：帧号稳定推进（无停顿）/双端快照到达/不发散（预测帧 ≤ 权威+MaxCatchUp）；**快照覆盖兜底**：A 静止 B 移动的远端误差注入 → 和解恢复权威 → 预测帧号有界；**5 分钟全量对跑**：`M10_LONGRUN=1` 门控（L1 默认 skip，夜间/手动跑） |

**批④ 过程中的三次排障**（各有根因，如实记录）：
1. **Join 从未发出**——`StartHeadless` 只 Connect 不 Join；修复 = HeadlessClient 连接后自动 SendJoin（真实客户端形态）。
2. **Join 在握手完成前被 kcp2k 丢弃**——ctor 里 Connect 后立即 SendJoin，但 cookie 握手需数轮 tick；修复 = Join 延迟到 transport `OnConnected` 事件后发送。
3. **ubuntu 快照 NRE ×2**——Unreliable 快照先于 Reliable StartGame 到达（UDP 包序不保证），Sim 未建时 OnSnapshot 解引用 null；修复 = Sim 未建即丢弃快照（信令随后必到，下一快照恢复）。

**验收**：L1 **326 绿**（LiteFramework 203 / LiteSim 75 / LiteNet 48）；CI（windows + ubuntu 矩阵）在 `a7d9b12` 转绿。

### 对账与结论（M10 完成）

- §9 M10 验收行逐项：
  - **同机两客户端对跑 5 分钟**：`M10_LONGRUN=1` 门控用例交付（5 分钟全量对跑 + 每 10s 帧号推进断言）——**夜间歇性跑**（M10_LONGRUN 门控），非 L1 常规
  - **模拟 10% 丢包不卡死**：harness 支持 `RunFor(skipEvery)` 丢包模拟（发送侧跳步），专项丢包用例随批③输出口径覆盖
  - **和解触发率可观测**：`MismatchReport` 链路 + `ReconcileCount`/`OnReconcile` 全通
  - **人为注入不确定性 → 快照覆盖兜底不发散**：用例验证（A 静止 B 移动的远端误差注入 → 预测帧号有界）
  - **30Hz 带宽 ≤ 预算**：快照通道数据流在批③对跑可观测；**预算锚点校准留 M11 联调实测**（全量预测形态下的和解率与快照尺寸的真实分布需 M11 场景才完整）
  - **延迟补偿 150ms 用例**：`LagCompensationTests` 单元级覆盖；高 ping 端到端形态归 M11 联调
- **M10 剩余（移交 M11/M12）**：KcpNetworkService 桥接的 Unity 装配（M11 ProcedureMatch）、150ms 端到端、带宽实测校准、E2 优先级调度（后置）
- **遗留清理**：`Tests/LiteNet.Tests/TempProtoReflection.cs`（本会话临时诊断件，未跟踪，可删）

**M10 完成。M11（表现层 + 可玩 demo）就绪可开工。**

### 2026-09-19 — CI 红修复（RoomBroadcaster 拆分漏接线）

**现象**：`9f6ec1f`（Room 拆出 RoomBroadcaster）与 `cbe68f3`（ServerHost 补装配）两次推送 **CI failure**，`LiteNet.Tests.RoomBroadcastTests` **4 条失败**（掉线成员不广播 / 背压超限降档抽帧 / 快照按30Hz广播 / 首包全量后续增量）。

**根因**：拆分后快照改经 `RoomBroadcaster` 下发，生产侧（`ServerHost`）已补装配，但**测试的捕获口仍挂在 `room.SendTo`** → 快照类断言收不到包。

**修法**（`c3d66c1`）：测试装配 `room.Broadcaster.SendTo = room.SendTo`，与生产侧同一个捕获口。

**验证**：LiteNet.Tests 44+4失败 → **48 绿**；CI 双平台矩阵 **首次真验通过**（`87d9ab8`：ubuntu 43s / windows 1m36s）——即 **ubuntu 侧抓出这批失败、也在 ubuntu 侧验证修复**（matrix 的价值现场兑现）。

**同批**：玩法数值解耦落地（`87d9ab8`）——新增 `LiteSim/CombatConfig`（手感参数单源 + `LoadFrom` 的 `tb_combat_num` 装载接缝），`SimConfig` 只留确定性架构常量；`InputSystem/MovementSystem/ShootingSystem/DevHUD/测试` 消费点同步改名；伤害浮动表达式参数化（语义与 RNG 消耗等价，3000 帧基线未变可证）；BuildHash 重生成。

### 2026-09-19 — 客户端审查四条处置（RoomClient 输入冗余 / 生命周期 / 分配）

**审查发现四条，逐条核对后全部成立并已修**（`Assets/LiteNet/Client/RoomClient.cs`）：

| # | 问题 | 实况 | 处置 |
| --- | --- | --- | --- |
| 1 | **冗余窗口名不副实** | 实现把**同一份 latest 重复 4 次**（`for i < MaxRedundancy`），既没有历史帧，也**绕过了 `InputPacker.Pack`**（协议单源红线：手搓了一份打包逻辑） | 维护**最近帧环形窗口**并统一走 `InputPacker.Pack(frame, recent, …)`：包内 = 本帧 + 往回**连续**的帧（各自内容），服务器 `TryGetFrame` 逐帧取用 → 丢一包可补帧 ✓ |
| 2 | **开火位只在首个冗余帧** | `Buttons = i == 0 ? latest.Buttons : 0`（原注释把"开火不预测"的**预测语义**错用到**传输冗余**上） | 每帧携带**各自**的 `Buttons`（开火帧的开火位随该帧一起重传）✓ |
| 3 | **事件订阅未取消** | `OnData += OnWireData` 无 `-=`；`OnDisconnected` 用 lambda 订阅（无法退订）→ 传输活得更久时重复回调/滞留；且 `Dispose` **代管了注入传输的释放**（所有权错位） | 具名处理函数 + `Dispose` 退订；**不再 Dispose 传输**（注入依赖，所有权归创建方；M11 `KcpNetworkService` 持有）；新增 `IClientTransport` 窄端口（对齐 `IRoomTransport` 先例）→ 退订行为**可用假件验证** |
| 4 | **每包两次分配** | `PacketCodec.Encode` = protobuf `ToByteArray()` + 信封 `new byte[]`；60Hz 输入持续 GC | 新增 `PacketWriter`（复用缓冲 + 复用 `MemoryStream`/`CodedOutputStream`，扩容时重建）→ **每包零托管分配**；复用安全由 kcp2k 实测保证（`SendReliable`/`SendUnreliable` 均**同步 `Buffer.BlockCopy`** 到内部缓冲，返回后即可覆盖） |

**协议语义补充（写进代码注释）**：跳帧时冗余窗口**重置为 1**——绝不把上一帧的输入冒充成缺失帧（否则服务器会把陈旧移动当成该帧输入）。重发同帧只覆盖内容、不推进窗口。

**用例（12 条，`Tests/LiteNet.Tests/`）**：
- `RoomClientInputTests`（8）：窗口带真实历史且各帧内容独立 / 开火位随帧保留 / **丢包场景从后续包补帧** / 跳帧重置不用陈旧帧冒充 / 重发同帧窗口不推进 / 首帧窗口为 1 / **Dispose 退订且不释放注入传输**（假传输验，含 `Dispose` 后发送抛 `ObjectDisposedException`）
- `PacketWriterTests`（4）：与 `PacketCodec.Encode` **逐字节一致**（线格式单一来源）/ 复用不残留上次内容 / 超长消息按需扩容不截断 / Dispose 后写入抛

**验收**：L1 **344 绿**（LiteFramework 203 / LiteSim 75 / LiteNet **66**，0 警告）；L2 退出码 0（meta 10920 / 程序集新鲜 / 编译无失败 / EditMode 6/6）。
