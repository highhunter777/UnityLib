# 通用流程状态机施工图（A 路线：一次重构到位）

> 2026-09-17。**用户拍板走 A 路线**：现在就把 `Fsm<T>` 重构为通用 `StageMachine<TId, TReq>`，并把 Procedure 一起迁过来，**终态只有一个状态机机制**。
> 依据：设计讨论（"有没有重构一个通用的流程状态机"）＋《UI扩展能力设计》§1.5.6（转场为何拒用旧 FSM）。
> 与 B/C 路线的关系：B（两个机制并存）被否；C（先给转场用、M11 顺手迁流程）被放弃——用户选 A，**现在就统一**。

---

## 0. 影响面（实测，非估计）

| 项 | 现状 | 说明 |
|---|---|---|
| Core 内核 | `Core/Fsm/Fsm.cs` **120 行** + `Core/Fsm/Procedure.cs` **49 行** | 待重构/退场 |
| Core 测试 | `Tests/LiteFramework.Core.Tests/FsmTests.cs` **7 用例** | 语义安全网，逐条移植 |
| 真消费者 | `GameEntry.cs`（`s_fsm` + `CreateFsm` + `Start<ProcedureLaunch>`）、4 个 `Procedure*`、`ConfigPanel.cs`（状态展示） | 共 **6 个调用点** |
| 非消费者 | `Assets/GameMain/**`、`Assets/GameFramework/**` | **用的是 GameFramework 自己的 Fsm**（无 `using LiteFramework`），不受影响 |

## 1. 目标终态

**一个机制**：`StageMachine<TId, TReq>`（四个正交维度全部参数化）。

| 退场 | 接任 |
|---|---|
| `Fsm<TOwner>` | `StageMachine<TId, TReq>` |
| `FsmState<TOwner>` | `IStage<TId, TReq>` |
| `ChangeState<TState>()` | `Request(TId, in TReq)` + `Advance()`（两段式） |
| `CurrentState` / `CurrentStateTime` | `Current` / `StageTime` |
| `IProcedureOwner` + `ProcedureOwner`（owner 承载业务状态） | **`TReq` payload**（每次迁移随参数带，见 §5 决策记录） |
| `ProcedureBase<TOwner>` | `ProcedureStageBase<TId, TReq>` |

## 2. 核心 API（Core 新增两文件）

```csharp
// Core/Fsm/IStageHost.cs（2026-09-17 追加：钩子只认这个窄接口，见《层级状态机施工图》§实施记录"契约抽窄"）
public interface IStageHost<TId, TReq> where TId : struct, Enum
{
    bool Started { get; }  TId Current { get; }  float StageTime { get; }
    void Request(TId nextId, in TReq req);   void Request(TId nextId);
}

// Core/Fsm/StageMachine.cs
public interface IStage<TId, TReq> where TId : struct, Enum
{
    void OnInit(IStageHost<TId, TReq> m);
    void OnEnter(IStageHost<TId, TReq> m, in TReq req);     // ★ payload 走参数：不进 owner、不用字典
    void OnUpdate(IStageHost<TId, TReq> m, float elapseSeconds);
    void OnLeave(IStageHost<TId, TReq> m);
}

public sealed class StageMachine<TId, TReq> : ITickable, IModuleStats, IStageHost<TId, TReq>
    where TId : struct, Enum
{
    public StageMachine(string name, params (TId id, IStage<TId, TReq> stage)[] stages);

    public TId Current { get; }
    public bool HasPending { get; }
    public TId PendingId { get; }
    public float StageTime { get; }
    public long TransitionCount { get; }

    public void Start(TId initialId);                        // 一次；全部 stage 先 OnInit
    public void Request(TId nextId, in TReq req);            // 只入队（last-wins，payload 同时覆盖）
    public void Request(TId nextId);                         // = Request(nextId, default)
    public void Advance();                                   // 应用队首：OnLeave → OnEnter(in req)
    public void Tick(float realDelta);                       // OnUpdate + 有挂起 → Advance
}
```

```csharp
// Core/Fsm/ProcedureStage.cs（流程基类：保留"可取消 RunAsync"语义）
public abstract class ProcedureStageBase<TId, TReq> : IStage<TId, TReq> where TId : struct, Enum
{
    public virtual void OnInit(StageMachine<TId, TReq> m) { }
    public void OnEnter(StageMachine<TId, TReq> m, in TReq req)   // sealed：进流程即建 CTS + RunAsync
        => RunAsync(m, req, StartCts());
    public void OnLeave(StageMachine<TId, TReq> m)                // sealed：离场即 Cancel + Dispose
        => CancelCts();
    public virtual void OnUpdate(StageMachine<TId, TReq> m, float dt) { }
    protected abstract void RunAsync(IStageHost<TId, TReq> m, in TReq req, CancellationToken ct);
    protected void Fail(StageMachine<TId, TReq> m, Exception ex, string where);  // Log.Fatal + 由业务 Request(Error, args)
}
```

## 3. 语义契约（**7 条既有语义逐条保住** —— 迁移的红线）

| # | 既有语义（`FsmTests` 用例） | `StageMachine` 如何保 |
|---|---|---|
| 1 | `Start` 触发 OnEnter；**未 Start 时 Tick 静默** | 同：`_current == null` → Tick 直接 return |
| 2 | 迁移在 **OnUpdate 之后**、帧末应用；`StageTime` 归零 | 同：`Tick` = `OnUpdate` → 若有挂起则 `Advance`；`Advance` 末尾 `_stageTime = 0` |
| 3 | 同帧多次请求 **last-wins** | 同：`_pendingId`/`_pendingReq` 覆盖式写入 |
| 4 | `OnEnter` 内请求 → **挂到下次 Advance**（期间收到一次 OnUpdate） | 同：`Advance` 内 `OnEnter` 里再 `Request` 写 pending，不递归 |
| 5 | **重入禁止**（请求 = 当前）→ 抛 | 同 |
| 6 | **`OnLeave` 期间改道 → 抛** | 同：`_inLeave` 窗口内 `Request` 抛 |
| 7 | **未注册 id → 调用时当场抛**（不等帧末） | 同：`Request` 内查表 |
| +8 | （新增）**payload 随迁移传递**：`OnEnter` 收到的是 `Request` 时给的 `req` | `_pendingReq` 字段随 `_pendingId` 一起交接 |
| +9 | （新增）**两段式**：`Request` 后不迁移，`Advance()` 才迁移（SignalDriver 就是"调用方自己调 Advance"） | — |
| +10 | （新增）payload 也 **last-wins** | 同 #3 |

**异常策略内核不管**（保持旧 `Fsm` 的"回调不 try/catch"）：流程侧靠 `ProcedureStageBase` 的 `RunAsync` 内部 try/catch；转场侧将来由驱动层捕获 + 超时。这是设计里的"策略注入点"，本图不动。

## 4. 文件级改动清单

| 文件 | 动作 | 方式 |
|---|---|---|
| `Assets/LiteFramework/Scripts/Core/Fsm/StageMachine.cs` | **新增** | 直接写（新文件，Unity 导入时自建 meta） |
| `Assets/LiteFramework/Scripts/Core/Fsm/ProcedureStage.cs` | **新增** | 同上 |
| `Assets/LiteFramework/Scripts/Core/Fsm/Fsm.cs` | **删除** | **经 Unity `delete_asset`**（禁手删 meta） |
| `Assets/LiteFramework/Scripts/Core/Fsm/Procedure.cs` | **删除** | 同上（`IProcedureOwner`/`ProcedureBase` 退场） |
| `Assets/LiteGame/Scripts/Runtime/Procedure/ProcedureId.cs` | **新增** | `enum ProcedureId { Launch, Preload, Main, Error }` + 预留注释（Match/Battle/Result 见 §7） |
| `Assets/LiteGame/Scripts/Runtime/Procedure/ProcedureArgs.cs` | **新增** | `readonly struct`：`Error`（错误流程读）；预留 `roomId/frameNo/BattleContext` 字段位（M10 四批/M11 填） |
| `Assets/LiteGame/Scripts/Runtime/Procedure/Procedure{Launch,Preload,Main,Error}.cs` | **修改** | 基类 + 调用点（`ChangeState<>` → `Request(id)`；`fsm.Owner.LastError` → `req.Error`） |
| `Assets/LiteGame/Scripts/Runtime/Procedure/ProcedureOwner.cs` | **删除** | 经 Unity（owner 机制被 payload 取代，§5） |
| `Assets/LiteGame/Scripts/Runtime/GameEntry.cs` | **修改** | `s_fsm` → `s_machine`；`CreateFsm` → `CreateMachine`；`Start(ProcedureId.Launch)`；容器注册类型替换 |
| `Assets/LiteGame/Scripts/Editor/ConfigPanel.cs` | **修改** | `GetFsm()` → `GetMachine()`；`CurrentState?.GetType().Name` → `Current.ToString()` |
| `Tests/LiteFramework.Core.Tests/StageMachineTests.cs` | **新增** | 10 条语义（§3 表） |
| `Tests/LiteFramework.Core.Tests/FsmTests.cs` | **删除** | Tests 不在 Assets，可直接删 |

## 5. 决策记录（本轮定案）

1. **状态标识用枚举（`TId : struct, Enum`）而非类型**：类型作状态标识无法携带 payload（只能靠 owner 字段或字符串键字典，后者**你们已删**）。改枚举后 `ChangeState<T>()` → `Request(TId, in TReq)`。**状态逻辑仍写成类**（`IStage` 注册表，键从 `Type` 变 `TId`），现有 4 个流程类**保留类形态**，改动只是注册键 + 钩子签名。
2. **`IProcedureOwner` + `ProcedureOwner` 退休**：owner 的原始用途是"框架不承载业务状态、业务字段放业务 owner"（2026-09-10 泛型化决策）。**payload 参数更彻底地实现了同一意图**——每次迁移的数据随参数走，且是**编译期强类型**（不是 owner 上一堆可能为 null 的字段）。`LastError` 的职责由 `ProcedureArgs.Error` 承接。
3. **`Request`/`Advance` 两段式**：请求只入队、迁移点独立。这一条是让"队列推进"绕开 `OnLeave` 禁改道窗口的关键（旧 `Fsm` 的队列推进无处安放），也为将来的 `SignalDriver` 留好口——**它不需要新类，`Advance()` 已是公开入口**。
4. **异常策略留内核外**：内核不 try/catch（保旧语义），流程侧由 `ProcedureStageBase.RunAsync` 自己兜，转场侧将来由驱动层兜 + 超时。

## 6. 分批实施与验收

| 批 | 内容 | 验收 |
|---|---|---|
| **S1** | Core 内核（§2 两文件） | 编译 0 错 |
| **S2** | LiteGame 迁移（§4 全部 LiteGame 行） | 编译 0 错 + Play 冒烟：启动链 Launch→Preload→Main 走通 |
| **S3** | 测试迁移 + 删旧件（`Fsm.cs`/`Procedure.cs`/`FsmTests.cs`/`ProcedureOwner.cs`） | L1 全绿（10 条 StageMachine 语义 + 既有 220 基线） |
| **S4** | 验证与实施记录 | L1 绿 + Unity 编译 0 错 + Play 零 error + `ConfigPanel` 显示正确 |

## 7. 不在本轮范围（插槽已就位）

| 项 | 说明 |
|---|---|
| `RequestQueue` / `Completion` / `Timeout`（设计里的 Layer 2） | 属 **UI 转场编排**任务（《UI扩展能力设计》§1.5），无消费者前不建空件 |
| `SignalDriver` 类 | 不需要——`Advance()` 是公开入口，"信号驱动"就是调用方直接调它 |
| `ProcedureId.Match/Battle/Result` | **M10 四批/M11 前置**（《UI扩展能力设计》§9.1），本轮不动；`ProcedureId` 与 `ProcedureArgs` 留好扩展位 |
| UI 转场状态机本体 | 见《UI扩展能力设计》§1.5（本轮只把内核备好） |

## 8. 风险与回滚

| 风险 | 缓解 | 回滚 |
|---|---|---|
| `ProcedureStageBase` 的 CTS 语义搬丢（进流程 new CTS → RunAsync；离场 Cancel+Dispose） | §2 里 sealed OnEnter/OnLeave 承担，`RunAsync(m, in req, ct)` 签名不变 | — |
| Play 冒烟才发现流程链断 | S2 完成后**立即** Play 冒烟，不等 S3 | 单文件回退（4 个流程类 + GameEntry） |
| 删 `Fsm.cs` 引发域重载期间 pipeline 抖动 | 删除经 Unity `delete_asset`，删后立即 `recompile_status` 复核 | 从 git 恢复两个文件 |
| `ConfigPanel` 漏改（Editor asmdef 编译失败） | S2 一并改并 recompile 复核 | — |

---

## 附：实施记录

**批次实况（2026-09-17，一次做完 S1–S4，无偏差）**：

| 批 | 交付 | 备注 |
|---|---|---|
| S1 | `Core/Fsm/StageMachine.cs`（`IStage` + `StageMachine`，~180 行）+ `Core/Fsm/ProcedureStage.cs`（`ProcedureStageBase`，~60 行） | 新文件，Unity 导入自建 meta |
| S2 | `ProcedureId` / `ProcedureArgs`（新）；`Procedure{Launch,Preload,Main,Error}` 改基类与调用点；`GameEntry`（`s_fsm` → `StageMachine`、`CreateFsm` → `CreateMachine`、`Start(ProcedureId.Launch)`、容器注册类型）；`ConfigPanel`（`CurrentState?.GetType().Name` → `Started / Current.ToString()`）；`M0SelfTestRunner`（自测项 `Fsm<ProcedureOwner>` → `StageMachine<ProcedureId, ProcedureArgs>`） | 见下"漏改教训" |
| S3 | 新 `StageMachineTests.cs`（**11 用例** = 旧 7 条逐条移植 + 4 条新增）；删 `FsmTests.cs`；删 `Core/Fsm/Fsm.cs`、`Core/Fsm/Procedure.cs`、`ProcedureOwner.cs`（**三者经 Unity `delete_asset`，连同 meta**） | — |
| S4 | L1 + Unity 编译 + Play 冒烟 + 本文记录 | — |

**验收数据**：

- **L1 = 224 绿**：LiteFramework **145**（= 旧 141 − 7 `FsmTests` + 11 `StageMachineTests`）/ LiteSim 68 / LiteNet 11，0 失败。
- **Unity 编译 0 错误**（`recompile_status` → completed / failed=false / errors=[]）。
- **Play 冒烟（Test.unity）**：`current=Main`、`switches=2`（Launch→Preload→Main）、`assetInit=True`、`luaMain=True`、**启动后 error 数 0**。
- 残留检查：`Assets/LiteGame` + `Assets/LiteFramework` 内 `Fsm<` / `ProcedureOwner` **仅剩注释中的历史说明**（`ProcedureArgs` / `ProcedureStage` 的 doc-comment），无代码引用。
- `Assets/GameMain/**`、`Assets/GameFramework/**` 用的是 GameFramework 自己的 `Fsm`（无 `using LiteFramework`），**不受影响**——已复核。

**教训（两条，都是环境/流程类，与代码无关）**：

1. **全仓 grep 截图会漏消费者**：定位 `Fsm<` 消费者时我用了 `grep ... | head -8`，`M0SelfTestRunner.cs` 被截掉 → Unity 编译报 `CS0246: Fsm<> / ProcedureOwner 找不到`。**教训：迁移类改动要先全量列出消费者再动手，别截断输出**。（L1 侧没报是因为 `M0SelfTestRunner` 在 LiteGame.Runtime，双轨 csproj 不含它 —— 又一次印证"L1 绿 ≠ Unity 可编译"。）
2. **Play 冒烟前必须让编辑器前台/帧泵转起来**：本次 Play 后 `stageTime` 冻在 1.0、`AssetService` 卡在 init 不动（症状像"重构把启动链改坏了"），实为**编辑器失焦时帧泵不转**（已知坑）。用 Pipeline 的 **`editor_focus`** 唤到前台后立刻跑完（`current=Main`）。**以后 Play 冒烟固定加一步 `editor_focus`**，否则会把环境问题误判成代码问题。

**决策登记**：见 §5（状态标识改枚举 / `IProcedureOwner`+`ProcedureOwner` 由 payload 取代 / 两段式 / 异常策略留内核外）。旧 `Procedure.cs` 里"形态：Core 只留 `IProcedureOwner` + `ProcedureBase<TOwner>`"的 2026-09-10 决策**已被本轮取代**——其"框架不承载业务状态"的意图由 `TReq` payload 更彻底地实现（编译期强类型，且无"owner 上可能为 null 的字段"）。

**未做（插槽已就位，见 §7）**：Layer 2 组件（`RequestQueue` / `Completion` / `Timeout`）、`ProcedureId.Match/Battle/Result` 三阶段、UI 转场状态机本体（《UI扩展能力设计》§1.5）。
