# 客户端 C1 施工进度：统一生命周期、所有权与可测试运行时

> 依据：《商业级通用客户端框架总设计》§6.1 ClientHost / §6.2 作用域 / §7 流程纪律 / §8 内容租约 / §9 ConfigSnapshot / §16 C1 + §5.1 迁移策略（先逻辑边界后拆包）；《框架先行建设与业务接入专项设计》§11 施工顺序；《待办总览》§5 G1 客户端交付行。
> 本文件只记录施工状态与证据；目标与验收以设计为准，不在此重复定义。

## C1 收口结论（2026-09-24）

**C1 主体交付**：C1-①～⑩ 十批全部落地并推送（Host/AppLifetime/ClientScope/统一取消链/租约与共享加载/代次/ConfigSnapshot 快照化/Lua 候选通道/激活事务与 Patch 流程）。收口核证：**全量 L1 505/505**（LiteFramework 272 + LiteNet 143 + LiteSim 83 + LiteTesting 7）、**Unity 编译 0 错误**、**EditMode 51/51**、**Player 构建 + 冒烟 PASSED**（[Asset] ready 真标记——字面量判据修复后）。
**余项（后续线承接，不阻塞 G1 其余线）**：U1 租约消费者迁移（UI-03/05/06/09）；C2 Account/Match 作用域与 ErrorRecovery 重试执行器（有消费者再建）；热更批 Host 模式下载/验签/远程候选健康检查/原子写切换/深度 Lua 候选验证/安全窗口重发布。

## 批次规划

| 批 | 范围 | 状态 |
|---|---|---|
| C1-① ClientHost 核心与统一取消 | IClientModule/ClientContext/ClientHost（Core 双轨接 UniTask）；平台事件订阅口 | 已完成（见下） |
| C1-② AppLifetime 与 GameEntry 收敛 | AppLifetime 平台桥（LiteFramework.Unity）；GameEntry 收敛为引导适配器（装配步骤→IClientModule 序列）；失败回滚→确定错误态 | 已完成（见下） |
| C1-③ ClientScope 原语 | 链接父级 CTS（父子取消级联）；IDisposable LIFO 登记（逆序释放、异常隔离、幂等）；死作用域登记显性失败 | 已完成（见下） |
| C1-④ Scope 树与内容契约 | ClientScope.CreateChild；五层树生命周期用例；IContentService/AssetLease 契约 + FakeContentService（失败注入） | 已完成（见下） |
| C1-⑤ ConfigSnapshot 与 ErrorRecovery 骨架 | ConfigSnapshotService（候选校验→原子发布→版本单调）；ClientErrorRecovery（§7.2 分类→动作映射） | 已完成（见下） |
| C1-⑥ 全链验证与冒烟终局 | L1 445/L2 39/L3 8/Unity 0 错误/Player 冒烟 PASSED（健康路径 [Asset] ready）| 已完成（见下） |
| C1-⑦ 统一取消链与 Scope 服务归属 | 流程阶段 CTS 链接宿主根令牌；宿主关闭先行取消+竞态守卫；SceneService 释放面；GameEntry 静态重置缝 | 已完成（A1 `1672ce8` + A2 `e9ce56e`，见下） |
| C1-⑧ AssetLease/共享加载与取消 | SharedLoadCoordinator（合并/取消/失败/引用/迟到）；ContentGeneration 键维度；YooAssetContentService 真适配 + 装配接线；Preload 改经 IContentService | 已完成（`445be7f`，见下） |
| C1-⑨ ConfigSnapshot/Lua 候选与版本 | ConfigService 快照化（候选→校验→原子发布，半发布缺陷修复）；配置/Lua 字节通道经内容租约（代次统一）；Lua 候选完整性检查 | 已完成（`ae8e6bb`，见下） |
| C1-⑩ 启动更新和恢复 | ActivationTransactionStore（三态事务+恢复决策）；FileActivationRecordIO（不可信即弃）；Patch 流程（恢复→资源包初始化）；冒烟标记字面量修复 | 已完成（`4700fc1` + `c771a12`，见下） |
| C1-后续（未开始） | Root/Account/Match/Scene/UI 完整 Scope 树实装（依赖 C2 Meta/对局）、ContentService YooAsset 适配与 Patch 事务、ConfigSnapshot 与 ConfigService 接线、ErrorRecovery 执行器（重试调度/回滚/重新登录）| 未开始 |

## 施工记录

### 2026-09-23 · C1-①② ClientHost/AppLifetime 交付

**代码位置**：

- `Assets/LiteFramework/Scripts/Core/Hosting/ClientScope.cs`——作用域原语：命名 + 链接父级 CTS（父子取消级联；父/外部令牌互斥）+ IDisposable LIFO 登记（逆序释放、单项异常隔离聚合、幂等）+ 登记进已死作用域就地释放并显性失败。`Core/Hosting/ClientModule.cs`——`IClientModule`（InitializeAsync/ShutdownAsync 契约：ct 穿透不吞、关闭可重入不抛）与 `ClientContext`（根 Scope + 按类型产物袋：同型重复登记即装配错误；Require fail-fast）。
- `Core/Hosting/ClientHost.cs`——注册顺序 = 初始化顺序 = 关闭逆序（依赖图由调用序表达，Host 不推断）；初始化失败**只逆序关闭已成功模块**后原异常上抛（确定错误态）；ShutdownAsync = 刷新钩子（登记序，失败聚合）→ 逆序模块关闭（单模块失败不阻断，OperationCanceledException 区分主动放弃）→ 根 Scope 释放；幂等；平台事件订阅口（Pause/Focus/LowMemory/QuitIntent，订阅者异常吞并聚合）；`Product<T>()` 只读产物透传（引导适配器驱动容器/流程机用）；**无静态可变状态**，`ResetForEditorReload()` 为未来静态门面唯一清理入口。
- `Assets/LiteFramework/Scripts/Unity/App/AppLifetime.cs`——Unity 平台桥：OnApplicationPause/Focus/OnLowMemory/OnApplicationQuit → Host 转发；退出序列 = QuitIntent 收集 → 刷新钩子 → ShutdownAsync（有界等待 `GracefulShutdownMaxMs=3000`，挂死模块不放行成僵尸）；`RuntimeInitializeOnLoadMethod(SubsystemRegistration)` 转发静态清理（Domain Reload 关闭场景）；OnDestroy 走同一完整关闭（非 Quit 销毁路径不泄漏）。生命周期消息为 public——编辑态 SendMessage 不派发引擎消息（ShouldRunBehaviour 断言，L2 实测），直调 public 是 EditMode 可测的唯一形态。
- `Assets/LiteGame/Runtime/Main/Hosting/GameModules.cs`——GameEntry 装配步骤全量模块化（9 模块）：PlatformInfrastructure（FileSys/Log）→ Settings（Load；退出 Save）→ Clocks（双轨时钟/事件/墙钟）→ Schedulers（逻辑/UI/时间轴）→ LuaHost（组件；退出 Shutdown）→ Config → UiShell（注册表+红点+UIService；退出红点清空）→ Presentation（Entity/Audio/VFX）→ Container（容器注册 + CreateMachine 流程机平移）。产物经 ClientContext 传递（同型唯一、Require fail-fast）；每模块 Shutdown 只调已存在能力，缺口（UIService Shutdown/租约/VFX 释放）登记归 U1/M11。
- `Assets/LiteGame/Runtime/Main/GameEntry.cs`——收敛为 Unity 引导适配器（§6.1 目标形态）：Awake = 重复引导守卫 → ClientHost + AppLifetime 绑定 → 模块注册 → InitializeAsync（失败 `ProcedureError.ShowBootstrapError` 进入确定错误态——公开引导错误入口新增于 ProcedureError）；Start = 流程机启动；Update = 驱动 Tickables；OnDestroy = 取消引导期残留异步（完整关闭由 AppLifetime 负责）。静态收敛：仅剩 `s_booted` 幂等标记（容器/流程机收为实例字段——§4 原则 8）。
- `Assets/LiteFramework/Scripts/Core/LiteFramework.Core.asmdef` + `LiteFramework.Core.csproj`——双轨接 UniTask：Unity 引内嵌 `com.cysharp.unitask 2.5.11`（asmdef references += "UniTask"，noEngineReferences 保留）；dotnet 引 NuGet `UniTask 2.5.11`（注意：nuget.org 包 ID 为 **UniTask** 而非 Cysharp.Threading.Tasks——实测 NU1101 后修正）。
- `Assets/LiteGame/Editor/Resource/BuiltinBundleBuildInvoker.cs`——内置 Bundle 包构建入口（ScriptableBuildPipeline + ClearAndCopyAll 到 StreamingAssets/yoo；RunAsync + Status 轮询——SBP 真构建超 Pipeline eval 5s 主线程窗口，fire-and-forget + 轮询为唯一可靠形态）；补齐 C0-③"健康路径（AssetService 就绪）"的包来源。
- `Assets/Tests/EditMode/LiteGame.EditModeTests.asmdef`——补 `LiteFramework.Unity` 引用（AppLifetime 可见性）。

**实施口径偏离与登记**：

1. **IClientModule 契约为 UniTask**（§6.1 设计原文）；为此 LiteFramework.Core 双轨引入 UniTask（Unity 内嵌源码 + dotnet NuGet 同版本 2.5.11）。Core 的"零依赖"属性从"零第三方"放宽为"零引擎 + UniTask 客户端异步单源"——LiteSim/LiteNet 纯度红线不受影响。
2. **AppLifetime 生命周期消息改 public**：编辑态 SendMessage 不派发引擎消息（ShouldRunBehaviour 断言，L2 实测）；直调 public 方法绕开编辑态限制，引擎运行时行为不变。
3. **模块 Shutdown 只调已存在能力**：Settings 退出 Save、Lua Host Shutdown、红点 Clear；VFX/Entity/Audio/FSM 无关闭面 = no-op 并登记（实例/租约释放归 U1/M11）——本批交付 Host 编排与逆序保证，不虚构尚未存在的释放语义。
4. **GameEntry 静态收敛幅度**：仅删 RegisterInstance 死入口与 M0SelfTestRunner 注入（吸收客户端线清理）；`s_booted` 幂等标记保留静态（跨场景重载防重复引导，不持真实生命周期）。
5. **BuiltinBundleBuildInvoker 为本批新增**：C0-③ 的 Offline 冒烟走的是双标记中"错误 UI"分支（内置包从未真实构建——`Bundles/` 只有 Simulate 产物）；本批补齐"AssetService 就绪"健康分支的可重复生产方式，并纳入冒烟必验。

### 2026-09-23 · C1-④⑤ Scope 树 / IContentService 契约 / ConfigSnapshot / ErrorRecovery 交付

**代码位置**：

- `Core/Hosting/ClientScope.cs`——补 `CreateChild(name)`（§6.2 五层树建树语法糖；父子取消级联已有）。
- `Core/Hosting/ContentContracts.cs`——`AssetLease<T>`（构造器 = 实现方工厂缝 public；Dispose 幂等；Core 泛型不约束引擎类型）+ `IContentService`（InitializeAsync 幂等 / AcquireAsync 失败抛含 location / ShutdownAsync 释放全部剩余；§8.1/§8.2 契约冻结）。
- `Core/Config/ConfigSnapshotService.cs`——不可变快照 + 原子发布（候选校验委托失败 → 保留旧版并抛拒绝原因）+ 版本单调（ulong）+ 异步发布扩展（构造/校验/发布三段）。
- `Core/Hosting/ClientErrorRecovery.cs`——`ClientErrorKind` 五分类（§7.2 表逐行）→ `RecoveryAction` 映射；`ClientRecoveryException` 标记异常（分类在抛出点声明）；未知异常 Fatal 兜底；OCE 按 Transient（取消 ≠ 错误）。

**L1 测试（新增 24 例）**：

- `ClientScopeTreeTests.cs`（5 例）——五层树父取消级联/逐层 Dispose 兄弟隔离/LIFO+异常隔离/子先释放父后取消不抛/OwnedCount 泄漏断言（释放后归零）。
- `Hosting/ContentContractTests.cs`（6 例）——对称释放+幂等/共享引用计数/失败注入/未初始化与未登记显性失败/Shutdown 全量释放+幂等/OCE 穿透。
- `ConfigSnapshotTests.cs`（5 例）——原子发布/校验失败保留旧版/null 拒绝/异步取消 OCE 穿透/异步原子发布。
- `ClientErrorRecoveryTests.cs`（8 例）——分类→动作逐行映射/未知 Fatal 兜底/取消按 Transient/标记异常透传。

**实施口径偏离与登记**：

1. **ClientScope.OwnedCount 语义**：Dispose 后登记数清零（实现=释放即清登记表）——"泄漏断言"口径=释放后归零。
2. **ConfigSnapshotService 放 LiteFramework.Core**：§9 的 ConfigSnapshot 概念先行冻结契约（快照+版本+原子发布）；与 ConfigService（Luban 装载）的接线归 C1 后续/热更批。
3. **ErrorRecovery 执行器不在本批**：§7.2 的重试调度/回滚流程/重新登录需要主流程（C2 Login/Match）配合——本批只冻结分类→动作映射。

### 2026-09-23 · C1-⑥ 全链验证（最终态）

- `scripts/test.ps1 -Lane L1`：**PASS 445 项**（=G0 关闭态 407 + U0 批 6 + R0 批后 ClientHost 13 + 本批 Scope 树 5 + 内容契约 6 + ConfigSnapshot 5 + ErrorRecovery 8 + SafeCall 2 ≈ 445，以本轮实测为准）。
- `scripts/l2-unity-gate.ps1`：**L2 通过**——EditMode 39/39。
- **Player 构建 + 启动冒烟**：**PASSED**（`[Asset] ready` 健康标记，process alive）。
- 纪律扫描：随 L1 全绿。

**已知边界（后续批次）**：

- Root/Account/Match/Scene/UI 完整 Scope 树装配、ContentService YooAsset 适配与 Patch 事务、ConfigSnapshot 与 ConfigService 接线、ErrorRecovery 执行器（重试调度/回滚流程/重新登录）——C1 后续批（依赖本批原语）。
- AppLifetime 退出等待为同步有界轮询（≤3s）——挂死善后的进程级兜底由 OS/引擎回收；异步化改造无收益（Unity 退出窗口本身是主线程同步的）。
- AppLifetime.OnDestroy 触发完整关闭依赖组件销毁顺序（GameEntry/AppLifetime 同 GO）；严谨的退出顺序契约随 C1 热更事务批固化。

### C1-③ 冒烟排障记录（决定性验证的过程与根因，2026-09-23）

初次冒烟 FAILED 排障发现四处工具/链路缺口：

- ①`build-player.ps1` 增量构建**不感知 Packages/ 源码变更**（Library/Bee 缓存未失效——YooAsset 源码加面包屑后 IL2CPP 元数据无该字符串，删 `Library/Bee` 全量重建后才生效）。
- ②StreamingAssets/yoo **从未被真实构建过**（`Bundles/` 只有 Simulate 产物）——C0-③ 冒烟"通过"实为双标记的错误 UI 分支。补 `BuiltinBundleBuildInvoker` 补齐包来源。
- ③构建失败被误判 PASSED：一个残留 Player 进程锁住输出文件 → 构建复制失败（9s 假构建、`result:Failed` + 5 errors）→ 脚本失败检测正则不匹配 `result:Failed` 形态 → 误判 PASSED；冒烟跑了陈旧 exe。杀进程后重建（180s Succeeded、0 errors）即恢复。
- ④冒烟标记口径：release Player 下 LiteFramework `Log.Info`（UnityLogHelper）**不落 Player.log**——健康分支标记改用 `Debug.Log("[Asset] ready ...")` 直打，player-smoke.ps1 标记集补 `[Asset] ready`。

**复跑终局**：Windows 目标包（BuiltinCatalog.bytes + 14 bundle 随包分发）+ 冒烟窗口 120s → **`[Asset] ready` 健康标记命中（process alive: True），C0-3 player smoke PASSED**。

### 2026-09-24 · C1-④⑤⑥ 接手复核与本批入库（新会话）

交付会话 2026-09-23 23:48 收尾后未提交；按用户指令接手复核、补缺、分批提交。

**复核验证（本轮实测）**：

- `dotnet test Tests/LiteFramework.Core.Tests/LiteFramework.Core.Tests.csproj -c Release`：**243/243 全绿**（含本批 Scope 树 5 + 内容契约 6 + ConfigSnapshot 5 + ErrorRecovery 8；C1 交付会话实测全量 L1 445 见上节）。
- Unity（Pipeline `recompile`/`console_status`）：**compilationFailed=false，Console 0 错误**；补导入 ClientErrorRecovery.cs 生成缺失 `.meta`。
- Pipeline `run_tests --mode editor`：**EditMode 39/39**（AppLifetime 3 + U0 真 Lua 6 + UI 模板/转场/VFX 等，4.3s）。
- **全量 L1 暂缓**：R1 服务端线并行会话在途重构 RoomServer（Runtime/ 拆分进行中，Tests.slnx 构建含 RoomServer.csproj 暂不可绿）——非本批缺陷；R1 落地后随下一批跑全量回归。

**接手处置与偏离登记**：

1. **7a7b79a 漏提 `.meta` 本批补齐**：上一提交包含 ClientHost/ClientModule/ClientScope/AppLifetime/GameModules/BuiltinBundleBuildInvoker 的 `.cs` 与部分 `.cs.meta`，但漏 `Hosting.meta`/`App.meta`/`Resource.meta`/`Main/Hosting.meta`/`Config.meta` 五个目录 meta 与 ClientHost/ClientModule/ClientScope 三个文件 meta——新机器克隆会重新生成 GUID。本批全部入库（ClientErrorRecovery.cs.meta 由本轮 Unity 导入生成）。
2. **Test.unity 随批收编**：改动为移除已删 M0SelfTestRunner 组件的挂载引用（guid 80f9d5ad…，-14 行）——修复 7a7b79a 遗留的场景悬空引用，非手工编辑（编辑器会话产物）。
3. **AgentScripts 6 个一次性脚本删除收编**（独立 chore 提交）；目录留空。
4. **Assets/StreamingAssets/ 加入 .gitignore**（BuiltinBundleBuildInvoker 可再生构建产物；`StreamingAssets.meta` 原已忽略；CI 冒烟按 C0 既有口径走错误分支标记）。
5. **临时诊断行原样入库**：AssetService `[Asset] init begin`/`[Asset] ready` 双打与 YooAsset `[YooBreadcrumb]` 面包屑为冒烟标记/排障产物（记录于上节），下批清理需连带复跑 Player 冒烟。
6. **文档重组收口**：环境恢复指南取代克隆后自备清单/CI门禁/L2本地执行手册三文档的删除与引用改写（ci.yml 注释、客户端/服务端总设计关联文档节）；`RoomServer/CombatNumbers.cs` 注释仍引《克隆后自备清单》——R1 在途域不碰，留 R1 线处置。

### 2026-09-24 · C1-⑦ 批次A：统一取消链与 Scope 服务归属（完整 Scope 树/Procedure 取消段）

> 依据：《框架先行建设与业务接入专项设计》§11 第一段"完整 Scope 树/Procedure 取消"；客户端总设计 §4 原则 4、§6.1 根取消源、§6.2 作用域表、§7.1 流程纪律；G1 退出"客户端启动中断可进入确定错误态"。

**代码位置**：

- **A1 Core（提交 `1672ce8`）**：`Core/Fsm/ProcedureStage.cs`——阶段基类构造可传入宿主根令牌，OnEnter 建链接 CTS（宿主关闭级联在途流程异步；离场取消语义不变；根已取消时新进阶段立即观察到取消）。`Core/Hosting/ClientHost.cs`——ShutdownAsync 固化为"先行取消根令牌 → 刷新钩子 → 逆序模块关闭 → 根 Scope 释放"；InitializeAsync 增关闭竞态守卫（ShutdownAsync 并发打断引导时以 OCE 中止装配、回滚已成功模块、未初始化模块不再触碰）。
- **A2 LiteGame（提交 `e9ce56e`）**：`Main/Hosting/GameModules.cs`（Container）——流程机四阶段构造传入 `ClientHost.RootScope.Token`；SceneService 移交 Container 模块所有并在其 ShutdownAsync 释放。`Shell/SceneService.cs`——补 `ReleaseAll()`（退出路径只归 YooAsset 引用不卸场景）。`Main/GameEntry.cs`——`[RuntimeInitializeOnLoadMethod(SubsystemRegistration)]` 静态重置缝（s_booted 跨 Play 残留在 Domain-Reload-Off 下误杀二次引导的缺陷修复）。`Procedure*` 四文件构造签名加 rootToken。

**测试（L1 +6 / L2E +2）**：

- `Hosting/ProcedureCancelTests.cs`（6 例）——离场取消语义不变（无根退化）/根取消级联在途阶段/根预取消时新进阶段立即观察/阶段释放不影响根源/宿主关闭根令牌先于模块关闭（探针模块回读）/关闭竞态中止回滚（UniTaskCompletionSource 定向续延——不依赖真实时钟）。
- `Assets/Tests/EditMode/GameEntryEditModeTests.cs`（2 例）——s_booted 跨 Play 残留复位/幂等清理。

**验证**：L1 `LiteFramework.Core.Tests` **251/251**（243 接手基线 + 本批 6 + R1 线 DisciplineScanner 在途 2）；Unity 编译 **0 错误**；EditMode **41/41**（39 存量 + 2 新增）。

**实施口径偏离与登记**：

1. **Scene 未做每场景 ClientScope 子作用域**：SceneService 本身即 §6.2 Scene 域的句柄持有者（显式租约、不外泄），叠加子作用域 = 第二套记账；本批只补宿主关闭释放面。UI 实例/展示双作用域（U1）、Account/Match 作用域（C2）由各自消费者接线——无消费者不建空层（总设计 §22）。
2. **GameEntry C1-③ 临时诊断行未清理**：`[GE]`/`[Preload]`/`[Asset]` 系列与 YooAsset 面包屑的清理需连带复跑 Player 冒烟，归后续批（批次D 启动更新验证时一并处理）。
3. **R1 并行线交互**：R1 在途修改曾阻塞 Unity 编译（RoomClient 用 .NET 6+ 的 `Environment.TickCount64`——Unity netstandard2.1 编译不过），由 R1 线自行修复（注入虚拟时钟）后本批完成 EditMode 复跑；两会话共享工作区与 git 索引，本会话全部提交走 pathspec 精确路径，未收编 R1 任何暂存/在途文件。

### 2026-09-24 · C1-⑧ 批次B：AssetLease/共享加载与取消（YooAsset 适配）

> 依据：《框架先行建设与业务接入专项设计》§11 第二段"AssetLease/共享加载与取消"；客户端总设计 §8.1/§8.2；热更专项 §7（取消语义）/§9（内容代次、租约）/§14 G1"AssetService/异步适配"。

**代码位置**：

- **Core**：`Hosting/ContentContracts.cs`——新增 `ContentGeneration`（ReleaseId + 单调 Value 的代次键身份；Default = builtin#0）；`IContentService.AcquireAsync` 增加代次参数（default = 当前代——旧 Scope 显式携带固定代，不从 Current 混取，§8.2）。`Hosting/SharedLoadCoordinator.cs`（新）——共享加载协调器：同键并发合并（loader 一次）、失败同异常穿透且键移除可重试、**单人取消只退本人**（其余等待者共享任务不受牵连）、全员退出弃置 + 尽力取消 + **迟到结果就地卸载不复活**（YooAsset 操作不可中途取消的兜底）、引用归零末位卸载恰好一次、Dispose 释放面（在途尽力取消/迟到丢弃）；**无保留缓存**（预算/保留策略归 U1/热更批）。
- **LiteGame**：`Shell/Resource/YooAssetContentService.cs`（新）——IContentService 的 YooAsset 适配：句柄不再"加载完立即 Release"（旧反模式），由协调器持有到引用归零，租约 Dispose → `handle.Release()`；键 = (代次, location, 类型)；失败含 location；Shutdown = 协调器释放面。`Main/Hosting/GameModules.cs`——新增 Content 模块（⑨，`IContentService` 注入装配；Container 顺延 ⑩）；`Main/Procedure/ProcedurePreload.cs`——资源初始化改经 `IContentService.InitializeAsync`（旧行为同时序，幂等共用 AssetService 主链）。
- **测试**：L1 新增 `SharedLoadCoordinatorTests` 10 例 + `ContentContractTests` 代次 3 例（Core 实测 264/264）；EditMode 新增 `YooAssetContentServiceEditModeTests` 3 例（未初始化失败含 location/空 location 拒绝/关闭幂等——EditMode 44/44）。

**验证（本批实测）**：L1 `LiteFramework.Core.Tests` **264/264**；Unity 编译 **0 错误**；EditMode **44/44**；**Player 构建通过 + 启动冒烟 PASSED**（`[Asset] ready` 健康标记——Preload 经 IContentService 的新链路端到端）。

**实施口径偏离与登记**：

1. **EditMode 无法验证真实 YooAsset 加载**（实测：`YooAssets.Initialize` 内部 `DontDestroyOnLoad` 仅 Play mode 合法，编辑器脚本直接抛 InvalidOperationException——run_script 探针实证）：真资源租约/共享/引用归零的运行时验证走 **Player 冒烟**（已过）与后续 L2P lane（接入后补真资源用例）；适配层合并/取消语义由 L1 协调器用例全量覆盖。
2. **契约演进登记**：`IContentService.AcquireAsync` 增加代次参数（C1-⑤ 冻结后的首次受控演进——消费者为零时迁移，FakeContentService/契约用例同步更新）。
3. **Host 模式下载/校验/激活/回滚归批次D**（PatchCoordinator）；UI/VFX/场景消费者迁移到租约归 U1（本批只交付服务与装配，不改消费者）。

### 2026-09-24 · C1-⑨ 批次C：ConfigSnapshot/Lua 候选与版本

> 依据：《框架先行建设与业务接入专项设计》§11 第三段"ConfigSnapshot/Lua 候选与版本"；客户端总设计 §9；热更专项 §10（Lua 候选阶段）/§11（ConfigSnapshot 与双端发布）。

**代码位置**：

- `Shell/Resource/ConfigService.cs`——三段式重构：①候选字节预取（缺表 fail-fast 含 location）→ ②候选建表（Luban 同步解析，失败不触碰已发布状态/全局数值）→ ③校验（tbcombatnum 单行表等）+ 数值回填 + `ConfigSnapshotService<Tables>` 原子发布（版本单调）；新增 `Version`/`CurrentSnapshot`。**修复半发布缺陷**：旧实现先设 `_tables` 再 ApplyCombatNumbers，校验炸了 Loaded 已 true（热更 §2 Current 登记）。
- `Shell/Lua/LuaPreloader.cs`——字节通道可注入（主链经内容租约；null = AssetService 直读，DevReload 兼容）；重复 key 显性拒绝（候选集合完整性）。
- `Main/Hosting/GameModules.cs`——模块重排：Content 升 ②（Config ⑦ 依赖它——"注册序即依赖序"）；Config 字节通道经 `IContentService.AcquireAsync<TextAsset>` 租约。
- `Main/Procedure/ProcedurePreload.cs`——Lua 预载经 `LoadLuaBytesViaContent`（提取 bytes 即释放源租约）。
- **测试**：EditMode 新增 `ConfigSnapshotEditModeTests` 3 例（正常发布 version=1 / **坏表保留空态无半发布**（回归卡）/ 缺表 fail-fast 含 location）。

**验证（本批实测）**：Unity 编译 **0 错误**；EditMode **47/47**（44 + 3 新增）；**Player 构建 + 冒烟 PASSED**（`[Asset] ready`——配置/Lua 全走租约通道的启动链路端到端）；L1 无 Core 变更（264 维持）。

**实施口径偏离与登记**：

1. **发布顺序**：校验（Publish 内）→ ApplyCombatNumbers → `_tables` 赋值——发布成功后回填；Apply 的行取值失败模式已被校验覆盖（不可达路径，注释钉死契约）。
2. **深度 Lua 候选验证（语法/导出/Bridge 能力/受控验证 env）与运行态重发布（安全窗口）随热更批**——本批交付候选通道（租约/完整性）与快照原子发布底座；对局固定玩法快照与 CombatConfig 静态迁移归 G2（热更 §14）。
3. **GameplayDigest/Release 完整版本身份**（热更 §5/§11）随热更批的版本握手——ConfigSnapshot 当前为代次起点（builtin#0）。

### 2026-09-24 · C1-⑩ 批次D：启动更新和恢复（激活事务 + Patch 流程）

> 依据：《框架先行建设与业务接入专项设计》§11 第四段"启动更新和恢复"；客户端总设计 §7.1（Patch 阶段）；热更专项 §4（启动恢复）/§8（激活、健康确认与中断恢复）。

**代码位置**：

- **Core**：`Hosting/ActivationTransactionStore.cs`（新）——三态事务记录（Confirmed/Candidate/PendingActivation）+ 恢复决策：启动时在途回退 Confirmed（持久化、`RecoveryAttempts` 计数上限 3——超限仍以 Confirmed 继续，禁止无限重启）；`Confirm` 单调推进；`ActiveGeneration` 反映已确认代次；`IActivationRecordIO` 持久化端口。
- **LiteGame**：`Shell/Resource/FileActivationRecordIO.cs`（新）——persistentDataPath JSON；**不可信即弃**（缺文件/解析失败/未知 SchemaVersion → null → 全新安装回 builtin，fail-safe）；单文件覆盖为最小写实现（非原子——最坏后果 = 记录损坏回 builtin；临时文件+Replace 原子切换与中断测试矩阵随热更批）。`Main/Procedure/ProcedurePatch.cs`（新）+ `ProcedureId.Patch`——Launch→**Patch**→Preload：启动恢复 → 资源包初始化（原 Preload 内初始化移入）；失败 RecordFailure + Error 确定错误态。`GameModules.Content` 登记事务存储产物。
- **测试**：L1 新增 `ActivationTransactionStoreTests` 8 例（全新安装/在途回退/待激活回退/超限/确认单调/无候选拒绝/空 id 拒绝/失败留档）；EditMode 新增 `ActivationRecordEditModeTests` 4 例（文件往返/缺文件/损坏/未知版本）。
- **修复**：`scripts/player-smoke.ps1` 标记改 `[regex]::Escape` 字面量匹配（`c771a12`——排障记录见下）。

**验证（本批实测）**：L1 `LiteFramework.Core.Tests` **272/272**；Unity 编译 **0 错误**；EditMode **51/51**；**Player 构建 + 冒烟 PASSED**（`[Asset] ready` 真标记命中——Patch 链路端到端）。

**实施口径偏离与登记**：

1. **Host 模式下载/验签/远程候选健康检查归热更批**——无真实 CDN/签名环境不虚构（热更 §6"首次远程激活前完成"）；C1-⑩ 交付事务状态机、恢复决策与 builtin 健康路径（初始化成功 = [Asset] ready）。
2. **ErrorRecovery 重试执行器未建**——启动链按设计 fail-fast 进确定错误态（不自动重试）；重试调度/重新登录有真实消费者时随 C2/C3 落地（禁止空层）。
3. **受控恢复 = 回退 Confirmed**（无远程重取通道）；远程重取候选随热更批。

### 2026-09-24 · C1-⑩ 冒烟排障记录（player-smoke 标记正则缺陷，决定性验证过程）

批次D 冒烟两连 FAILED（60s/120s 窗口均未命中标记），日志转储却含 `[Asset] ready`——根因不在启动链而在**冒烟脚本**：

- `markers` 按裸正则使用：`'[Asset] ready'` 中 `[Asset]` 是**字符类**（匹配 A/s/e/t 单字符），对目标行 `[Asset] ready package=...`（"]" 不在类内）**永不匹配**；
- C1-⑥ 起的"通过"实为误命中旧行 `[Preload] asset ready` 中的 **"t ready"**（'t' ∈ 字符类）——批次D 把资源初始化移入 Patch 流程后该行消失，潜伏缺陷暴露；
- 修复：`[regex]::Escape($m)` 字面量匹配（`c771a12`）；复跑 **PASSED**（真标记 `[Asset] ready` 命中，process alive）。
- **证据口径修正**：C1-⑥ 记录的"[Asset] ready 健康标记命中"实为 preload 行误命中——当时 Player 确实达到健康态（[Asset] ready 行在日志中），但脚本判据错位；本批起判据为真字面量。
