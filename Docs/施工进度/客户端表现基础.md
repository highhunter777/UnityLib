# 客户端表现基础施工进度：表现壳生命周期收敛与动画基础契约

> 依据：《框架先行建设与业务接入专项设计》§7 队列第 4 项"U1 与客户端通用表现"的余部（**Scene/Entity/Audio/VFX 生命周期**）与 §11"随后接通通用 UI/Scene/Entity/Audio/VFX、首版动画"；§5 接缝 1（谁创建、谁取消、谁等待清理、谁释放资源）、§6 样例三失败清单；《动画模块专项设计》§3–§9、§13 L1 行、§15 首个动画批次"基础契约与 UI 接缝"。
> 本文件只记录施工状态与证据；目标与验收以设计为准，不在此重复定义。

## 批次规划

| 批 | 范围 | 状态 |
|---|---|---|
| 表现壳 ❚ 生命周期收敛 | SceneService 迟到加载代次检查、EntityService 实体作用域/关闭面/重复归还、AudioService 分域时钟、GameObjectPool 释放面 | 已完成（`a850bcb`，见下） |
| 动画 ❚ 基础契约（纯规则半部） | AnimationId/Channel/Handle（三分量身份）/终态、Profile/Resolver（登记校验 + 回退深度/成环）、CharacterAnimationPlayer（通道仲裁/终态恰好一次/有界保留/§9 释放序）、IAnimationBackend 能力面 | 已完成（`88344e1`，见下） |
| 动画 ❚ UI 接缝半部 | 转场策略的播放终态与复位契约（§6.3/§10）：`MotionOutcome`/`MotionPlayback`、`Kill(true)` 不复位修复、`OnKill` 不作完成信号 + `KillOnDisable` 不回调的壳层主动取消 | 已完成（`914fb19`，见下） |
| 动画 ❚ Animator 后端与 Driver | AnimatorControllerPlayable + Manual PlayableGraph（§7）、Profile 资源解析、Sim 快照/事件 → 请求的 Driver、真角色验收 | **首段已交付**（2026-09-25，见下——后端/驱动/移动三态/真角色模型接入；上半身混合、真对局动作对齐/打断/重连/8 人基线留 G2 垂直切片余部） |
| 表现壳 ❚ VFX 治理 | VfxService 在途真取消（生命周期令牌）、关闭释放面（回收活体/排空池/清缓存/销毁自建根）、迟到不复活、诊断计数 | **已完成**（2026-09-25，见下） |

## 施工记录

### 2026-09-25 · 表现壳生命周期收敛（提交 `a850bcb`）

> 依据：《框架先行》§7 队列第 4 项余部 + §6 样例三失败清单（**加载后 Owner 已退出、重复归还、世界/UI 暂停**）+ §5 接缝 1。

**修掉的两个真实缺陷**：

- **SceneService 迟到加载回写**（`Shell/SceneService.cs`）：原实现在 `await` 之后**无条件回写** `_single`——等待期间宿主 `ReleaseAll()` 后，加载完成仍把**活句柄**写回已释放的服务，后续无人释放 = 租约泄漏。现改为**代次检查**：`ReleaseAll` 递增代次并使在途加载过期，迟到结果**就地释放自己的租约**并计数（`DiscardedLateLoads`），不回写——落实 §9"无法取消的底层加载仍须在完成时检查代次；迟到结果释放自己的租约，不能回写已回收对象"。叠加加载同样处理；`ReleaseAll` 幂等 + 取消宿主代次令牌（`LifetimeToken` 供调用方链接）；释放后拒绝新加载（fail-fast）。
- **AudioService 分域时钟**（`Shell/Audio/AudioService.cs`）：原 `FadeAsync` 读 `Time.deltaTime`，注释声称"世界轨：时停即停"——但本工程 `Time.timeScale` **恒为 1**（`GameClock` 纪律：单一缩放源），读它等于**永不暂停**，注释所称语义从未成立。现按组取时钟域：Effect/Voice 走 `IWorldClock`（世界暂停/变速），Ui/Bgm 走 `IUIClock`；暂停期间 dt 为 0 → 淡变原地保持（不推进也不误判完成）。未绑定时退化为真实帧步进（编辑器/测试兜底）。`BindClocks` 在 `GameModules.Presentation` 注入。

**所有权与释放面**：

- **EntityService**（`Shell/Entity/EntityService.cs`）：`EntityHandle.Scope`（`ClientScope`）作为实体的**取消源与资源登记面**——宿主作用域注入时建子作用域，宿主退出（离场）级联取消实体上的全部工作；回收点先 Dispose 作用域再归还池（§5 接缝 1）。新增 `Shutdown()`：取消在途加载（链接生命周期令牌）→ 连锁回收全部活体 → 清空竞态表 → 排空实例池。重复归还/未知句柄由 Warning 改为**幂等 no-op + `DuplicateReturns` 计数**（清理路径抛异常会断在半路，静默又埋掉纪律问题）。新增诊断面 `PooledTotal`/`ActiveCount`/`InFlightCount`。
- **GameObjectPool**（`LiteFramework/Scripts/Unity/Pool/GameObjectPool.cs`）：新增 `Clear()`（排空闲置件）与 `Dispose()`（Clear + 销毁**自建**池根；注入的 root 归注入方）。`ObjectPool.Clear()` 已存在（Trim(0)），不重复造。`DestroyAtRuntime` 统一销毁口径：**编辑态 `DestroyImmediate` / 运行态 `Destroy`**——EditMode 无延迟销毁帧，直调 `Destroy` 会报错（本批编辑态用例实测踩到）。`DontDestroyOnLoad` 补 `Application.isPlaying` 门禁（与 UIService/AudioService 同口径）。

**开发中被用例揪出的真实缺陷**：`Shutdown` 按 `_active` 快照遍历时，**级联回收会带走子件**——不检查"是否仍在 `_active`"会对已回收句柄再走一次 `HideInternal` 并抛键不存在。已补检查并用**带挂接链的关闭用例**钉死。

**已知边界（登记）**：

- `VfxService` 在途加载仍用 `CancellationToken.None` + `Cancelled` 标志（**惰性取消**：结果丢弃但不真取消底层）；其预算/池/租约治理随表现壳后续批统一收敛。
- `SceneService` 仍是进程级单例（**非 SceneScope 子作用域**）；SceneScope 与真场景资源验收归 U2/场景批。
- 本仓多个第三方资产目录缺 `.meta`（GameFramework/FX/Suriken 等，**预先存在**）——不在本批范围。

### 2026-09-25 · 动画基础契约（提交 `88344e1`）

> 依据：《动画模块专项设计》§15 首个动画批次"基础契约与 UI 接缝"的**纯规则半部**——其退出条件"分域暂停、重复停止、旧回调、迟到加载、清理全部可验证"在纯规则层可完全验证。§3/§4/§5/§6 逐条落地。

**代码位置**（`Assets/LiteFramework/Scripts/Core/Animation/`，**零引擎依赖**——Animator/PlayableGraph 等 Unity 类型留在后端实现里，§3"所有 Unity 类型留在 Unity/View/Shell 层"）：

- `AnimationTypes.cs`——`AnimationId`（类型化语义 ID；框架不认识具体语义）、`AnimationChannel`（视觉归属，**不是**业务优先级）、`AnimationStopReason`/`AnimationTerminalState`（§5 终态表）、`AnimationHandle`（**三分量身份**：播放器 + Owner 代次 + 请求序号——旧句柄因此不可能停到复用对象的新播放）、`AnimationRequest`（**不含资源路径**，无法绕过 Profile）、`AnimationStartResult`（接受/拒绝 + 稳定拒绝原因）、Resolver 输出 `AnimationResolvedPlayback`。
- `AnimationProfile.cs`——Profile/Resolver：登记即校验（空 ID/空绑定/非法速度区间显性失败）；回退策略 `Reject`/`UseFallback`；**成环在登记期拒绝**、**深度上限在解析期约束**（链长是随登记逐步生长的性质，单次登记看不到全貌）；速度越界按 `UnsupportedCapability` 拒绝而非静默夹取（§4"不把不支持的能力静默降级"）。
- `IAnimationBackend.cs`——后端契约 + 能力标记（Looping/StartAtNormalized/SpeedOverride/LayeredChannels）：能力显式声明，**播放器据此拒绝而非假装支持**。
- `CharacterAnimationPlayer.cs`——§5/§6 仲裁核心：每通道至多一个待提交 + 一个当前播放（**无队列**）；替换时旧播放（含加载中）得 `Interrupted`；**迟到装载只释放自己的资源、不抢回通道**；终态**恰好一次**且回调重入安全；终态记录**有界保留**（超限淘汰并计数，过期查询返回未找到）；释放顺序按 §9（代次失效 → 停止接受请求 → 在途/在播确定终态 → 撤销订阅 → 释放后端）。时钟：**不持有分域时钟、不自行缩放**——调用方按 §7 把已缩放的 delta 交给 `Tick`（§7"避免重复缩放"）。

**开发中被用例揪出的三处真实缺陷**：`slots.Active` 未置位导致"替换"路径从不触发；静态通道缓冲在嵌套播放器场景互相覆盖；已终态句柄无可查记录（`TryGetState` 答不出"它是什么"）。

**验证（两批合并实测）**：

| 门禁 | 命令 | 结果 |
|---|---|---|
| L1 | `scripts/test.ps1 -Lane L1 -Profile PullRequest` | **550/550 PASS**（LiteFramework 272→303，新增 31 例动画契约） |
| Unity 编译 | `scripts/l2-unity-gate.ps1` | **0 错误** |
| L2 EditMode | `scripts/l2-unity-gate.ps1` | **107/107 PASS**（+10 ShellLifecycle；UI 接缝批后为 112） |
| Player 构建 | `scripts/build-player.ps1` | **PASSED** |
| Player 冒烟 | `scripts/player-smoke.ps1 -TimeoutSec 120` | **PASSED**（`[Asset] ready` 真标记） |

**用例覆盖**（§13 L1 行"失败不抢占；终态恰好一次；旧请求无写入权；容量受限"）：字段校验（NaN/零/负速度、越界起点、越界速度区间）、回退（Reject/UseFallback/成环/深度）、终态（Completed 恰好一次、循环不自然 Completed、重复 Stop 不重复通知、释放得 OwnerDisposed）、句柄（旧 Handle 停不动新播放、Owner 换代全体失效）、通道（不同通道互不替换、同通道替换 Interrupted、加载中替换终止待提交、装载失败 Failed）、拒绝不抢占、后端执行失败收 Failed、有界淘汰、Tick 单一入口与加载中不推进。

**剩余边界（登记）**：

- **UI 接缝半部**：已于 `914fb19` 交付（见下节）——转场策略的播放终态与复位契约，含 §2 登记缺陷的修复。
- **Animator 后端与 Driver 未做**：`AnimatorControllerPlayable` + Manual `PlayableGraph`（§7）、Profile 资源解析（`Assets/LiteSim/View/Animation`）、Sim 快照/事件 → 请求的 Driver（含 `action_seq` 对齐）。**本仓已有可用素材**（`Assets/Animator/Kazuko.controller`、`Assets/Animation/{Aim,Reload,Shoot}`——该资产现状修正了《动画专项》§2"未发现 Controller/Clip 资产"的核查基线），但真角色垂直切片属 G2，受角色资源与玩法阶段稳定约束（§15 第二行）。
- 编辑器校验（§11）、诊断与 0B GC 预算（§12）、L2 PlayMode（§13）随后续批次。
- **没有真实角色与 Player 证据时，不以接口和单元测试齐全宣布动画模块完成**（§13 末句）——本批交付的是契约与纯规则，不是动画模块完成。

### 2026-09-25 · 动画 UI 接缝半部（提交 `914fb19`）

> 依据：《动画模块专项设计》§15 首批 UI 接缝半部、§10"UI 的播放终态与 UI 操作结果分层映射"；《UI框架总设计》§6.3 复位契约；动画 §2 登记的"OnKill 不当完成信号"缺陷。

**修掉三个实测发现的真实缺陷**（本仓 DOTween 1.3.030，反射 + 行为双向实测）：

1. **`Kill(true)` 不复位** —— §6.3 复位契约**从未兑现**。原实现注释称 "Kill(complete=true) 跳终值即复位"，实测**在裸 Tweener 与 Sequence 上都不跳终值**且不派发任何回调。后果：超时/取消时 Tween 停在途中，页面留在半透明/偏移状态。正确复位 = 先 `Complete()`（跳终值并触发回调）再 `Kill(false)`（清理）。
2. **`OnKill` 被当作完成信号**（§2 登记缺陷；实测**比记录更严重**）：本仓 DOTween 的 **OnKill 根本不触发**——显式 `Kill(false)` 与 `KillOnDisable` 均实测无回调。后果：页面在转场中被回收（缓存淘汰/Destroy/低内存）时，任务**悬着直到 2s 超时**才收。修法两层：策略层 `OnComplete` 记 Completed、`OnKill` 只兜底让任务不悬（迟到回调不覆盖已写终态）；壳层 `UITransitionRunner` 感知事务锁定页面进入 `Recycled/Disposed` → 主动取消并立即收尾（`TransitionContext.OwnerGone`）。Cover/Pause/Closing **不作**取消依据——那些仍会收尾，避免把正常流程误判成丢失。
3. **无播放终态出口**：调用方无法区分"播完"与"被杀"。新增 `MotionOutcome`/`MotionPlayback`/`MotionHandle`（`Shell/UI/Anim/MotionOutcome.cs`，零引擎依赖）；`OutcomeOf` 分类改为"超时/丢弃两种事务级事实优先，其余交给播放终态"。`ITransitionStrategy`/`IReplaceTransition` 增带终态的**可选实现位**（默认接口实现转发旧签名）——既有策略零改动、语义不变。

**另修：player-smoke 标记过宽的假通过**。标记集里的裸词 `'AssetService'` 会匹配 `[Asset] init begin` 的**栈帧行**（`LiteGame.AssetService:InitAsync(...)`）——即应用仍在启动中就判通过。本轮实测撞到：一次运行卡在 `BFS step=CheckPlatform`（120s 未就绪）却因该裸词被判 PASSED。已移除该标记（只保留完成态证据），并在脚本注释登记为与 C1-11 正则缺陷同类的问题。收紧后复跑：真标记 `[Asset] ready` 命中，PASSED。

**验证**：Unity 编译 0 错误；EditMode **112/112**（+5：策略 Completed/Cancelled 分类、超时优先且已复位、真实策略正常完成 alpha 到目标、**真实策略取消复位回归卡**、**页面被回收主动取消回归卡**）；L1 **550/550**；Player 构建 PASSED + 冒烟 PASSED。

**剩余边界（登记）**：

- **UiFx 原语的取消复位契约未做**：Pulse/Flash/Slide/CountUp 是"原语按需执行"而非"页面生命周期动效"（后者已由本批转场策略覆盖）。原语被中断时属性不自动复位——需按 §14 清单随调用方迁移时补，届时再决定是否需要 Owner 代次。
- **未定位的观察项**：本轮有一次冒烟在 120s 内未走完 YooAsset BFS 初始化（复跑即通过）。**不声称已定位根因**；标记收紧后此类慢启动会如实失败而非假通过，后续若复现再排查。

### 2026-09-25 · VFX 所有权收敛（关闭释放面 + 真取消）

> 依据：《框架先行建设与业务接入专项设计》§3"Scene/Entity/Audio/VFX 所有权"（四类中 VFX 是唯一未收敛项）、§5 接缝 1（谁创建、谁取消、谁释放）、§6 样例三失败清单（加载后 Owner 已退出）。VFX 预算/降级（`VfxBudget`：MaxActive/Overflow/低端跳过）与租约通道（C2 批①已把加载口接 `PrefabLeaseCache.GetAsync`）此前已就位，本批不动。

**背景**：VFX 是表现壳四类生命周期中唯一未收敛的——缺的是**服务本体的关闭面**：`LoadAndMaterializeAsync` 传 `CancellationToken.None`（宿主关闭无法取消在途）、迟到加载结果会写进已死服务（`_prefabs` 缓存 + 实例化进将被销毁的世界容器）、无 `Shutdown()`（活体不归池、池不排空、自建 `[VfxWorld]` 不销毁）。

**修正**（对齐 EntityService/AudioService 的宿主关闭语义）：

- **真取消**：内部 `_lifetime` CTS；加载改传 `LifetimeToken`——宿主关闭即真取消（经 `PrefabLeaseCache`→`SharedLoadCoordinator` 链路，最后一个等待者退出会撤销共享加载）。**单实例 Stop 仍走 `Cancelled` 丢弃**——prefab 加载是共享缓存，不因单个调用者退出而撤（§5.1"共享工作与单个调用者取消区分"，代码注释钉死）。
- **迟到不复活**：续体在 `inst.Cancelled || IsShutdown` 时**不写 `_prefabs` 缓存、不实例化**，计数"迟弃"（`Snapshot` 新键）——此前只挡了"宿主回收"路径，"服务关闭"路径会把迟到 prefab 写进已死服务。
- **关闭释放面 `Shutdown()`**（幂等）：先行取消在途 → 快照遍历 `Stop` 全部活体（与正常回收同一路径，不留第二套语义）→ 清挂点表 → `_pool.Dispose()`（排空闲置 + 销毁自建池根；注入的 root 归注入方）→ 清 `_prefabs` 引用（**租约释放归装配层** `PrefabLeaseCache.ReleaseAll`——本服务只持引用）→ 销毁自建 `[VfxWorld]`（编辑态 `DestroyImmediate` / 运行态 `Destroy`，与 GameObjectPool 同口径）。关闭后 `Play` **fail-fast** 拒绝。
- **DontDestroyOnLoad 补 `Application.isPlaying` 门禁**（自建世界容器分支——表现基础批修 UIService/AudioService/GameObjectPool 时漏了 VFX 这处，EditMode 直调会抛）。
- **装配关闭序修正**：`Presentation.ShutdownAsync` 现为 **Entity → Audio → VFX → `PrefabLeaseCache.ReleaseAll`**（先收使用者、再放租约——VFX 缓存的 prefab 引用先清，租约最后归零）。
- `VfxHandleTable` 补 `CollectAll`（Shutdown 快照遍历，避免边 Stop 边枚举）。

**测试**（EditMode +4，全替身零素材）：真取消令牌断言（loader 收到的就是服务生命周期令牌，关闭后 `IsCancellationRequested`——区分"真取消"与"只靠丢弃"）、迟到完成不写缓存不实例化 + 迟弃计数、关闭回收活体/排空池/幂等/关闭后 Play fail-fast、自建世界容器随关闭销毁且注入的不动。

**验证（本批实测）**：L1 **713/713**（本批未动 Core，回归核证）；L2 门禁通过（12026 meta 合法、Unity 编译零错误、EditMode **166/166**）；Windows x64 IL2CPP Player 构建 PASSED + 冒烟 PASSED（`[Asset] ready` 真标记）。

**边界登记**：预算/池深/低端降级已有不重做；VFX 无分域时钟需求（粒子由引擎驱动；到期回收按世界时钟冻结——时停即冻结到期，语义本就正确）；防重播静默门住 View（SimView），不在本服务（原边界不变）。

### 2026-09-25 · 真角色模型接入与动画后端首段（CombatGirlsCharacterPack）

> 依据（资源指令 2026-09-25）：**模型只用 CombatGirlsCharacterPack**；主要动画也用该包，其余酌情补充；《动画模块专项设计》§7"首版角色后端用 AnimatorControllerPlayable 承载控制器，PlayableGraph 采用 Manual 更新"、§15 第二批"角色垂直切片"的可在本阶段落地部分。资源包按仓库政策**不入 VCS**（.gitignore 第三方素材）——缺包克隆对局回灰盒（可复现降级，兜底不删）。

**① 运行时视图 prefab**（经 Pipeline 创建——序列化资源不手工改；实测排障两处）：

- `Assets/CombatGirlsCharacterPack/Runtime/RifleGirl_View.prefab` = 包内 `Rifle_Full_Body.FBX` 模型 + 包内 `Rifle_Controller.controller` + `AnimatorCullingMode.AlwaysAnimate`（俯视角+插值位置——视锥外也推进）。
- **Avatar 必须挂包内主源** `Humanoid_F.fbx` 的 Avatar 子资产：实测 RifleGirl 全部模型 `avatarSetup=CopyFromOther`（自身**无** Avatar 子资产、根上无 Animator），全部包内模型/预制都引用 Humanoid_F 的 Avatar——直接用包内现成 `Magica cloth` prefab 不可行（依赖未安装的 Magica Cloth 插件，脚本 GUID 失链），故自建视图 prefab。
- 收集器新增 **Characters 组**（CollectPath=`Assets/CombatGirlsCharacterPack/Runtime`——只收运行时 prefab 目录，FBX/材质/控制器/动画经 YooAsset 依赖自动进包；`BundleCollectorSetting.asset` 经 Pipeline 修改）。
- `SimView.DefaultEntityPrefab` → 新路径（原 `Assets/Art/Characters/BoxFighter.prefab` 占位从未入库）；`ProcedureBattle` 灰盒兜底**保留**（缺包克隆降级）。

**② 动画后端与驱动首段**（`Assets/LiteSim/View/Animation/`，View 层——Unity 类型不出程序集）：

- `AnimatorAnimationBackend : IAnimationBackend`：`AnimatorControllerPlayable` + **Manual PlayableGraph**（Evaluate 只经 `Tick`——§7"Graph Evaluate 只由一个驱动器调用"）；绑定 = 控制器状态名，存在性经 `Animator.HasState` 校验（**未知绑定显性 Failed**，不静默留在旧姿态假装成功）；能力只声明 `Looping`（起点/速度/多通道未声明 → 播放器按 §4 拒绝而非降级）；一次性状态到边界 **Completed 恰好一次**（循环状态永不 Completed）。
- `CharacterLocomotionDriver`：视图速度分档（<0.5m/s Idle / <2.5m/s Walk / ≥2.5m/s Run——`CombatConfig.MoveSpeed` 默认 5）→ 每实体 `CharacterAnimationPlayer`（共享不可变 Profile）；速度源 = 视图 Transform 帧间位移（本地衰减/远端插值同一来源，不读 Sim 内部）；**视图回收即播放器收口**（Graph 不随复用泄漏——新占用者重建，Owner 代次语义）；灰盒视图（无 Animator）静默降级为无动画。
- `CombatGirlsAnimationProfile`：ID→状态绑定单源登记（Idle/Walk/Run——本批消费者）；包内 Hit1/Hit2/Die1/Die2/Reload/Aim*/Evade/Stun 状态已备，**出现消费者时再登记**（不预建空绑定）。
- 装配：`ProcedureBattle.AttachView` 建驱动 → `OnUpdate` 在 `BattleContext.Tick` 后推进（视图位置先更新、再解析姿态，§7 更新次序）→ `DetachView` 先拆驱动（消费者先于视图本体）。

**测试**（EditMode +7；缺包克隆 `Assert.Ignore` 跳过，有包机器全量校验）：资产校验 3（prefab 挂包内控制器+主 Avatar+蒙皮网格；控制器移动与主要战斗状态齐备；收集组覆盖运行时目录——反射读配置，不为一条断言扩测试 asmdef）；后端/驱动 4（有效状态提交+循环不自然结束；**一次性 Die1→Completed 恰好一次 + 未知绑定→Failed**；速度三档 Idle/Walk/Run 与视图回收收口；灰盒视图不建播放器不抛）。**实测确认**：`Animator.HasState` / `AnimatorControllerPlayable.Play` / Manual Graph `Evaluate` 在 EditMode 均可用（编译期曾踩两处：`PlayableGraph` 在 `UnityEngine.Playables`、驱动命名空间需 LiteGame 侧 using）。

**验证（本批实测）**：L1 **713/713**（未动 Core 域）；L2 门禁通过（12034 meta 合法、Unity 编译零错误、EditMode **173/173**）；Windows x64 IL2CPP Player 构建 PASSED（Characters 组随内置包链验证）+ 冒烟 PASSED（`[Asset] ready` 真标记）。

**边界登记（G2 垂直切片余部）**：上半身混合（LayeredChannels）、开火/换弹/受击/死亡的事件驱动动画（帧事件→Driver 消费面已备）、真对局动作对齐/打断/重连去重与 8 人基线未做；世界暂停的动画冻结随视图移动的时钟裁决一并归 G2；冒烟不进对局——真对局真角色验收留 G2 垂直切片（资源包不入库政策下，克隆机器跑对局仍是灰盒）。
