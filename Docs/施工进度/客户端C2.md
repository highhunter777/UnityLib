# 客户端 C2 施工进度：真实用户与对局闭环（会话子集 + 表现）

> 依据：《商业级通用客户端框架总设计》§19 C2"Login、Lobby、Match、Battle、Result 流程和 Account/Match Scope；MetaClient、BattleClient、Connect 完整终态和客户端重连 Restore；BattleContext 创建/销毁、网络/Sim/View/HUD 接线"；§6.2 作用域表（Match 域持 BattleClient/BattleContext/输入历史）、§7.1 标准流程链、§10.3 重连链；《框架先行建设与业务接入专项设计》§4"客户端通用服务"、§7 施工队列第 5 项"C2 会话/R2 必需宿主"；《状态同步专项设计》§5.2/§6/§9；《动画模块专项设计》§1 时钟裁决。
> 本文件只记录施工状态与证据；目标与验收以设计为准，不在此重复定义。

## 批次规划

| 批 | 范围 | 状态 |
|---|---|---|
| C2 批① 会话与对局上下文 | BattleClient（传输+RoomClient 所有权）、BattleContext（网络→镜像→预测/和解编排）、Match/Battle 流程与 Account/Match Scope、SimMapData 地图单源、断线自动重连策略 | 已完成（`3a66e73`，见下） |
| C2 批① 附带：通用表现壳所有权 | PrefabLeaseCache 租约缓存、Entity/VFX 加载口接入、Audio StopAll/Shutdown、DOTween Manual 轨接 UIClock | 已完成（同提交，见下） |
| C2 批② 表现视图与输入 | LiteSim.View：SimView（镜像/远端插值/本地和解衰减/事件静默门）、EntityViewMap 池、BattleCameraRig、PlayerController 输入采集与三个门、帧事件逻辑帧交付接缝 | 已完成（`b336336`，见下） |
| C2 批③ Login/Lobby/Result | MetaClient、身份票据、大厅与结算页（正式业务范围） | 未开始（归 G3；`ProcedureId` 未加枚举以免出现空阶段） |
| 后续：HUD | 战场 HUD（HP/护盾/弹药/CD/比分/延迟）、C# 脏位模型 | 未开始（归 U2；设计 §3"高频 HUD 由 C# 脏位更新"） |

**已交付"会话 + 表现"两批**：进房 → 对局运行（可见可操作/远端平滑/本地 0 延迟）→ 断线重连 →
离场收尾。设计标题里的 Login/Lobby/Result 与 HUD 见上方批③与 U2，不据本批宣称 C2 整体完成。

## 施工记录

### 2026-09-25 · C2 批① 会话子集与通用表现壳所有权（提交 `3a66e73`）

**代码位置（会话子集）**：

- `Main/Battle/BattleClient.cs`（新）——对局客户端会话：**持有传输与 RoomClient**，负责连接/进房装配与生命周期。会话状态机**不重复**——§9.3 相位机（Idle/Connected/SuspectedLost/Reconnecting/Restoring/Failed）全部在 `RoomClient`（R1 批③交付，L1 已有 7 例）；本类只管"传输 OnConnected 且相位 Idle 时发 Join"与所有权闭环（Dispose 逆序：RoomClient → transport，因 `RoomClient.Dispose` 契约明确不代管传输）。初次 Join 与重连两条路径互斥不重叠（重连时相位非 Idle，`OnTransportConnected` 自动跳过）。
- `Main/Battle/BattleContext.cs`（新）——一局对局的**全部运行态编排**：网络事件 → 持久镜像重建（协议单源 `SnapshotReassembler`）→ 预测/和解（`RollbackSim`）→ 输入上行。逻辑镜像 `Tests/LiteNet.Tests/HeadlessClient.cs`（已验证的集成形态），差异三点：传输归 `BattleClient`、地图走 `SimMapData.StandardBattleMap` 单源、生命周期挂 Match Scope（ctor 建 `Match` 子作用域，Dispose 即拆）。事件退订经 `DelegatedDisposable` 登记进 Scope，LIFO 保证晚挂先退——不靠调用方记得退订。
- `Main/Procedure/ProcedureMatch.cs`（新）——建 Account Scope → BattleClient 连接 + Join → JoinAck 即移交 Battle（超时 10s 进确定失败态）。失败/取消就地 Dispose Account，不流转。
- `Main/Procedure/ProcedureBattle.cs`（新）——OnEnter 建 BattleContext 并每帧驱动（`OnUpdate` 是唯一驱动入口：容器 Seal 后不可再注册 ITickable，对局期驱动走阶段生命周期，天然随离场停止）；Ended/会话失败/取消一律逆序收尾（`BattleContext.Dispose` → Match Scope → Account Scope，§6.2）后回 Main。
- `Main/Procedure/ProcedureArgs.cs` / `ProcedureId.cs`——迁移 payload 增 `BattleClient`/`AccountScope`（只读、随迁移移交所有权）；流程 id 增 `Match`/`Battle`，兑现原《UI扩展能力设计》§9.1 预留位。
- `Core/Hosting/DelegatedDisposable.cs`（新，LiteFramework.Core）——把退订/归还/计数回退类无类型清理动作包成 `IDisposable`，供 `ClientScope.Register` 登记；Scope 的 LIFO 释放面因此覆盖"事件退订"这类最易漏的生命周期项。
- `LiteSim/Core/Scripts/SimMapData.cs`——新增 `StandardBattleMap()`：**地图构造单源**。原 `RoomServer.BuildStandardMap` 平移至此并改为委托调用——客户端预测世界与服务端权威世界必须共用同一构造，否则出生点错位、预测永不分叉收敛。

**断线恢复策略（C2 会话子集口径）**：`SuspectedLost` 即凭 JoinAck 票据自动 `BeginReconnect`；`Failed`（重连超时/被拒/版本不符/无票据）→ `Ended(SessionFailed)` 交流程层裁决回 Main。**不在上下文内静默重建会话**（正式重试 UI 与重新登录归 G3）。恢复完成路径：`OnReconnectResponse` → 权威全量重建镜像 → 和解到该帧 → `CompleteRestore` 发恢复 ACK（服务器收到前抑制本席位增量广播，§9.3 步骤 3~6）。

**代码位置（通用表现壳所有权，同批）**：

- `Shell/Resource/PrefabLeaseCache.cs`（新）——表现壳 prefab 租约缓存：按 location **持租约返回**，同 location 重复取用共享一份；并发首取竞态后到者归还、共享先到者；`ReleaseAll` 幂等且释放后拒绝再取。纠正旧绑定"`LoadAssetAsync` 返回前 Release 句柄"的**悬空引用反模式**（UI-05 同源问题在表现壳的落点）。
- `Shell/Entity/EntityService.cs` / `GameModules.Presentation`——Entity/VFX 加载口改经 `PrefabLeaseCache`（装配点注入，缺省静态门面为迁移期兼容）；Presentation 模块 Shutdown 接上释放面（音频全局停止 + 租约归零）。
- `Shell/Audio/AudioService.cs`——`StopAll()` 总线全局停止面（系统路径不等淡出）；`Shutdown()` 释放面（全局停止 + 根销毁，幂等）。ctor 的 `DontDestroyOnLoad` 补 `Application.isPlaying` 门禁（编辑态直调非法——与 `Shutdown` 的 `DestroyImmediate` 分支同口径）。
- `Shell/Lua/LuaPreloader.cs` / `ProcedurePreload`——Lua 清单经**装配点注入**，运行时不再直查 YooAsset 静态门面（热更批以发布清单替换绑定时本类零改动）。

**DOTween 版本裁决（本仓 DOTween 为闭源 DLL，反射实证）**：

原实现按 `SetUpdate(UpdateType.Custom)` 接线——**该枚举值在本仓 DOTween 中不存在**（`UpdateType` 仅含 `Normal/Late/Fixed/Manual`），编译期即失败。实测确定 Manual 轨语义后改接：

- `Manual` 轨**不受引擎自动更新**，只由 `DOTween.ManualUpdate(dt, unscaled)` 推进；不调用即完全冻结 → 暂停语义天然成立；
- `independent=true` 时取 `unscaled` 参数（实测 `ManualUpdate(0.1, 0.4)` 推进 0.4）；
- `ManualUpdate` **只推进 Manual 轨**，Normal/Late/Fixed 轨的既有 Tween 不受影响（用例钉死）——这是 UIClock 接管"只动 UI 轨"的前提。
- `Shell/UI/Anim/UiAnimationClock.cs`（新）——`UiAnimationClock`（静态 UI 时钟绑定点，域重载静态清理）+ `DotweenUiClockDriver`（按 `UIClock.ScaledDelta` 派发 Manual 轨；步进为零即不调用 = 暂停即停）。`UiFx` 四原语与转场策略 `BuildBase` 改 Manual 轨；`AnimatedImage` 序列帧改走 `UiAnimationClock.Delta`；`GameEntry.ResetForEditorReload` 补清绑。落实《动画模块专项设计》§1 裁决"当前 SetUpdate(true) 不等价 UIClock：必须由 UI 动效适配层明确接入项目 UIClock"。

**测试**：

- `Assets/Tests/EditMode/BattleSessionEditModeTests.cs`（新 9 例，全替身/零网络/零墙钟）——BattleClient 装配面：连接即 Join 且载荷正确、断线重连闭环（重拨自动发请求 → Restoring → CompleteRestore 回 Connected）、版本不符转 Failed 且**绝不发恢复 ACK**、票据耗尽 `BeginReconnect` 失败且不重拨；BattleContext 编排面：StartGame 按 seed 重建同构世界 + 快照和解 + 本地实体按 Slot==PlayerId 对齐、快照先于 StartGame 到达被安全丢弃、会话失败触发 `Ended(SessionFailed)`（自动重连先行）、离场 `Ended` 恰好一次 + Dispose 拆订阅且 Match Scope 清零（**离场无 Match 残留**的载体断言）、Tick 未开局只泵网络/开局上行输入并推进预测。
- `Assets/Tests/EditMode/ShellOwnershipEditModeTests.cs`（新 7 例）——租约共享/ReleaseAll 归零/释放后拒绝、音频 StopAll 与 Shutdown 幂等、Manual 轨按 UIClock 派发且暂停即停、**轨隔离**（Normal 轨不被 `ManualUpdate` 推进）、未绑定退化、序列帧暂停不推进。
- `Assets/Tests/EditMode/LiteGame.EditModeTests.asmdef`——补 `LiteNet` 引用与 `Google.Protobuf.dll`/`DOTween.dll` 预编译引用（`overrideReferences: true` 下非引用时不可见，与 `LiteNet.asmdef` 同款形态）；分类改用 `[Category]`——Unity 自带 NUnit **3.5 无 `TraitAttribute`**（.NET 侧 L1 的 3.13 才有），对齐既有 EditMode 约定。
- Sim 源码变更 → 重跑 `scripts/gen-build-hash.py`：`213a3fd1a45c1138`（52 文件；同输入复算一致，可复现）。

**验证（本批实测）**：

| 门禁 | 命令 | 结果 |
|---|---|---|
| L1 | `scripts/test.ps1 -Lane L1 -Profile PullRequest` | **505/505 PASS** |
| Unity 编译 | `scripts/l2-unity-gate.ps1` / Pipeline `recompile_status` | **0 错误，compilationFailed=false** |
| L2 EditMode | `scripts/l2-unity-gate.ps1` | **85/85 PASS**（69 存量 + 本批 16 新增） |
| Player 构建 | `scripts/build-player.ps1 -Profile BuildProfiles/windows-x64.json` | **PASSED** |
| Player 冒烟 | `scripts/player-smoke.ps1 -TimeoutSec 120` | **PASSED**（`[Asset] ready` 真标记命中，process alive） |

**实施口径偏离与登记**：

1. **单提交覆盖两条线**：`ProcedurePreload` 构造签名变更（清单注入）与 `GameModules` 的装配点改动同处一文件——只提交会话子集会使该提交编译不过，故表现壳所有权与本批合并为一个自洽提交。
2. **DOTween 接线偏离原设想**：`UpdateType.Custom` 在本仓 DLL 不存在，改用 `Manual` 轨。二者语义等价（都由项目自己泵、都不受引擎自动更新），但**这是版本事实而非设计选择**——若后续升级 DOTween 出现 `Custom`，应重新裁决而非静默切换。
3. **F9/F10 测试热键加门禁**：进对局/离场热键按项目既有口径加 `UNITY_EDITOR || DEVELOPMENT_BUILD || LITEFRAMEWORK_DEBUG`（与 `SimSandbox` 一致），不进正式包；正式入口（页面按钮/Matchmaking/离场）归 U2/G3。
4. **`ProcedureId` 不加 `Result`**：设计 §19 列出 Result，但正式结算页属 G3；本轮不加枚举值以免出现空阶段（`ProcedureId` 注释已登记）。对局结束当前回 Main。
5. **`BattleContext` 的 `inputProvider` 为空（批次①当时）**：输入采集属批②（`LiteSim.View` 的 `PlayerController`），该批已接线——`ProcedureBattle` 经 `AttachInput` 注入真实采集。
6. **`viewFrame: 0` 与 `HeadlessClient` 同口径（批次①当时）**：延迟补偿回溯点尚未接线（取视点帧的正确来源是 `LastSnapshotFrame + SimConfig.InterpFrames`）——批②已接真实视点帧；**`HeadlessClient` 仍用 0**（它是 .NET 侧集成夹具，不属 C2 范围，按需另行调整）。
7. **地图单源的跨端一致性**：`SimMapData.StandardBattleMap` 被 `LiteSim.Core`（双端共享）与 RoomServer 共同引用，`buildHash` 覆盖 Sim 源码，故本次地图改动使两端 hash 同步变化——已重跑生成器，`Tests/LiteNet.Tests/BuildHashTests` 按同规则复算通过。

### 2026-09-25 · C2 批② 表现视图与输入采集（提交 `b336336`）

> 依据：《联机战斗演示专项设计》§3（SimView 职责、相机跟随）、§5 联调顺序"**输入和三个门 → SimView/相机**"；《状态同步专项设计》§6.2（本地跟预测/远端插值/和解衰减/复位硬切）、§3.4.1（视点帧）；《M11实施指导》§2.2–2.4。

**代码位置（LiteSim.View 表现层）**：

- `View/ViewTransformMath.cs`（新）——**零引擎依赖**的纯表现数学：`Lerp` / `YawLerp`（最短弧，跨 ±π 不绕远路）/ `Decay`（指数衰减）/ `ShouldSnap`（硬切判据）/ `PlanarDistance`。按《M11实施指导》"插值/衰减抽成纯函数 → 进 Tests/LiteSim.Core.Tests（L1 覆盖）"落地：该文件经 csproj **源链接**编入 L1 项目（`LiteSim.View` 引 UnityEngine，L1 不能 ProjectReference 它）——单一来源，语义不会在两侧漂移。衰减用 `1-exp(-k·dt)` 而非固定系数：帧率无关，掉帧不改变收敛手感。
- `View/SimView.cs`（新）——槽位镜像（活体建视图/死亡回收）、**远端**两份快照按视点帧插值（快照停摆时停在最新，**不无限外推**）、**本地**跟预测位置 + 和解误差指数衰减 + 超距硬切（复活/传送）、帧事件**静默门**。只读 Sim、只写视图（§1 原则 3）。
- `View/EntityViewMap.cs`（新）——槽位 → 视图的定长索引 + 按 prefab 分池（槽位是 Sim 的稳定寻址，禁 swap-remove）。回收方为 null 时走自有池；给了回收方（生产接 EntityService）则池归它所有——两份池不重复记账。
- `View/Camera/BattleCameraRig.cs`（新）——俯视角档位（俯角/距离/FOV/跟随平滑）参数化，相机不认识"游戏模式"（§2.4）；跟随用同一套指数平滑。
- `View/Input/PlayerController.cs`（新）——键鼠 → `SimInputFrame`；**上下文门**（UI 打开 → 意图全零，含 `Buttons`）；移动/瞄准长度 ≤1 归一。只产数据、不产生位移。

**帧事件交付接缝（Sim 侧修正）**：

原实现让 SimView 在**渲染帧轮询** `SimWorldState.Events`——**必然读不到**：`FrameDriver` 在逻辑帧回调返回后**立即清空**事件缓冲（决策⑥），且该回调已被 `RollbackSim` 占用。新增 `RollbackSim.OnFrameEvents`（**每逻辑帧**交付，追帧时一帧一次；重放路径仍按决策⑫"不消费即清"——正是不重播一次性副作用的来源），`SimView.OnFrameEvents` 据此消费并由静默门过滤。撤回原 SimView 中无效的 `VfxService` 调用：VFX 的位置入参接缝未定，且"服务不互相调用"（《动作与特效设计》§2.3）——事件经 `EventSink` 交编排方，VFX/音效接线归 C4。

**接线与灰盒兜底**：

- `BattleContext`——`AttachView`/`AttachInput`/`LocalPosition`；快照与重连恢复推进视图插值源（`CaptureFull` 两块轮转：增量包的缺席槽位必须从**持久镜像整体**取，不能只取本包槽位）；`viewFrame` 由 `0` 改为真实视点帧 `LastSnapshotFrame + SimConfig.InterpFrames`（§3.4.1 延迟补偿回溯点，批次①登记的缺口本批补上）。
- `ProcedureBattle`——持视图资源作用域与实体 prefab 租约；实体 prefab 缺失时**回退程序化灰盒**（`GreyboxEntity`）。灰盒阶段美术资源未入库属正常，不把缺资源当启动失败；且 `Assets/Art/` 被 VCS 忽略（第三方授权内容），硬依赖该路径会让对局路径不可复现——故兜底走引擎内置基本体（`Resources.GetBuiltinResource` 在本工程实测取不到内置胶囊/默认材质，改 `GameObject.CreatePrimitive`，实测自带当前管线共享材质）。视图根/相机自建（无场景相机时）；关闭序：视图 → BattleContext → Match Scope → Account Scope。

**验证（本批实测）**：

| 门禁 | 命令 | 结果 |
|---|---|---|
| L1 | `scripts/test.ps1 -Lane L1 -Profile PullRequest` | **519/519 PASS**（+14 纯数学用例） |
| Unity 编译 | `scripts/l2-unity-gate.ps1` | **0 错误** |
| L2 EditMode | `scripts/l2-unity-gate.ps1` | **97/97 PASS**（+12 SimView 用例） |
| Player 构建 | `scripts/build-player.ps1` | **PASSED** |
| Player 冒烟 | `scripts/player-smoke.ps1 -TimeoutSec 120` | **PASSED**（`[Asset] ready` 真标记） |
| 构建哈希 | `scripts/gen-build-hash.py` | `3a3c1be5960b0fc7`（Sim 源码变更必须重跑；LiteNet 153/153 含复算守卫） |

**实施口径偏离与登记**：

1. **事件交付从"渲染帧轮询"改"逻辑帧交付"**：这是**缺陷修正**而非风格选择——轮询形态在 `FrameDriver` 清缓冲语义下根本读不到事件（见上）。渲染帧只做视图同步/插值/相机。
2. **灰盒实体兜底而非报错**：对局路径不因缺美术资源而失败；正式角色 prefab 入库后租约获取自然命中真资源，兜底分支退场（不删代码）。
3. **HUD 不在本批**：设计 §3 的 HUD 属 U2（C# 脏位模型 + 页面）；本批交付 SimView 的 `EventSink` 与视图镜像两个数据面，HUD 可直接消费。
4. **EntityService 池对接为后续收敛项**：当前 `ProcedureBattle` 用 `SimView` 自有池（`recycler: null`）；接 `EntityService.Hide` 可复用表现壳的实例池与租约，但会让两套池语义耦合，留待表现壳收敛批一并处理。
5. **输入重绑/触屏未做**：`PlayerController` 为键鼠固定键位（同 SimSandbox 口径）；Action Maps + 重绑落 `SettingService`、触屏 `IntentSource` 归 U2（设计 §2.2）。

### 2026-09-25 · 审查修复：正式构建拒绝以测试身份进房（框架先行 §6）

**背景**（框架先行落地审查发现）：批① 偏离登记 3 的 F9 开发门禁只守住了**入口**——
`ProcedureMatch` 的测试身份（`TestHost/TestPort/TestRoomId/TestToken`）硬编码于生产 Runtime
程序集，正式包一旦出现任何新入口到达 Match 阶段，将以测试身份连接服务器（服务端 Join 侧
票据校验仍为原型级非空判定，会放行）。与框架先行 §6"生产配置缺真实依赖时**拒绝启动或拒绝
相应功能**，不能悄悄退回 fake"不符——原类注释"生产缺真实依赖时由服务端拒绝"的声明
在 R2 前并不成立。

**修正**：`ProcedureMatch.RunAsync` 增加与 F9 同款门禁的**第二道防线**——
`#if UNITY_EDITOR || DEVELOPMENT_BUILD || LITEFRAMEWORK_DEBUG` 之外（正式构建）直接
`Fail` + `Request(ProcedureId.Error)` 进确定错误态，拒绝该功能而非悄悄退回 fake。
测试身份常量保留在开发门内使用（R2 配置化 / G3 Join Ticket 接入时移出生产程序集——
当前正式包 IL 中仍含该死数据，行为面已封死）。

**验证（审查修复批同轮实测）**：L1 **713/713**；Unity 编译零错误 + EditMode **162/162**
（编辑器走开发分支）；**release 分支（#else）编辑器不编译，经 Windows x64 IL2CPP Player
构建编译验证 PASSED**（01:20 完成）+ 启动冒烟 PASSED（`[Asset] ready` 真标记，process alive）。
另随批修正 `ConfigSnapshotService` 类注释口径（"Interlocked"→"lock 互斥"，与实现一致）。
