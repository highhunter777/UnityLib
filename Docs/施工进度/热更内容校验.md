# 热更内容校验：施工进度

> 依据：[《热更与内容发布专项设计》](../design/client/content/热更与内容发布专项设计.md) §5（原始字节摘要）/§7（下载、候选校验与容量）/§12（错误类别与动作）；
> [《框架先行建设与业务接入专项设计》](../design/architecture/框架先行建设与业务接入专项设计.md) §4"资源、配置与热更"行、§6 样例 1 必测失败项；
> [《商业级通用客户端框架总设计》](../design/architecture/商业级通用客户端框架总设计.md) §8。
> 本文件只记录施工状态与证据；目标与验收以设计为准，不在此重复定义。

## 批次规划

| 批 | 范围 | 状态 |
|---|---|---|
| H3-a 候选校验内核 | 候选文件 IO 端口、空间预检、失败分类、逐文件摘要复算与双向差异、下载计划与多源轮转/有界重试 | **已完成**（2026-09-25） |
| H3-c 补丁编排 | `PatchCoordinator` 状态机（恢复→依赖→预检→获取→校验→待激活→激活→健康→确认），接 `ActivationTransactionStore` | **已完成**（2026-09-25） |
| H3-e 健康确认（聚合面） | `IHealthProbe` 逐项探针 + `CompositeHealthCheck` 聚合（一次报全部、不短路、异常按不健康） | **已完成**（2026-09-25） |
| H3-d Lua 候选（静态面） | `CandidateScriptSet`：不可变脚本集合、模块名/路径校验、依赖闭包、环检测、ScriptDigest | **已完成**（2026-09-25） |
| H3-b 真实下载 | `HttpCandidateFetcher`（UnityWebRequest 全链路）+ 临时文件 Release 归属 + 失败分类 + 11 例本地真实 HTTP 测试 | **已完成**（2026-09-25） |
| H3-e2 健康探针实现 | 候选配置 / 候选入口文件 / 入口资源可加载 / Lua 脚本 / Bridge 能力 | **已完成**（2026-09-25） |
| H3-f 运行时适配 | FileSys 字节/递归 API、`FileSysCandidateFileSource`、`LocalDirectoryCandidateFetcher`、`UnavailableDiskSpaceProbe` | **已完成**（2026-09-25） |
| H3-g 接线 | `PatchRunner` 信任门 + `ProcedurePatch` 接编排 + `GameModules` 装配；`DriveInfoSpaceProbe`、`SignedManifestEnvelope`、`FileSystemCandidateProvider`、`ContentActivator`、`YooAssetContentService : IGenerationSink` | **已完成**（2026-09-25） |
| H3-d2 Lua 动态验证 | `CandidateLuaValidator` 受控沙箱（独立 `LuaEnv` + fenv 白名单，回收 `CS`/`io`/`os`/`debug`/越集 `require`）+ `LuaScriptsHealthProbe`（接 `IHealthProbe`）+ Bridge 能力探针 | **已完成**（2026-09-25） |
| H3-h 反回退基线 | `ActivationRecord.ConfirmedRevision` + `Confirm(..., revision)` + `PatchRunner` 信任门改用已确认修订 | **已完成**（2026-09-25） |
| H3-i 真实下载/公钥/探针 | `HttpCandidateFetcher`、`TrustedKeyStore`、`ContentHealthProbes`、`BridgeCapabilityHealthProbe`、`WriteProbeDiskSpaceProbe` | **已完成**（2026-09-25，L2 161/161） |
| H3-j IL2CPP 验证 | Player 启动自检验证 RSA 在 IL2CPP 后端可用 | **已完成**（2026-09-25，§6 缺口关闭） |
| 审查修复（§8 表行 1 收口） | 候选事务先于下载落盘（`BeginCandidate` 时机）+ 临时文件回收端口（失败收尾/启动恢复两处） | **已完成**（2026-09-25，见下） |

## 未交付待办（登记，非本批范围）

本表逐项收敛。**已完成项保留划除痕迹以留审计线**；剩余项均需真机矩阵或真实信任根：

| # | 待办 | 阻塞原因 |
|---|---|---|
| ~~1~~ | ~~接线 `PatchCoordinator` 到 `ProcedurePatch`~~ | **已完成（H3-g）** |
| ~~4~~ | ~~Lua 候选动态验证（H3-d2）~~ | **已完成（H3-d2，2026-09-25）** |
| ~~9~~ | ~~反回退基线（已确认发布修订）~~ | **已完成（H3-h，2026-09-25）** |
| ~~10~~ | ~~`ActivationRecordIntegrity` 的 `"\x00null"` 占位符~~ | **已核查（2026-09-25）**：自 `bd75616` 即如此、非损坏、与空格形式能力等价 → **不改**（改动会使既有记录失效）；已补注释与回归钉 |
| ~~2~~ | ~~真实下载适配~~ | **已完成**（`HttpCandidateFetcher` + 11 例本地真实 HTTP 全链路；实测抓到 2 个替身测不出的缺陷） |
| ~~11~~ | ~~受信公钥分发~~ | **已完成**（`TrustedKeyStore`：内置锚点 + 撤销清单；未登记/已撤销 → 候选被拒） |
| ~~13~~ | ~~其余健康探针~~ | **已完成**（候选配置 / 候选入口文件 / 入口资源可加载 / Bridge 能力） |
| ~~9~~ | ~~IL2CPP 密码学验证~~ | **已完成**（Player 自检：RSA-OK backend=IL2CPP，含篡改反例） |
| 3 | 移动端磁盘余量 | 桌面由 `DriveInfoSpaceProbe` 覆盖，通用 `WriteProbeDiskSpaceProbe` 可作回退；**Android IL2CPP 密码学后端未验证**（无设备/工具链），与真机矩阵同批 |
| ~~12~~ | ~~端到端装配~~ | **已接通（2026-09-25 端到端装配批）**：`GameModules` 装配改为 `TrustedKeyStore.AsResolver()`（按清单 KeyId 每候选解析验签器）+ `CompositeHealthCheck` 接入候选配置/Lua 脚本探针（发布约定 `config/`、`lua/` 前缀，经 `ReleaseLayout` 单源）。**当前零内置锚点 → 候选仍一律被拒**（fail-closed 保持）——但装配面已完整：发布流程生成密钥对 → 公钥条目加入 `ContentTrustAnchors` 内置锚表 → 链路即真正接受候选。CDN 通道（`HttpCandidateFetcher`）随部署配置接入 |

**当前装配的能力边界（如实标注）**：`GameModules` 已把编排、信任锚解析、探针族接入主链——
但内置锚表为空（发布流程未生成密钥对）→ 任何候选按 UnknownOrRevokedKey 拒。
**装配面已完整：锚点 provisioning（生成密钥对 → 公钥入 `ContentTrustAnchors` → 重新编译）后链路即真正接受候选。**
**不得据此宣称热更已可用**——真锚点/真 CDN/真候选内容均不存在。

## 施工记录

### 2026-09-25 · 端到端装配批（#12 收口——装配面完整，候选仍 fail-closed）

**背景**（框架先行落地审查发现的收口项）：#12"端到端装配"的缺口是 `GameModules` 装配
`verifier: null` + 空健康聚合——件已全交付但装配面断开。本批把**信任锚解析、探针族、
验签器按 KeyId 每候选解析**三面接通，装配面从"断开"变为"完整但当前零锚点"。

**① 信任锚**（`ContentTrustAnchors.cs` 新）：编译期内置锚表（`IEnumerable<Anchor>`——
当前**零条目**）。根信任编入应用本体（随应用签名分发），**不放可写存储**——否则攻击者
加入自己的公钥 + 自签候选即绕过整条验签链。锚点 provisioning = 发布流程生成密钥对 →
公钥条目加入锚表 → 重新编译。

**② 验签器按 KeyId 每候选解析**（`PatchRunner`）：原 `ISignatureVerifier verifier` 单验签器
改为 `Func<string, ISignatureVerifier> verifierResolver`——按 `offer.Manifest.KeyId` 从
受信公钥库解析（§6"签名公钥标识（对应 TrustedKeyRing）"）；未登记/已撤销 → null →
校验器按 UnknownOrRevokedKey 拒（fail-closed 语义不变）。

**③ 探针族接入编排健康确认**（`GameModules` Content 模块）：
`CompositeHealthCheck` 从空聚合改为接入候选配置（`config/` 前缀）与候选 Lua（`lua/` 前缀 →
模块名 → `CandidateLuaValidator` 受控沙箱）两个探针——发布约定经 `ReleaseLayout` 单源。
入口资源可加载性探针（`AssetsHealthProbe`）查当前代次入口，须在资源包初始化后才有意义
（Patch 先于初始化，时序不可能）——归 Preload 后接缝（待办总览 §2.1 边界登记）。

**④ `ReleaseLayout`**（`Shell/Resource/`，新）：发布布局约定单源——`config/` 前缀 = 配置、
`lua/` 前缀 = 热更脚本、模块名派生（剥 `lua/` 与 `.lua`、`'/'` 保留与 LuaPreloader 同规则）。
探针装配与发布流水线共用。

**测试**：EditMode +7——`ReleaseLayoutEditModeTests` 5 例（config 挑选、Lua 模块派生与
LuaPreloader 同规则、空清单、零锚点 ApplyTo 返回 0、登记/撤销 Resolve 语义）+
`UiNavModalEditModeTests` 2 例修复回归。

**验证（本批实测）**：L1 **778/778**（LiteFramework 496 + LiteNet 160 + LiteSim 97 + LiteTesting 7 + Meta 18）；L2 门禁通过（12070 meta、编译零错误、EditMode **222/222** + PlayMode 12/12）。

**边界登记**：当前零内置锚点 → 候选仍一律被拒（fail-closed 保持——装配面完整但"接受候选"
需发布流程 provisioning 真实密钥对）。CDN 通道（`HttpCandidateFetcher`）随部署配置接入。

### 2026-09-25 · 候选校验内核交付（H3-a）

**背景**：热更批(一)(二)交付的发布身份/签名校验/激活事务在**生产代码中零消费者**
（全仓引用只有定义文件与测试）；设计 §7 的"下载/校验"执行侧一行未落。
本批补上其中**可纯规则交付**的部分——填平"信任侧已备好、执行侧为零"的缺口。

**① 候选文件与空间端口**（`Assets/LiteFramework/Scripts/Core/Content/CandidateFileSource.cs`）

- `ICandidateFile` / `ICandidateFileSource`：候选文件读句柄与访问端口。只暴露长度与完整字节——
  设计 §5 要求摘要对**原始字节**计算完整 SHA-256，`ContentHash.Hasher` 需整块输入；
  逐文件整体读入有界（`ReleaseBudget.MaxFileBytes` 默认 512MB）。**流式分块属大包优化，
  在有真实包规模数据前不预先引入**（§15"没有目标包规模与设备数据时不虚构"）。
- `IDiskSpaceProbe`：磁盘余量端口。**单独成端口**而非并入文件端口——平台可能能读文件却拿不到配额，
  且预检失败必须有明确结果而不是静默跳过。
- `SpacePrecheck` / `SpaceCheckRequest` / `SpaceCheckResult`：§7"计入候选、临时/解压峰值、保留版本及余量"。
  **溢出安全**：各分项 long 累加用显式判定，溢出即判"不足"——回绕会让"空间不够"变成"空间充足"。
  可用空间不可知（-1）同样按**不足**拒绝。

**② 失败分类**（`Assets/LiteFramework/Scripts/Core/Content/DownloadFailure.cs`）

`DownloadFailureKind` 按 §12 错误类别表分类，并带 `IsTransient` 判定：
暂态（网络超时/源不可用）可重试换源；确定性（缺失/长度不符/摘要不符/读取错误/空间不足/
清单外文件/重复文件）**重试无效**——§12"不通过反复重试或忽略验证绕过"。
分类决定动作，不是一个笼统的"失败"。

**③ 候选内容校验**（`Assets/LiteFramework/Scripts/Core/Content/CandidateContentVerifier.cs`）

**本批核心**：`ReleaseManifestValidator` 只校验描述**字段形态**（路径合法、长度非负、摘要形状），
**从不读文件字节**；本类把清单与**落盘内容**对上：

- 逐文件**复算摘要**（§7"文件通过完整性校验后才进入资源/Lua 解析与执行"）；
- 长度先于摘要：廉价且能立刻拒掉截断/超长，避免为大文件白算摘要；
- 端口自报长度与实读不符时以实读为准再核一次；
- **双向差异**的"多"一侧：清单外杂散文件检出（防多源拼接/残留污染）；
- 大小写按 `OrdinalIgnoreCase` 比较——Windows 文件系统不区分大小写，
  清单声明的 `a.lua` 与落盘的 `A.lua` 在真实平台无法区分，必须同一口径；
- `TryFindDuplicatePath` 补上 `ReleaseManifestValidator` 精确重复检查之外的**平台归一化口径**；
- 批量复用单个 `ContentHash.Hasher`（该类注释的设计意图"对成百上千个候选文件循环校验"，
  此前生产零调用）。

**④ 下载计划**（`Assets/LiteFramework/Scripts/Core/Content/DownloadPlan.cs`）

§7"YooAsset 负责下载实现，项目负责可信发布描述、事务代次与激活决策"的**决策侧**——
纯决策不做 IO，与"下载怎么做"完全分离，使 L1 能覆盖全部分支：

- 来源按优先级稳定排序（同优先级保持声明顺序，避免平台排序不稳定导致行为漂移）；
- **每次重试轮转到下一个来源**，优先级只决定首次用哪个源；
- 有界重试（`MaxAttemptsPerFile`）、指数退避**只计算不等待**（Sleep 属 IO 层，Core 不引入等待语义）；
- `AllDownloaded` 按清单逐项核对（大小写不敏感）。

**设计取舍**：初版设了 `MaxSourceSwitches` 与 `MaxAttemptsPerFile` 两个旋钮，实测发现二者职责重叠——
源数 ≤ 尝试上限时轮转自然覆盖全部源。已删除前者（无独立消费者，§22 精神）。


### 2026-09-25 · 补丁编排、健康聚合、Lua 静态身份交付（H3-c / H3-e 聚合面 / H3-d 静态面）

**⑤ 补丁编排**（`Assets/LiteFramework/Scripts/Core/Content/PatchCoordinator.cs` + `ContentOrchestrationPorts.cs`）

**这是热更链的第一个生产消费者**——把此前零散且无调用方的 `ReleaseManifestValidator`（描述层）、
`DownloadPlan`（获取决策）、`CandidateContentVerifier`（内容执行侧）、`ActivationTransactionStore`
（持久事务）**串成一条可测编排**。

顺序（§8 + §4 mermaid，每步持久化以便中断恢复）：
启动恢复 → **依赖检查** → 空间预检 → 获取 → 校验 → 标记待激活 → 激活 → 健康 → 确认。

- **依赖检查**（§6"本发布生效前必须已确认的版本"）：未确认则拒绝候选。判据为"已确认**或**本次在途"。
- **中止点顺序**：`MarkPendingActivation` 持久化**早于**激活——顺序颠倒会让进程在激活后中断时无从恢复（§8 表行 2）。
- **失败一律保留允许版本**（§8"从允许的 Confirmed 版本重建，不使用半成品"）：留档失败 → 重建已确认代次 →
  代次提示回已确认。**留档先于重建**，即便重建抛错失败原因已持久化。
- 未进入激活链路时**不调用**重建——已确认内容本就在运行，重建是多余的清缓存。
- 端口分离：`ICandidateFetcher`（网络 IO 特性）独立于 `ICandidateFileSource`——
  R11 纯化纪律禁止核心逻辑引用 HTTP/传输，端口分离让编排保持纯逻辑可 L1 覆盖。
- `IGenerationSink` 补上设计 §9 的缺口：`YooAssetContentService._generation` 此前无 setter、
  代次从未切换，健康确认后必须推进否则新内容永远不被加载。

**⑥ 健康确认聚合**（`Assets/LiteFramework/Scripts/Core/Content/CompositeHealthCheck.cs`）

§8"健康确认至少覆盖候选 ConfigSnapshot、Lua/main、全部必需注册表、关键 UI/入口及其资源"。
**一项一个探针**（合并成"全都检查"会让失败无法定位到具体项）；聚合**不短路**——
一次报告全部不健康项（与 `MetaConfig.Validate` 同款纪律）；探针抛异常按**不健康**处理，
不中断链路也不被当作健康。**无探针 = 判为不健康**（未确认任何覆盖项就声称健康，等于放弃 §8 要求）。

**⑦ Lua 候选静态身份**（`Assets/LiteFramework/Scripts/Core/Content/CandidateScriptSet.cs`）

§10"从固定文件清单构建不可变脚本集合"中**可静态做**的部分：模块名/路径校验（与
`ReleaseManifestValidator` 同口径）、**依赖必须在同批提供**（§7"同步 require 的依赖必须完整预载"）、
环检测（迭代实现，深链不爆栈）、`ScriptDigest` 规范化摘要（与输入顺序无关）。
真实语法/执行/Bridge 能力检查需 Lua VM，属 H3-d2。

**设计取舍**：初版 `RunAsync` 接受 `budget`/`resolveRelease` 参数，实现时发现会引出
不存在的 `ReleaseBudget.Validate/Extend` 与 `ReleaseManifest.Sources`——改为由调用方注入
`DownloadPlan` 与预算（来源属平台配置，不属于发布描述）。

### 2026-09-25 · 运行时适配与 FileSys 字节通道（H3-f）

**⑧ FileSys 扩展**（`Assets/LiteFramework/Scripts/Core/IO/FileSys.cs`）

候选文件**含二进制**（资源/字节码），而 `FileSys` 此前只有文本/JSON API，且 `GetFiles` **非递归**。
LiteGame 层**零 `System.IO`**（沿用项目纪律：IO 唯一入口是 FileSys），故在此补足而非绕开：

- `WriteAllBytes`/`ReadAllBytes`/`GetFileLength`（不存在返回 -1，区别于 0 字节空文件）；
- `GetFilesRecursive`（候选根全量清点——双向差异必须看到全部层级；
  与既有非递归 `GetFiles` 并存，后者刻意不带通配符以免跨平台大小写差异）；
- `DeleteDirectory`（幂等；**越界保护**：复用 `Combine` 的路径校验，拒绝空/绝对/`..`）；
- `DirectoryExists`。

`WriteAllBytes` 复用既有 `CommitAtomic`（临时文件 + 元数据级原子提交），与文本路径同款纪律。

**⑨ 运行时适配**（`Assets/LiteGame/Runtime/Shell/Resource/ContentRuntimeAdapters.cs`）

- `FileSysCandidateFileSource`：`ICandidateFileSource` 的 FileSys 实现。
  **路径契约**：`Open` 收**相对候选根的清单路径**，根路径只在装配层出现一次——
  使 `CandidateContentVerifier` 保持"零根路径概念"的纯规则。
- `LocalDirectoryCandidateFetcher`：清点**已落盘的**候选文件——覆盖本地/内置候选与
  已由其它通道下载完成的候选。**不发起任何网络请求**（真实 CDN 下载属待办 2，不伪造）。
  清单外文件一并返回，**不替校验器下结论**。
- `UnavailableDiskSpaceProbe`：**.NET Standard 无跨平台"目录可用空间" API**，返回 -1 = 不可知；
  `SpacePrecheck` 对不可知按**不足**处理。

### 2026-09-25 · Lua 动态验证、反回退基线、占位符核查（H3-d2 / H3-h / #10）

**⑩ Lua 候选动态验证**（`Assets/LiteGame/Runtime/Shell/Lua/CandidateLuaValidator.cs`、
`LuaScriptsHealthProbe.cs`）

此前 H3-d 只交付静态面。本轮查证推翻了两条先前的误判：
① LiteGame 已可经 **L2 EditMode** 验证（此前以为 L1 引用不了 LiteGame 就无法验证）；
② xLua 支持隔离 env 加载（`LoadString<T>(bytes, name, env)` 走 `lua_setfenv`）。
故 §10 的"受控验证环境"**可做且可验证**。

§10 硬要求"验证环境不得绑定正式全局 Bridge 注册表，不得打开 UI、订阅正式事件、
发业务网络请求或写存档"的落点：

- **独立 `LuaEnv` 实例**——与运行时 `LuaComponent` 的 env 完全隔离，候选脚本在任何路径上都拿不到正式 Bridge 表；
- **独立 fenv 沙箱**——每段脚本用新建沙箱表作全局环境，**白名单**放行（未列入者不可见），
  回收 `CS`（C# 类型直通/反射）、`io`/`os`/`debug`/`package`/`loadlib`/`dofile`/`loadfile`/`load`
  （对应 §10"还需收口 CS/反射、文件/网络/系统库、动态加载与调试入口"）；
- **受限 `require`**——只解析本批候选集合内已装载的模块，越集即拒（§7"依赖必须完整预载"）；
- **不执行 main**，不产生业务副作用。

**能力边界（如实标注）**：沙箱回收是**能力收口**而非形式化安全边界——未做 `debug` 库加固，
不声称可抵御恶意构造的 Lua 字节码；§10 要求的"错误熔断、内存与执行预算"中，
本类只做**逐脚本**报告，**不含指令计数/超时中断**（需 Lua VM 的 hook 接缝）。

**⑪ 反回退基线**（§6）

`ActivationRecord` 增 `ConfirmedRevision`（发布修订）——与 `ConfirmedVersion`（同 Release 内代次计数）
**语义不同**；`PatchRunner` 信任门此前传 `confirmedRevision: 0`（不启用反回退，未伪造），
现改用 `store.Current.ConfirmedRevision`。

**刻意不并入完整性摘要**：并入会改既有记录摘要 → 全部失效（用户丢已确认版本）。
代价是该字段**不受完整性保护**（篡改不检出）——故它只提高拒绝门槛、**不是安全边界**；
重放防护由签名与发布修订单调性共同承担。待因其它原因升 `SchemaVersion` 时并入。
字段新增本身向后兼容（Newtonsoft 缺失字段保留默认 0），无需迁移。

**⑫ 空值占位符核查（#10）**

`ActivationRecordIntegrity.Compute` 的 4 处空值占位是 `"\0null"`（NUL + null），
而非类注释暗示的 `" null"`。**git 追溯结论**：自 `bd75616` 起即如此，
**非后续损坏**；且与空格形式在"区分 null 与空串"上**能力等价**（`??` 对 null 生效，空串走另一分支）。

**结论：不改**——改动会改摘要 → 既有记录全部失效。已补类注释 + 回归测试钉住行为，
防止将来"顺手修正"。

**未做**：Lua **Bridge 能力检查**（§10"新 Lua 引用旧包不存在的能力必须在执行前拒绝"）需
`BridgeApiVersion`/能力集合的事实源，与受信公钥分发同属"发布身份分发"批次。

### 2026-09-25 · IL2CPP 密码学验证（#9 —— §6"首次远程激活前"缺口关闭）

**背景**：`SignatureVerifier` 的算法选型（**RSA-2048 + PKCS#1 v1.5 + SHA-256**）结论来自
**Mono** 实测（`ECDsa` 抛 `NotImplementedException`、Ed25519 无类型）。但 **IL2CPP 是另一套 BCL 后端**，
Mono 的结论不能跨后端外推——§6 明确要求"首次远程激活前"补此项。

**做法**：临时挂一个 Player 启动自检（`Il2CppCryptoProbe`，`#if !UNITY_EDITOR`），
本机闭环——自生成密钥 → 导出 `RSAParameters` → 经**本项目验签器同一代码路径**
（`RsaSignatureVerifier`）验证 → 附带篡改反例 + SHA-256 已知向量。不依赖外部密钥或网络。

**证据**（Windows x64 IL2CPP Player，构建产物 `GameAssembly.dll` 68MB 确认非 Mono）：

```
[CryptoProbe] RSA-OK backend=IL2CPP sha256=OK sign/verify/tamper-reject 全部通过
```

冒烟同步通过：`[Asset] ready`（`scripts/player-smoke.ps1`）。

**结论**：RSA-2048 + PKCS#1 v1.5 + SHA-256 在 IL2CPP 后端可用，
且"篡改被拒"一并验证（只验正例无法排除"永远返回 true"）。

**收尾**：自检**已从生产代码移除**（它是构建期一次性验证，不是运行时逻辑；
结论以本记录为准）。若需复验，按上述做法重建即可（探针源码见本记录描述，约 60 行）。

**未覆盖**：Android IL2CPP（另一套 NDK/BoringSSL 后端）——本机仅有
`BuildProfiles/android-il2cpp.json` 配置而无设备/工具链验证条件，属 G4 真机矩阵（待办 3 同类）。

### 2026-09-25 · 审查修复：候选事务先于下载落盘 + 临时文件回收（§8 表行 1 收口）

**背景**（框架先行落地审查发现的两处偏差）：

- `PatchCoordinator` 在 Fetching/Verifying **之后**才 `BeginCandidate`（且与 `MarkPendingActivation`
  背靠背调用）——§8 表行 1"下载或候选校验中 → 清理/恢复属于该候选的临时文件"的 **Candidate
  恢复分支在生产路径不可达**：下载/校验中进程被杀时磁盘上无在途记录，临时文件无归属可查、
  无人回收。`ActivationTransactionStore` 自注释也自相矛盾：`RecoverOnStartup` 称
  "Candidate（下载/校验中）"、枚举注释却称"下载/校验完成"。
- 失败/取消/超长路径不清理 `.tmp/<releaseId>`——垃圾在隔离目录内累积，可能顶爆下次空间预检。

**修正**：

- `PatchCoordinator.RunAsync`：`BeginCandidate` 提前到 **Fetching 之前**（空间预检通过后），
  Candidate 状态覆盖整个获取+校验区间；原⑤处只保留 `MarkPendingActivation` 推进。预检/依赖
  阶段失败不开事务（无临时归属可清，测试钉死）。
- `ICandidateFetcher` 增补 `CleanupTempAsync(releaseId)` 端口（**幂等、不抛**——清理是恢复的
  次要目标，不得阻断"以 Confirmed 继续"的主流程；Core 仍零 IO）。`PatchCoordinator` 在
  **启动恢复**（按恢复前记录的在途身份，NoWork 路径同样执行）与**失败收尾**（`FailAsync`
  留档之后）两处调用。
- `HttpCandidateFetcher` 实现整目录回收（`FileSys.DeleteDirectory(<候选根>/.tmp/<releaseId>)`）；
  `LocalDirectoryCandidateFetcher` 不产生临时文件，按端口契约 no-op。
- `ActivationTransactionStore` 两处注释口径修正（枚举与 `BeginCandidate` 改为"下载/校验中/下载
  开始前"）。该文件含 4 个真实 NUL 字节（`"\0null"` 占位符——批八已登记**不得改动**），文件
  工具按二进制拒绝编辑，本批经**字节级替换**完成，NUL 数量保持 4 个不变。

**测试**：L1 `PatchCoordinatorTests` +2——`获取失败_候选事务先于下载落盘_临时文件随即回收`
（用 `FakeFetcher.BeforeFetch` 钩子证明**下载开始前**记录已在盘，而非仅失败后可见）、
`无候选_启动恢复仍回收上次在途候选的临时文件`；扩展 2 例断言（空间不足不开候选事务、
中断恢复清理在途临时归属）。EditMode `HttpCandidateFetcherEditModeTests` +1——
`临时目录回收_按发布隔离_幂等不抛`（只回收目标 Release、不误伤其它、重复调用/空身份 no-op）。

**边界登记**：断点续传（Range 偏移续取）仍未实现——失败仍从 0 重下；本批的"失败即回收"
与未来续传不冲突（续传落地时再裁决暂态失败是否保留半截）。

## 测试面

- `Tests/LiteFramework.Core.Tests/Content/FakeCandidateFileSource.cs`：内存候选文件端口与磁盘余量假件（L1 用，Core 不依赖 System.IO）。
- `Tests/LiteFramework.Core.Tests/Content/CandidateContentVerifierTests.cs`（21 例）：
  正确内容通过、**文件缺失**、**长度不符**（且验证长度先于摘要）、**内容篡改摘要复算检出**、
  读取失败、**清单外文件**、不支持目录枚举时仍复算摘要且只打开清单内文件、取消在循环边界生效、
  空清单/空端口确定性拒绝、路径大小写与反斜杠歧义检出重复；
  空间预检：充足/正好相等/差一字节/可用空间不可知/累加溢出/负分项/空请求。
- `Tests/LiteFramework.Core.Tests/Content/DownloadPlanTests.cs`（11 例）：
  首试选最高优先级源、暂态失败换源、**多源轮转每个源都被轮到**、**确定性失败不再给条目**、
  达尝试上限拒绝、清单外路径拒绝、路径大小写匹配、无可用来源/空清单构造即拒绝、
  `AllDownloaded` 逐项核对、总量与退避计算、来源优先级稳定排序、失败分类暂态判定。

覆盖《框架先行》§6 样例 1 必测失败项中的：**损坏/缺文件**、**下载中断**（取消）。

## 验证证据


| 项 | 命令 | 结果 |
|---|---|---|
| L1 | `powershell -NoProfile -File scripts/test.ps1 -Lane L1 -Profile PullRequest` | **778 通过 / 0 失败**（端到端装配批后实测：LiteFramework 496 + LiteNet 160 + LiteSim 97 + LiteTesting 7 + Meta 18） |
| L2 | `powershell -NoProfile -File scripts/l2-unity-gate.ps1` | **通过**——12070 个 .meta GUID 全合法；Unity 编译零错误；EditMode **222/222** + PlayMode **12/12**（端到端装配批 +7：ReleaseLayout 3 + 信任锚 2 + 导航/模态 2 修复回归） |

## 已知边界

- **热更链已接入主链但不会接受任何候选**（当前零内置锚点）：`GameModules` 已装配 `PatchRunner` →
  `PatchCoordinator`，编排在 `ProcedurePatch` 真正生效；信任锚经 `TrustedKeyStore.AsResolver()`
  按清单 KeyId 每候选解析、健康探针族（候选配置 + 候选 Lua）已接入编排——**装配面已完整**，
  但 `ContentTrustAnchors` 内置锚表为空 → 任何候选按 UnknownOrRevokedKey 拒。
  **锚点 provisioning 后链路即真正接受候选（生成密钥对 → 公钥入锚表 → 重新编译）。**
- **未交付项**：移动端磁盘余量（Android IL2CPP 后端同批）。
- **IL2CPP 密码学已实测（Windows x64）**：Player 启动自检 `[CryptoProbe] RSA-OK backend=IL2CPP`
  （含篡改反例与 SHA-256 已知向量），自检已从生产代码移除；**Android IL2CPP 未覆盖**
  （另一套 NDK/BoringSSL 后端，无设备/工具链），与真机矩阵同批。
- **Lua 沙箱是能力收口而非形式化安全边界**：未做 `debug` 库加固，不声称可抵御恶意构造的
  Lua 字节码；§10 要求的指令计数/超时中断未实现（需 Lua VM hook 接缝）。
  Bridge 能力检查已交付（`BridgeCapabilitySet` 显式声明 + `BridgeCapabilityHealthProbe` 执行前拒绝）。
- **`ConfirmedRevision` 未并入完整性摘要**：它只提高拒绝门槛、不是安全边界
  （篡改不检出）；待因其它原因升 `SchemaVersion` 时并入。
- **断点续传（Range 偏移续取）未实现**：`FileSys.AppendAllBytes` 已备，但
  `HttpCandidateFetcher` 失败仍从 0 重下；临时文件已随**失败收尾与启动恢复**两处回收。
  "原子写盘/杀进程中断矩阵"（§8）属 G4 真机。
