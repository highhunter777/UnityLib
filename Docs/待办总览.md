# 待办总览（2026-09-18 全量对账汇总，2026-09-19 批③ 后刷新）

> 用途：把散在各文档里的「未做项」收在一处，便于排期与交接。
> **本文件只做索引**——判据与施工细节仍以「出处」列的文档为准，别在此重复讨论。
> 来源：对 `Docs/` 全部文档与代码实际状态逐份核对（同日已把过期处就地标注，见各文档内的 ⚠️/对账 段落）。

## 0. 当前状态（基线）

- **L1 = 357 绿**：LiteFramework 203 / LiteSim 75 / LiteNet 48，0 失败（2026-09-19 M10 批④ 后实测）；**CI 双平台矩阵已转绿**（`87d9ab8`：ubuntu 43s / windows 1m36s 全过——ubuntu 侧正是抓出 RoomBroadcastTests 4 条失败的一侧）。
- **L2 = 通过**：meta 扫描（10914）+ Unity 编译状态 + **EditMode 用例 4/4 全绿**（IEEE 基线逐位对账 / 运算链逐位一致 / UI 模板完整性 / 控件模板自检）。
- **工作区干净**；**M10 四批全部完成**，批④ 交付见《M10实施指导》附实施记录。
- **玩法数值已表化**（`tb_combat_num`：Luban 表源 → 客户端 bin + 服务端 json；改表免改代码；表数据进 buildHash 闭包）；
- **M11 已出施工图**：《M11实施指导》（19 项前置决策 / 批次 P+C1–C5 / 预算 ~4300 行 / 6–8 天）+《VFX服务实施指导》（~800 行 / 1 天）——**前置已清零可开工**（M10 遗留：150ms 端到端、带宽实测、流程三阶段已并入 M11 批次）。
- 联机线顺序：M9 ✅ → **M10 批①②③④ ✅（M10 完成）** → **M11（表现层 + 可玩 demo）就绪可开工** → M12；M5（纵向切片）/ M6（热更闭环）按定案**后置**。

## 0.1 链路完备度快照（2026-09-18 实测核对）

> 判据：**"能走通"≠"能自助做"**。两链的短板方向相反——UI 缺工具与性能面，美术缺接入与自动化。

### UI 链：骨架完备、两端未闭合

| 环节 | 状态 | 实况 / 缺什么 |
|---|---|---|
| 控件库 | ✅ | 25 件模板 prefab + 确定性构建器 + 自检 31/31（编辑态+Play） |
| 绑定 | ✅ 双路径 | `BindRoot/BindNode` 标记 → `LiteCodeGen` 生成 partial → 回填；运行期 `BindIndexBuilder` 索引（Runtime/Editor 均已接线） |
| 界面壳 | ✅ | `UIForm` 七态 + `UIStack`/`UILayerGroup` + 池化复用 + 受控 API（`self.ui` 13 方法 + `Bridge.*`） |
| 动效/转场 | ⚠️ 一半 | `UiFx` + 转场**策略件**有；**编排层（跨帧队列/可等待完成/超时）未做** |
| 资源与热更 | ✅ 机制完备 | 收集组 + 目录归属 + 运行期增量重填；**真机分支仍 `throw`（M6）** |
| 上游制作工具 | ❌ | 编辑器四面板（标记向导/属性/校验/控件库）未实现，`Editor/BindGen/` 为空 → 标记靠手挂组件 |
| 下游性能数据面 | ❌ | 批①–④ 全未做：无 CanvasScaler、**零图集**、无分层 Canvas；热点未修；**无 `HudModel`/`HudTicker`/脏位** |
| API 覆盖/护栏/场景 | ⚠️ | 19 件控件无 Lua 口（G5–G21）+ 列表协议待设计；规范四项未进 CI；只 1 个界面且 `ProcedureMain` 空转 |

### 美术链：规范与素材齐、接入链断

| 环节 | 状态 | 实况 / 缺什么 |
|---|---|---|
| 规范 | ✅ | 《美术资源清单》§0 六条规格约束（Humanoid 优先 / 面数 / 俯视角拍摄 / 特效不携带判定 / 图集九宫格 / **命名即引用** `fx_ anim_ sfx_ ui_`）+ §7 交付约定 |
| 素材 | ✅ | 模型 143 / 动画（Erika 31 + Opsive 30）/ 特效 65 粒子 / 音效 424 / 中文 SDF 字体 / 地图 Map_v1·v2；`LiteGame/Art/*` 骨架已建 |
| 导入设置 | ❌ | **零 `AssetPostprocessor`**：488 未压缩贴图 / 6 Read-Write / 13 张 ≥4K / 1233 无 mipmap 全待处理 |
| 动画接入 | ❌ **断链** | 全工程仅 `Kazuko.controller`；**Erika 31 动画无 Controller**、Generic→Humanoid 未转、AvatarMask/Animator Layer 未建 → **角色现在动不起来** |
| 特效接入 | ❌ | **无 VFX 服务**：65 粒子 prefab 只能手挂/直调 ParticleSystem，无 `Play/Stop/attach/follow` + 同屏上限 + 观察层 |
| 动作数据 | ❌ | `tb_action`/`tb_action_num` **表源未建**；Timeline 轨+导出器+秒→帧校验未做 → 动作时序无单源 |
| 进包 | ❌ | 收集组只含 `Scenes/Config/Lua/UI`；**模型/动画/特效/音频无收集规则**（M6） |
| UI 美术 / 护栏 | ⚠️ | 字体✅、模板 25 件✅；**零图集**；命名/面数/整帧时长**只写在文档里**，无 lint/CI（对比代码侧有 `DisciplineScanner`） |

**结论**：
- **UI**：做 demo 够用（手工可走通），做"日常制作流水线"不够。优先级 = **批②（图集+分层，M11 真机前必须）> 批③（HudModel 直驱，越晚越贵）> 编辑器四件 > 批⑧ API**。
- **美术**：素材不再是瓶颈，瓶颈全在接入。优先级 = **① Erika Controller + Humanoid 转换（最硬，没它角色不播）② 导入设置规范/批处理 ③ 动作表 + Timeline 导出 ④ VFX 服务 ⑤ 美术资源收集组**。

## 1. 联机主线

| # | 待办 | 出处 | 前置 |
|---|---|---|---|
| ~~1~~ | ✅ **M10 批② 已交付（`eb8b9e2`）**：RoomServer 权威循环——`ServerHost/Session/SessionManager/Room/InputGate`（连接 cookie + 限速 + buildHash 校验 + 输入两层校验 + **E1 背压 / E3 抗放大**）+ Ops 指标；用例 `Tests/LiteNet.Tests/RoomServerTests.cs`（204 行，LiteNet 用例 16 → 21） | 《M10实施指导》§2.5/2.6 | — |
| ~~2~~ | ✅ **M10 批③ 已交付**：`SnapshotDiffer`（增量/全量兜底/ackInput，两段式）+ AOI 网格过滤（实例持有）+ 服务器回溯判定（`LagCompensator`，`LagCompHistory=16`）+ E1 三档降级 / E3 重连票据 + `Ops`/`ServerLoop`；LiteNet 用例 21 → **45**，L1 323 绿 | 同上 §2.7/2.8 + 附实施记录 | — |
| 3 | **M10 批④**：`RoomClient` + `RollbackSim.OnAuthoritativeSnapshot` + `KcpNetworkService` 桥 + `LoopbackDamageSim` + 无头双客户端对跑（§9 验收全组：5 分钟对跑 / 10% 丢包 / 带宽实测 / 和解率实测） | 同上 §2.9/2.10 | 批③ ✅ |
| 4 | **流程线补三阶段**：`ProcedureMatch/Battle/Result` + `ProcedureArgs` 的 `roomId/frameNo/BattleContext` 字段位 + "安全窗口"挂点 | 《UI扩展能力设计》§9.1/§9.4 | 建议**不晚于 M10 批④** |
| 5 | **M11 表现层与可玩 demo**（**已出施工图，可开工**）：`LiteSim.View` + `SimView`（镜像/远端插值/本地预测）+ 和解重对齐（`silenceUntilFrame` 静默门）+ 输入三件（`IIntentSource` 键鼠/触屏 + 重绑）+ `PlayerController`（**不做位移**，三个"门"）+ `ICameraService` + 地图装配（`MapExtractor`，视觉/判定分离）+ 角色动画（Erika→Humanoid）+ HUD 三件 + `KcpNetworkService` 桥与流程三阶段 | **《M11实施指导》**（19 项前置决策 + P/C1–C5 批次 + 预算 ~4300 行 / 6–8 天）；VFX 服务见**《VFX服务实施指导》**（~800 行 / 1 天） | M10 ✅（4 人房容量已参数化） |
| 6 | **M11 四批 C1–C4**：`IIntentSource`（键鼠/触屏）+ 键位重绑 → `PlayerController` → `ActionState`/`ActionSystem` + 两张动作表 → View Animator 参数推导 + 相机 | 《角色状态与动作实现设计》§9.6 | 同上；**C2 含 Aim 改造（已提前完成）** |
| 7 | **M12**：状态/快照面板 + 输入日志录制回放 + 断线重连 + 复查重放 + 对局归档管线 | 《状态同步实施方案》§9 M12 行 / §13 | M11 |
| 8 | **流程线 HSM 化落地**：`Main(Lobby/Match/Battle(Ongoing/Result))` 复合态 | 《层级状态机施工图》§5 | 与第 4 项同期 |

## 2. 框架 / 状态机

| # | 待办 | 出处 |
|---|---|---|
| 9 | 表→spec 加载器（Luban 表 → `StageSpec` 行，"表驱动"闭环最后一环） | 《通用流程状态机施工图》§10.4 |
| 10 | 调度三件：跨帧队列 / 可等待完成 / 超时兜底（转场 Layer 2） | 同上 + 《UI扩展能力设计》§1.5 |
| 11 | `Tags` 标签查询（"是否属于某类状态"） | 同上 |
| 12 | 机器组（统一 tick + 仲裁，正交轴正解） | 同上 |
| 13 | 迁移历史（最近 N 次，Debug 三宏下启用） | 同上 |
| 14 | 专用动作机 `ActionMachine`（帧数据/取消窗口/连段派生）——**后置**，先看表驱动够不够 | 《状态机ARPG形态扩展施工图》§7 |

## 3. UI 线

| # | 待办 | 出处 |
|---|---|---|
| 15 | **批① A1–A8** 热点修复（虚拟列表监听累积/池只增不毁/裁剪 SetActive、Countdown·HpBar·FlyText 每帧重建、Lua 回调分配与闭包） | 《UI性能优化规划》§3 |
| 16 | **批② B1–B6** 渲染面基础设施（CanvasScaler / 图集 / HUD 分层 Canvas / 动效子 Canvas / raycastTarget 规范）——**B2+B3 必须同批** | 同上 |
| 17 | **批③ C1–C2** 数据面（HUD 由 C# 直驱 + HudModel 脏位；绑定索引按 prefab 缓存） | 同上 §7 |
| 18 | **批④ D1–D4** 测量与护栏（ProfilerMarker / IModuleStats 扩展 / 基线记档 / 校验规则进 CI） | 同上 |
| 19 | **批⑧ 剩余受控 API（P1/P2）**：G5 切页 / G6 列表数据源协议（**待设计，首版建议方案 A**）/ G8 星级 / G9 滚动数值 / G11·G12 动图·头像 / G13 步进 / G14 输入三件 / G18 点击区 / G19 引导 / G20 动效口 | 《UI控件Lua用法表》§3/§4 |
| 20 | 界面级命名约定文档（实例化后子控件如何命名，避免重名） | 《UI控件库Prefab落地规划》§8.4 |
| 21 | UI 制作规范落地：TMP Sprite Asset、UI 特效层（Overlay 挡死）、🔴 硬规则进校验器 | 《UI制作规范》§7 |
| 22 | 真机 YooAsset 分支收口（`AssetService` 仍 `throw NotSupportedException`） | 《UI资源热更缺口收口》§6 |

## 4. 编辑器 / 内容工具

| # | 待办 | 出处 |
|---|---|---|
| 23 | UI 编辑器四件：标记工具 / 属性面板（Odin 只进 LiteGame.Editor）/ 校验面板 / 控件库面板（画布已定案用 Prefab Stage） | 《编辑器设计规划》§2 |
| 24 | Timeline 三件：自定义轨（判定/表现）+ 导出器（TimelineAsset→表）+ 导出校验（秒→帧硬规则）——**M11 demo 前必须就绪** | 同上 §3.1 |
| 25 | 技能编辑器一键导出（校验→写 CSV→调 Luban→Refresh→热生效；对局中禁止热重载） | 同上 §4 |
| 26 | 动作分类最终枚举（首版 `skill/dash/interact/death/respawn`，`hit` 待定）；VFX 服务（M11） | 《动作与特效设计》§4 |

## 5. 测试与 CI

| # | 待办 | 出处 |
|---|---|---|
| ~~27~~ | ✅ **L2 Unity EditMode 用例已交付**：`Assets/Tests/EditMode/`（asmdef `LiteGame.EditModeTests`，`UNITY_INCLUDE_TESTS` 门控）4 条用例 = IEEE 基线逐位对账 + 运算链记录 + UI 模板完整性 + 控件模板自检（与菜单同源 `WidgetPrefabCheck.RunAll`）；L2 脚本经 `unity command run_tests mode=EditMode` 直跑（编辑器无需关闭，`Total=0` 判失败）。装配冒烟（GameEntry→ProcedureMain）**明确留 M11** | 《测试开发方案》§7.1 |
| ~~28~~ | ✅ **Unity 侧 IEEE 对账已交付**：`LiteSim.Core.Editor/IeeeBaselineChecker`（菜单 + `RunCli`）；**探针下沉 `LiteSim.Core/IeeeProbe`**（两侧同一份样本/运算，杜绝漂移）。**实测发现**：116 条逐值行逐位一致 ✓，但 10k 步 **Chain 不一致**（Unity 3683559206 / .NET 2896875742）→ 见下方新增决策项 | 同上 §7.2 缺口 b |
| ~~29~~ | ✅ **R8/R9/R10 架构红线已进统一引擎**（R8 `Resources.Load` in LiteGame / R9 Mod 类型 in LiteSim / R10 `INetworkService` in Shell·UI）+ `ScanTargets` 目标根 + 5 条用例；全仓扫描 0 违规 | 同上 §7.6 |
| ~~30~~ | ✅ **基线防篡改（轻量版）已建**：CI 步检查 `Tests/**/Baselines/**` 变更，须在 PR 标题/正文或提交信息带 `[baseline]`，否则失败 | 同上 §7.5 缺口 i |
| ~~31~~ | ✅ **CI 跨平台 matrix 已开**：`windows-latest` + `ubuntu-latest`（`fail-fast: false`）。**下一次 CI 运行即为首次真验**——若暴露 Windows 假设，按平台修（已预核：临时目录 / 路径 Ordinal / BuildHash 行尾归一化均无平台依赖） | 同上 §7.5 缺口 j |
| 32 | ⚠️ **部分完成**：**超时保护**（job `timeout-minutes` + `--blame-hang-timeout 5m`）与 **CI 缓存**（`setup-dotnet cache`）已做；**未做**：分层 `Trait`、property-based、`Directory.Build.props` 三份收敛、覆盖率门槛 | 同上 §7.3/§7.6 |
| ~~33~~ | ✅ **跨运行时 `Chain` 底噪已定案 B 并落地**：真因**不是** BCL sqrt，而是 **Mono JIT 把 `a*b + c*d` 自动融合成 FMA**（.NET 8 不融合）→ 同源码差 1 ulp（实测：`x*x+y*y+z*z` 在 .NET=1075435771 / Unity=1075435770；拆局部变量**不能**阻止融合）。修法两刀：① `SimMath.Sqrt` 改自研 software sqrt（纯整数位运算 + 正确舍入，防御性消除 BCL 数学依赖）；② **融合安全算术件** `MulAdd2/MulAdd3/MulSub2/MulAddSub3/MulAdd/MulSub`（双精度累积 + 单次舍入，float×float 在 double 里精确 → 融合无害）——Sim 内 `a*b+c*d` 形态一律走它。结果：**Chain 两侧逐位一致**（.NET=Unity=3683559206 ✓）；基线重录（`[baseline]`）；Chain 从观测项**升为硬判据**（EditMode 用例直跑断言） | 《CI门禁（L1-L2）》2026-09-18 发现 · 《M7实施指导》附实施记录 |

## 6. 服务端 / 运维加固

| # | 待办 | 出处 |
|---|---|---|
| ~~33~~ | ✅ **E1 背压降级**（per-client 水位 → 抽帧 / 收缩 AOI / 裁最远实体，含滞回恢复）——**M10 批③ 已交付**（`ProtocolConstants` 数值定案 + 用例） | 《服务端架构设计》§10 |
| ~~34~~ | ✅ **E3 抗放大**（连接 cookie 内建 + per-IP 限速 + **重连一次性票据** `ReconnectService`）——**M10 批②③ 已交付** | 同上 |
| 35 | **E2 快照优先级调度**（按距离/变化/可见分档降频）——M10 之后调优 | 同上 |
| 36 | 进度服务器权威化（客户端自持进度 = 作弊洞；MVP 朋友局豁免，上线前必做） | 《状态同步实施方案》§10-12 / §13.3 |

## 7. 资源与工程清理

| # | 待办 | 出处 |
|---|---|---|
| 37 | Erika 31 个射击动画**无 AnimatorController**（现 5 个；Generic Rig → demo 用 Erika，Kazuko 留 locomotion） | 《联机Demo美术资源清单》§8.2 |
| 38 | 导入设置优化：488 未压缩贴图 / 6 Read-Write / 13 张 ≥4K / 1233 无 mipmap（**M6 前处理**） | 同上 |
| 39 | 待清理：`Plugins/UniRx`（未使用）、`XLua/Tutorial` 示例场景、`LiteGame/UI/_probe` 空目录；Luban 每次 regen 会删 `Luban.Tables.asmdef`（长期对策未做） | 同上 / 《UI资源热更缺口收口》§5 |

## 8. 预案（**不是待办**，勿当缺口排期）

- 帧同步：`《帧同步实施方案》` 已标"方向已抛弃"，仅在《状态同步实施方案》§12.2 判据命中时启动（需先做 M7′ 定点数重写）。
- 服务端拆分（Gate/Sim）、I/O 线程、匹配队列、Mongo/Redis/PG：触发条件未命中，均不做。

## 9. 已明确不做（判据留档，别重复讨论）

不做清单分散在各文档末章，此处只列高频易提的几条：Validate/注册表时机断言（已删）· 通用 UI 数据绑定层 · 自研 UI 合批 · LayoutGroup 排布控件/列表 · 并行状态/正交区域 · 黑板/字符串键字典 · 状态机线程安全与可视化状态图 · Sim 内使用框架状态机 · 服务端拒绝非法输入（改 clamp）· 变异测试 / L4 PlayMode 自动化。
