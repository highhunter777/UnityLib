# 测试开发方案（L1 纯 C# 单测 · L3 三进程联测 · 四层金字塔）

> 制订日期：2026-09-08
> 前置：《M0实施指导》（各件自测清单、M0SelfTest）、《状态同步实施方案》（M7–M12 验收）。
> 一句话定位：**三个零依赖程序集（LiteFramework.Core / LiteSim.Core / LiteNet）可以完全脱离 Unity 测试——`dotnet test` 秒级反馈是主回路，Unity 测试是慢回路，headless 三进程联测是联机验收的自动化形态。** 这是 `noEngineReferences` 设计的第四次兑现。
> 覆盖判定：哪些能测、用什么测、什么时候接进来；同样重要的是**不自动化清单**（§7）。

---

## 0. 测试金字塔（四层）

| 层 | 范围 | 框架 | 反馈 | 何时接 |
| --- | --- | --- | --- | --- |
| **L1 纯 C# 单测** | 三个零依赖程序集全部逻辑 + Server 组件 | xUnit + `dotnet test` | **秒级** | M0 收尾起 |
| **L2 Unity EditMode** | 依赖 `UNITY_EDITOR` 宏的构建差异行为 | Unity Test Framework（NUnit，manifest 已有 1.1.33） | 分钟级 | M0 收尾起 |
| **L3 联测** | headless 客户端 ×2 + Server 对跑 | xUnit 进程编排 | 分钟级 | M10 |
| **L4 PlayMode / 手动** | SimView 表现、Lua UI、手感调参 | PlayMode + 人 | — | M11 起 |

---

## 1. 前置工程（L1 之前必须完成的三件）

### 1.1 Debug 宏统一（最实际的一堵墙）

**现状**：Core 代码用 `#if UNITY_EDITOR || DEVELOPMENT_BUILD`（ReferencePool / EventCenter / Log 等）——在 .NET xUnit 下这些宏未定义，**走 release 分支，Debug 校验（重复归还检测、池溢出检测等）全部不可测**。

**修法**：所有条件编译改为三宏并集——

```csharp
#if UNITY_EDITOR || DEVELOPMENT_BUILD || LITEFRAMEWORK_DEBUG
```

- Unity 侧行为不变（前两个宏照常生效），无需全局加 symbol
- 测试 csproj 的 `DefineConstants` 显式定义 `LITEFRAMEWORK_DEBUG` → Debug 校验在测试里全量激活
- **新代码禁用裸 `UNITY_EDITOR` 判 Debug 行为**（进 lint 清单，见 §6.4）

> **状态（2026-09-15）**：已落地。Core 内共 **13 处**（EventCenter 1 / Log 1 / ObjectPool 4 / ReferencePool 7）全部改为三宏并集；`LiteFramework.Core.csproj` 已定义 `LITEFRAMEWORK_DEBUG`。

### 1.2 双轨编译（asmdef + csproj）

三个零依赖程序集要在 Unity 与 dotnet 两个世界编译。每个配一个 **SDK-style 手写 csproj**（`net8.0`，放在程序集目录内）：

- **关默认 glob、显式 include**：`EnableDefaultCompileItems=false` + `<Compile Include="Scripts/**/*.cs" Exclude="Scripts/Editor/**/*.cs" />`。默认 glob 会把 Unity 层 / Editor 源码一起扫进来编不过，必须关掉；显式 glob 仍保证**加新文件零维护**
- `DefineConstants`：`LITEFRAMEWORK_DEBUG`
- 范围锁死三个程序集 + `Server`：**有引擎引用/第三方引用的一律不进双轨**（LiteSim.View / LiteGame 不建 csproj）
- **产物重定向**：程序集目录放一份 `Directory.Build.props`，把 `bin`/`obj` 指到 `.dotnet/`（点开头，Unity 资产库忽略）——否则 Unity 会扫到 `obj` 里生成的 `.cs`。注意各程序集目录**平级，需各放一份**
- **`.csproj` 被 `.gitignore` 忽略**：`.gitignore` 有 `*.csproj` 全局规则 + 白名单例外（`!Assets/LiteFramework/*.csproj`、`!Tests/LiteFramework.Core.Tests/*.csproj`）——**新增程序集必须补 `!` 例外**（或 `git add -f`），否则工程文件进不了库（实况：M7 的 `LiteSim.Core.csproj` 就是这样被挡住的）

```
Tests/                          ← 与 Assets 平级，Unity 不编译
├─ LiteFramework.Core.Tests/
├─ LiteSim.Core.Tests/
├─ LiteNet.Tests/          （M10 建）
├─ Server.Tests/           （M10 建）
└─ Tests.slnx              ← dotnet test 一键
```

### 1.3 M0SelfTest 断言迁移（权威转移）

- **正式断言的唯一权威 = 测试项目**；`M0SelfTestRunner`（mono 组件）降级为"Play 模式冒烟"
- M0 指导各件的"自测"小节清单逐条翻译成 xUnit facts（事件中心零 GC、ReferencePool 容量/裁剪、FSM 切换语义、DI 解析时点报错 + Seal、设置件往返与钳制、时钟三域互不干扰……）
- 两处清单不一致时**以测试项目为准**——写进 M0 收尾对账（§14）

---

## 2. L1 单测规范

### 2.1 用例映射（已有清单 → 测试）

| 来源 | 用例 |
| --- | --- |
| M0 §3 事件中心 | 订阅/注销幂等、派发中改集、队列延迟、StrictMode、**零 GC 用例**（`GC.GetAllocatedBytesForCurrentThread` 前后差值为 0——.NET 有同款 API，直接迁移） |
| M0 §4 引用池 | 容量上限/裁剪/预热上调、重复归还抛、Debug 追踪 |
| M0 §6 FSM | 切换语义、重入抛、`GetData<T>` 类型不符抛、OnLeave 禁改道 |
| M0 §7 时钟 | 三域互不干扰（暂停/时停/墙钟）、ScaledDelta、timeline 跳帧安全 |
| M0 §10 DI | 解析时点报错（未注册 / 循环依赖含链 / 重复注册 / 构造抛真因）、Seal、注册即发现 |
| 状态同步 §2 | **IEEE 边界用例**：加乘除/sqrt 在 .NET 与 Unity 各跑一份**比对结果文件**（跨运行时）；`SimTrig` 查表精度；lint 抓 `Math.Sin` |
| 状态同步 §3 | 固定输入 3000 帧、槽位分配/版本号、checksum 按位哈希、命令缓冲分批、`EntityIt` 过滤 |
| 状态同步 §5 | SnapshotRing 深度/回退、InputHistory、回滚重放一致性 |
| LiteNet | **Protocol 打包/解包 roundtrip、冗余解包、丢包容错**——纯函数层，最好测 |
| Server | AuthSim Step、**SnapshotDiffer 性质测试：增量快照应用后 == 全量快照（不变式，值千金）**、Room 聚合定序 |

### 2.2 惯例

- **确定性是最好的 test double**：Sim/SimRng 注入种子直接用真物，不 mock；mock 只出现在 DI 服务边界（网络契约、配置服务）
- 时间断言不依赖 `Environment.TickCount` 精度（Log 的 Tick 字段不断言具体值）
- 命名：`{件名}_{行为}_{预期}`（如 `ReferencePool_Release_超过上限_丢弃并计数`）；**纪律 / 源码扫描型测试例外**（用可读断言句，如 `NativeCoroutineDisciplineTests.BusinessUnityCodeMustNotUseNativeUnityCoroutines`）

---

## 3. L3 联测：headless 客户端（零依赖红利最大兑现）

### 3.1 形态

```
headless client = LiteNet + LiteSim.Core + 脚本输入序列（纯 C#，无 Unity）
```

因为 Sim 与 Net 零依赖，联机测试**不需要 Unity 参与**。两档：

| 档 | 形态 | 用途 |
| --- | --- | --- |
| **进程内** | 测试 fixture 直接 `new` Room/AuthSim/headless 客户端对象，loopback 互发 | 默认——快、稳定、可断言内部状态 |
| **真三进程** | Server 进程 + headless ×2 独立进程，真实 UDP 栈 | 合并前——验证真实 socket 路径 |

### 3.2 断言（即 M10 验收的自动化形态）

- 对跑 5 分钟：**和解率 < 阈值**（客户端上报汇总）、无状态发散
- 丢包注入 10%：不卡死、追帧不雪崩
- 注入不确定性（人为改一帧输入）→ 服务器对账标记触发
- SnapshotDiffer 不变式（每次广播断言：增量应用 == 全量）

**丢包注入点**：LiteNet 的 `Protocol/Transport` 分层（§4.6 两刀）天然支持——Transport 实现一个带丢弃率的包装即可，不碰 Protocol。

### 3.3 帧同步预案的四端用例（触发预案时）

.NET 侧与 Unity 侧各跑同一输入序列 → 各自导出 checksum 文件 → 比对。测试架构不变，跑的范围变（预案文档 §1 的 M7′ 验收）。

---

## 4. CI 骨架

```
PR / 每次提交：
  ① dotnet test Tests.slnx                   （秒级，L1）
合并前 / 夜间：
  ② Unity batchmode -runTests -testPlatform EditMode   （L2）
  ③ dotnet test 三进程联测过滤用例            （L3）
发布前：
  ④ 真机包 + 手动 L4 清单
```

Unity 机器成本是 ② 的门槛——①③ 不需要 Unity，**日常开发只跑 ①**，②③ 挂夜间/合并。

---

## 5. 落地顺序（贴里程碑）

| 时机 | 动作 |
| --- | --- |
| **M0 收尾** | §1 三件前置（宏统一 / 双轨 csproj / 断言迁移）+ xUnit 骨架 |
| **M7–M9** | IEEE 边界、Sim、快照/回滚用例**每件完成后即迁入 L1**，不等攒批 |
| **M10** | headless 三进程联测进 CI |
| **M12** | MatchArchive 归档 → 重放一致性测试（归档文件进出 L1） |
| 帧同步预案触发 | §3.3 四端比对用例（M7′ 验收） |

---

## 6. 纪律与边界

1. **测试不证明"跨平台确定性"本身**——只证明测过的序列一致。float 版靠快照兜底无所谓；帧同步预案下（M7′）用例必须覆盖尽可能多的运算路径，且这是生命线级验收。
2. **不自动化清单**：Lua UI（§7.8 异常兜底 + 手动）、手感旋钮（inputDelay/插值缓冲——靠和解率观测指标 + 人）、纯表现（粒子/动画/转场）。
3. **权威转移**：M0SelfTest 与测试项目冲突时以测试项目为准；新代码的自测清单直接写进测试项目，不再扩 M0SelfTest。
4. **Lint 与测试互补**：lint 抓"禁用写法"（`Math.Sin`/裸 `UNITY_EDITOR`/Dictionary 遍历），测试抓"行为正确"——两者缺一不可，lint 不是测试的替代品。
5. **静态状态与测试并行**：Core 有三处全局可变静态（`Log` 环形缓冲 / `ReferencePool` 池字典 / `FileSys` Init 标志）——触碰它们的测试类必须 `[Collection("CoreStatic")]` 串行化，否则 xUnit 并行互踩。新测试类触碰静态状态时加 collection 是 PR 纪律。
   - **只读 / 线程安全静态豁免**：如 `SimTrigTables.SinTable`（`readonly` 只读数组）、编译好的 `Regex`——只读且线程安全，无需 collection。
   - **间接写入也算触碰**：经 `SafeCall.Invoke` 的路径在异常时会写 `Log.Error`（`GameScheduler.cs:123`、`TimelineRunner.cs:230`），故 `SafeCallTests` / `GameSchedulerTests` / `TimelineTests` 同属"触碰 `Log`"。**`Log` 无线程锁**——漏加 collection 会造成并发写数据竞争（尤其 `SafeCallTests` 断言 `Log.ErrorCount` 增量时）。

---

## 7. 现状盘点与改进（2026-09-15 核查）

### 7.1 现状覆盖

| 层 | 状态 | 实际内容 |
| --- | --- | --- |
| **L1 dotnet 单测** | ✅ **158 绿** | LiteFramework.Core **115**（EventCenter / RefPool / ObjectPool / FSM / GameClock / GameScheduler / Timeline / Log / FileSys / Setting(Service) / DI / LuaRegistry / RegistryFillReport / SafeCall + 2 纪律测试）；LiteSim.Core **43**（SimTrig / SimVector3 / SimRng / IEEE 基线 / 规则集自测） |
| **L2 Unity EditMode** | ⚠️ **门禁在位、用例 0** | manifest 有 `com.unity.test-framework` 1.1.33；`scripts/l2-unity-gate.ps1` + CI `l2-unity-gate` job（自托管 runner、`L2_ENABLED` 开关）已就位——但它是**非法 meta 扫描 + Unity 编译状态**，**EditMode 用例仍为 0**（§1 称"M0 收尾起接"，未兑现） |
| **L3 三进程联测** | ❌ 0 | M10（批④；`Tests/LiteNet.Tests` 已有同进程 loopback 用例） |
| **L4 PlayMode / 手动** | ❌ 0 | M11 起 |

> **2026-09-18 对账**：L1 现 **282 绿**（LiteFramework **198** / LiteSim **68** / LiteNet **16**）——上表 158 是 2026-09-15 的快照；第三个测试工程 `Tests/LiteNet.Tests` 已并入 `Tests.slnx`（M10 批①），L3 仍为 0（M10 批④）。

### 7.2 缺口（对照 §2.1 映射逐条核）

**未到里程碑（正常）**：状态同步 §3 Sim 层、§5 快照/回滚（M8/M9）；LiteNet `Protocol` roundtrip、Server 的 `SnapshotDiffer` 不变式 / `AuthSim` / Room 定序（M10）。

**该有而没有的：**

| # | 缺口 | 严重度 | 说明 |
| --- | --- | --- | --- |
| a | **无 CI** | 高 | §4 只写了骨架，仓库内**没有任何流水线文件**；"① 秒级 L1"仍靠人手跑 |
| b | **IEEE 跨运行时对账未闭环** | 高 | 基线文件在，但 **Unity 侧跑对账的脚本未写**（§2.6 称要落 `LiteSim.Core.Editor`）——"跨运行时逐位一致"只完成一半 |
| c | **L2 双轨"另一轨"零测试** | 中 | `UNITY_EDITOR` 分支在 Unity 侧的行为从未被测 |
| d | **纪律规则三处各写一遍** | 中 | `SimDeterminismLint`（引擎）/ `NativeCoroutineDisciplineTests`（自带 regex）/ `UnityEditorMacroDisciplineTests`（自带 regex）——改一条规则要改三处 |
| e | **R4 无测试也无强制** | 中 | Dictionary 遍历 / LINQ 只写进扫描器且 M8 才生效；R1~R3 也只扫 `Assets/LiteSim` |
| f | **无覆盖率度量** | 低 | 无 coverlet / 无阈值 |
| g | **无性能与分配回归基线** | 低 | 仅两处零散 `GC.GetAllocatedBytesForCurrentThread` 断言 |

> **进度（2026-09-15）**：缺口 **a（无 CI）** 与 **d（规则三处各写）** 已消除（见 §7.3 ①②）。另修掉一个**只在干净检出暴露**的缺陷：两个源码扫描型纪律测试用 `Assets + ProjectSettings` 定位项目根，而 `ProjectSettings/` **不在版本库中** → `git clone` / worktree / CI 必抛；已改为 `Assets + Tests/Tests.slnx`（均被跟踪），并统一收敛到引擎的 `ProjectLocator`。缺口 **b** 的另一半（Unity 侧跑 IEEE 对账的脚本）**仍未做**（2026-09-18 复核：`Assets/LiteSim/Core/Editor/` 目录不存在——现有的 `SimTrigTableGenerator.cs` 是**生成器**，在 `Assets/LiteSim/Core/Scripts/Editor/`，与"运行态逐位对账"是两件事）。

### 7.3 改进（按性价比排序）

1. ✅ **已完成（2026-09-15）**：GitHub Actions 跑 `dotnet test Tests/Tests.slnx`（`.github/workflows/ci.yml`，秒级、无需 Unity），L1 已是 PR 门禁。
2. ✅ **已完成（2026-09-15）**：纪律扫描统一为**单一引擎** `Assets/Tools/DisciplineScanner`（零依赖独立程序集，不再寄居 `LiteSim.Core`）——R1~R6 + **多根配置**（`Assets/LiteSim` = R1~R5；`LiteFramework/Scripts/Core` = R5；`LiteFramework/Scripts/Unity` + `LiteGame/Scripts/Runtime` = R6）+ 项目根定位（只此一份）。Editor 菜单（`Tools/纪律扫描`）与 dotnet 测试**共用同一引擎**；R6 吸收了原协程纪律测试的规则。此步消掉了缺口 d（重复），e 的规则集也已随多根配置就位。
3. **补 Unity 侧对账脚本**（M7 尾巴）：`LiteSim.Core.Editor` 菜单读 `Tests/LiteSim.Core.Tests/Baselines/IeeeBaseline.txt`，用同一批输入在 Unity 运行态**逐位**比对。不做则缺口 b 永远是纸面承诺。
4. **加 L2 最小骨架**：哪怕 1 个 EditMode smoke（编辑器下加载 `LiteSim.Core` + 跑对账），把"另一轨"纳管。
5. **引入 property-based 测试**：`SimRng` 均匀性、`SimVector3` 运算不变式、将来 `SnapshotDiffer` 的"增量应用 == 全量"——这类**不变式**用 property-based 库自动挖边界，比手写用例更配得上"不变式值千金"。
6. **分层 Trait**：测试打 `Trait("Layer","L1"/"L3")`，支持 `dotnet test --filter` 分层跑。M10 三进程联测进来后**必须**有，否则 L1 的秒级反馈会被联测拖垮。

### 7.4 边界（别扩）

§6.2 的不自动化清单（Lua UI / 手感旋钮 / 纯表现）保持不变。杠杆不在"多写用例"，而在**CI 门禁 + 规则引擎统一 + 对账闭环**这三件。

> **核查记录（2026-09-15）**：L1 全绿（158）；§1.1 三宏并集已落地（Core 内 13 处）；§6.5 collection 已补齐 3 个漏项（`SafeCallTests` / `GameSchedulerTests` / `TimelineTests`）；新增 `UnityEditorMacroDisciplineTests` 补 §1.1 承诺的 lint 缺口；LiteSim 侧新增 **R5**（禁裸 `UNITY_EDITOR`）规则；LiteSim 测试命名已统一为 `件_行为_预期`。
>
> **同日二轮**：纪律扫描统一为单一引擎（§7.3 ② 完成）、CI 落地（§7.3 ① 完成）；L1 现 **149 绿**（LiteSim 29 + LiteFramework 120，计数变动因规则测试由 Sim 侧迁至框架侧）。以下 §7.5~§7.8 为二轮复查新增。

### 7.5 二轮复查新增缺口（2026-09-15）

| # | 缺口 | 严重度 | 说明 |
| --- | --- | --- | --- |
| h | **架构红线没有任何可执行纪律** | 高 | 「资源唯一入口」「模组永不进 Sim」「薄壳不得直发业务包」「热更禁碰 Sim/Net」**只写在文档里**——没有规则、没有测试在守。文档承诺 ≠ 提交即拦 |
| i | **基线可被静默修改** | 高 | `Baselines/IeeeBaseline.txt` 与 `SimRngTests` 的硬编码锚点是「跨运行时一致」的**唯一凭据**，但**改基线即可让测试变绿**，无评审约束 |
| j | **CI 只跑单一平台** | 中 | 主线是跨运行时逐位一致，却在 Windows 单平台跑——**跨平台这条主线等于没验**（IEEE 基线在 CI 里从未被跨平台检验过） |

> **2026-09-18 对账**：h / i / j **三项仍全部未做**——统一引擎规则只到 **R7**（非法 meta GUID，`Assets/Tools/DisciplineScanner`）；`.github/workflows/ci.yml` 的 L1 job 仍 `runs-on: windows-latest` 单平台（无 matrix）；基线文件变更**无任何校验/评审步骤**。

### 7.6 二轮复查新增改进（按价值排序）

1. **把架构红线变成规则**（补进 §7.3 ② 的统一引擎，成本极低）——**状态（2026-09-18 晚，本轮落地）**：

   > **本轮完成**：① **R8/R9/R10 已进统一引擎**（`LintRule.R8ResourcesLoad`/`R9ModInSim`/`R10ShellSendsBusinessPacket` + `ScanTargets` 新目标根 + 5 条用例；全仓扫描 0 违规）
   > ② **跨平台 matrix 已开**（`windows-latest` + `ubuntu-latest`，`fail-fast: false`）
   > ③ **基线防篡改已建**（CI 步：`Tests/**/Baselines/**` 变更须带 `[baseline]` 声明）
   > ⑤ **超时保护已加**（job `timeout-minutes` + `--blame-hang-timeout 5m`） ⑧ **CI 缓存已加**（`setup-dotnet cache`）
   > **未做**：④ 断言/构件收敛 ⑥ `.editorconfig` ⑦ `Directory.Build.props` 三份收敛 ⑨ 覆盖率门槛
   > 落地细节见《CI门禁（L1-L2）.md》→「2026-09-18 升级」表。

   > ⚠ **编号已调整（2026-09-18）**：原提议的 R7 已被占用——2026-09-15 的非法 meta GUID 事故把 **`R7InvalidMetaGuid`** 加进了统一引擎（见 §7.3 与《CI门禁（L1-L2）》）。故下表顺延为 **R8/R9/R10**，勿再按旧编号提。

   | 规则 | 目标根 | 禁 |
   | --- | --- | --- |
   | **R8** | `Assets/LiteGame` | `Resources.Load` / `Resources.LoadAsync`（破「资源唯一入口」） |
   | **R9** | `Assets/LiteSim` | 出现 `Mod` 相关类型（守《模组系统设计》红线 M1「模组永不进 Sim」） |
   | **R10** | `Assets/LiteGame/…/Shell/UI` | `INetworkService`（薄壳/UI 不得直发业务包） |

2. **CI 加跨平台 matrix**：`[windows-latest, ubuntu-latest]`（ubuntu 可先 `continue-on-error` 观察），让「跨运行时一致」第一次真正被验。
3. **基线防篡改纪律**：基线文件变更**必须走评审**（写进 PR 模板）；CI 增一步「基线未被静默修改」检查。这是**测试可信度的根**。
4. **断言 / 构件收敛**：`Close(...)` 等在各测试文件重复定义 → 抽公共断言辅助；零分配断言（`GC.GetAllocatedBytesForCurrentThread`）体系化。
5. **超时保护**：含循环 / 时钟推进的用例加显式超时——xUnit 默认**无超时**，改坏会挂死 CI。
6. **`.editorconfig`**：统一行尾 / 缩进 / 编码（本轮已见行尾噪音）。
7. **`Directory.Build.props` 收敛**：`Assets/LiteFramework` / `Assets/LiteSim` / `Assets/Tools/DisciplineScanner` **三份内容相同** → 仓库根放一份即可（向下继承）。
8. **CI 缓存**（NuGet）。
9. **覆盖率**（coverlet）：只对 `LiteFramework.Core` / `LiteSim.Core` 卡门槛，不对整体卡。

### 7.7 按里程碑该补的测试类别

| 时机 | 该补 |
| --- | --- |
| **M8** | R4 真正生效（确定性容器）；Sim 全量 `ComputeChecksum` 回归锚点 |
| **M9** | **property-based**：快照环 / 回滚重放的**不变式**（「回滚后 == 全程真实输入」）——最该用 property testing 的地方 |
| **M10** | **Protocol fuzz**：随机字节喂解包器**不崩**（网络层必备）；「增量应用 == 全量快照」不变式；三进程联测进 CI + **分层 Trait** |
| **M12** | 归档 → 重放一致性（归档文件进出 L1） |

### 7.8 明确不做

- **变异测试**（Stryker 类）：成本高、收益晚。
- **L4 / PlayMode 自动化**：手感与表现类留人工（§6.2 不自动化清单）。
- **对整体卡覆盖率阈值**：会把精力引到凑数字；只对 Core / Sim 卡。
