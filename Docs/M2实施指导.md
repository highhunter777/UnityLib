# M2 实施指导（配置链路）

> 配套：《框架实施手册》§四（里程碑定位）、《自研框架设计方案》§5（配置链路）/§3.3（启动顺序契约）/§4.3（服务桥）。
> 写法纪律同 M0/M1 指导：API 面不超清单、断言权威在测试项目、文档如实记录提前兑现与偏差。

---

## 0. 现状盘点与四项已定决策（2026-09-10 用户确认）

**现状**：Luban 工具链已在库（`Luban/LubanGenerater/Luban/Luban.exe` 4.11 + gen.bat + luban.conf）；`GameData/Generated` + `Luban.Tables.asmdef` 已存在；AssetService/YooAssetComponent 试验件在 GameMain（MyGame 命名空间 + GameFramework 依赖）；YooAsset 3.0.5 本地包已入 manifest；GameMain 有旧 GameFramework 流程四件（只作参考不搬运）。

| # | 决策 | 内容 |
| --- | --- | --- |
| 1 | AssetService 最小面移植 | 只移植 EditorSimulateMode 初始化 + LoadAssetAsync/LoadRawFileBytes/Text + UniTask 适配；热更/CDN 归 M6 |
| 2 | 废弃统一 RawFile | 删 ConfigLoader.cs 与 StreamingAssets/Config；gen.bat 数据输出改 `LiteGame/RawFile/Config`（Android 下 StreamingAssets 不可 File 读——手册坑位） |
| 3 | M0 遗留三件并入 M2 收尾 | FakeNetworkService 用例（落位 Play 冒烟，见 §6）+ API 面对账 + 行数对账（附录 A/B） |
| 4 | Bridge.data 走 C# 骨架 | M2 交查询门面 + 按行缓存结构（手写两方法验证链路）；LuaTable 转换与生成器：M3/M2 末 |

---

## 1. gen.bat 双输出 + 注册表三件套

**gen.bat 两遍**（bat 内注释只写 ASCII——UTF-8 中文注释在 GBK 控制台乱码破坏解析，实施教训）：
① `-c cs-bin -d bin`：代码→`GameData/Generated`，数据→`LiteGame/RawFile/Config`
② `-d lua`：→`LiteGame/Lua/cfg`（M3 消费）

**三件套**（`Data/` 下，`read_schema_from_file=true`）：
- `#cfg.uiform.xlsx`：Id/Lua路径/Prefab/层级/全屏（Lua路径必填，M3 填充期校验）
- `#cfg.contententry.xlsx`：Id/Name/Entry
- `#cfg.strategy.xlsx`：Id/Name/Lua路径（空 = C# 默认实现）
- 表注册进 `__tables__.xlsx`（output 列实际被 Luban 默认命名覆盖——产物为 `cfg_tbuiform.bytes` 等，loader 键以 `Tables.cs` 为准）

**产物核对**（已跑通）：`Tables.cs` 含 `Tbitem/Tbuiform/Tbcontententry/Tbstrategy` 四表；`Tables` 构造签名 `Func<string, ByteBuf>`（同步）。

## 2. AssetService（`LiteGame/Scripts/Runtime/Resource/`，静态门面豁免）

- **重写而非照搬**试验件：namespace LiteGame、Log → LiteFramework.Log、回调 → UniTask（经 `UniTaskAssetExtensions.AsUniTask`：YooAsset `AsyncOperationBase` 失败转 `InvalidOperationException`）
- `InitAsync`（EditorSimulateMode，幂等）：`YooAssets.Initialize` → `CreatePackage` → `EditorSimulateBuildInvoker.Build(VirtualAssetBundle)` → `InitializePackageAsync` → `RequestPackageVersionAsync` → `LoadPackageManifestAsync`（YooAsset 3.0.x 清单加载独立于初始化——试验件 YooAssetComponent 是 API 参考）
- RawFile **实际实现走 TextAsset**（`.bytes`/`.txt` 以 PackDirectory 打包）：YooAsset 3.0.5 模拟清单单一 BuildBundleType，VirtualAssetBundle 下 RawFileObject 恒加载失败（ABH 路径实测，见实施记录）；`LoadRawFileBytesAsync/TextAsync` API 名不变。收集器 `PackRawFile` 路线与混合包方案待 M6 定。
- 取消语义：YooAsset 操作不支持中途取消——ct 在操作边界检查；等待中不可取消
- location 契约：资源完整路径（手册坑①）
- `internal static Package`：仅供 SceneService 等同程序集薄壳，业务禁止
- **静态门面豁免名单（现行，登记于 2026-09-10）**：① `Log` ② `ReferencePool` ③ `FileSys`（框架 Core 三位，设计方案 §1.1 封闭名单）④ **`AssetService`（业务侧，方案 A"沿用 AssetService 静态门面"结论落地）**——名单到此为止；新增静态门面必须先改本节登记，不接受默认放行。

## 3. IConfigService（`Resource/ConfigService.cs`，DI 单例）

- 构造收 `Func<string, CancellationToken, UniTask<byte[]>>` 字节委托（装配点绑定 `AssetService.LoadRawFileBytesAsync`）——服务不感知资源方案（"松"）
- `TableDataFiles` 显式登记表数据文件清单：逐文件预取 → 缺文件报错带 location（"删 bytes 报错明确"的实现位）；**新增 Luban 表时此清单加一行**（将来随 Bridge 生成器自动产出）
- Luban `Tables` 构造是同步签名——异步预取全部 bytes 后同步建表，loader 查预取缓存
- 失败 fail-fast（损坏/缺失不静默）——与存档"损坏不炸启动"相反：**配置缺失必须炸**（二分口径的另一侧）
- 旧件已删：`GameData/ConfigLoader.cs`、`StreamingAssets/Config/`

## 4. 流程三件 + 错误流程（`LiteGame/Scripts/Runtime/Procedure/`）

- **ProcedureLaunch**（唯一受信装配点）：注册 ConfigService/SceneService → `Seal` → `Bridge.Bind` → ChangeState\<Preload\>。依赖在 GameEntry.Awake 构造注入存为流程字段——流程依赖不从 Owner 取
- **ProcedurePreload**（M2 最小版）：`AssetService.InitAsync` → `IConfigService.LoadAsync` → Main；**M3 锚点**已留（Lua 预载 → main.lua → 三件套读取/校验/Fill）
- **ProcedureMain**：占位空转；**ProcedureError**：Fail 落点，停机 + LastError 日志（M4 接错误 UI）
- **时序契约落地**：FSM 启动从 Awake 移到 `GameEntry.Start()`——晚于全部组件 Awake 的 RegisterInstance，否则 Launch 的 Seal 会把组件注册挡在门外（M0 指导"执行顺序"契约的流程侧兑现）
- `RunAsync` 一行转发 `.Forget()` + try/catch + `Fail()` 后自行 `ChangeState<ProcedureError>()`（Core `Procedure.Fail` 只写 LastError 不切流程——ProcedureError 在 LiteGame，Core 不引用业务类型；M0 指导 TODO(M2) 就此关闭）

## 5. Bridge.Data 骨架 + SceneService + link.xml

- **`Bridge/Bridge.Data.cs`**：`Bridge.Bind(Func<Tables>)` 装配期绑定（Launch 调，不碰容器）+ `Data.GetItem(int)`（按行缓存字典——C# 行对象已被 Luban 缓存，字典为 M3 LuaTable 转换缓存预留同构位）+ `Data.GetUIForm(int)`。M3：ToLuaTable 转换 + 生成器产出
- **`Shell/SceneService.cs`**（DI 单例）：**两种加载方式**——单场景 `LoadSingleAsync/UnloadSingleAsync`（切换语义，先卸后载）与叠加 `LoadAdditiveAsync/UnloadAdditiveAsync`（并发多场景；同 location 重复加载 = Warning + no-op，卸载未加载 = no-op）。单场景切换后叠加登记一律清理（Unity Single 语义销毁全部旧场景，句柄只 Dispose 不再 Unload）。`Progress`/`SingleSceneName`/`AdditiveCount`/`AdditiveLocations`/`IsLoaded` 只读暴露；句柄由本类持有不外泄；**切换决策归 Procedure，业务禁止裸调 AssetService.LoadSceneAsync**；叠加场景的 AudioListener/Camera 冲突由场景制作者处理；进度回调等 M5 消费方出现再加。**场景收集**：`BundleCollectorSetting` 加 LiteGameScenes 组（Assets/Scenes → PackDirectory）；`Test.unity` 已进 EditorBuildSettings 供叠加实测。
- **叠加加载的重复引导隐患与守卫**（实测发现）：场景实例若含 `GameEntry`，叠加加载会因 `DontDestroyOnLoad + Awake` 二次引导并覆盖静态 `s_container`/静态设施（表现为"面板读到的服务与加载用的服务不是同一实例"）。已在 `GameEntry.Awake` 加守卫：`s_container != null` → Warning + 销毁重复件（`_active` 标志使 Start/Update 不参与首个引导之外的动作）。**纪律：除启动场景外，任何场景都不得挂 GameEntry**——守卫是兜底，不是许可。
- **`Assets/LiteGame/link.xml`**：Luban.Runtime / Luban.Tables / LiteFramework 三程序集 `preserve="all"`

## 6. 测试与 Play 冒烟

**xUnit 覆盖面说明**：Luban.Runtime 带引擎依赖（SimpleJSONUnity.cs 引 UnityEngine，`noEngineReferences: false`）→ Config/Bridge 用例进不了 net8 双轨，**降级 Play 冒烟**（本节）。`INetworkService` 在 LiteFramework.Unity（UniTask 签名）同理——**FakeNetworkService 装配用例落 Play 冒烟**（M0 遗留件的最终落位），M3 Lua 到位后在 Play 链路执行。

**Play 冒烟面板**（`LiteGame/Config/Smoke Panel`，Editor 件 `ConfigPanel.cs`——M2 冒烟菜单升级为可配置面板）：
1. **运行状态区**：Play / AssetService / 配置 / FSM 当前流程 / ErrorCount，Play 中 0.5s 自动刷新
2. **表查询区**：表清单由 `cfg.Tables` 反射自动发现（新增 Luban 表零改动，当前 Tbitem/Tbuiform/Tbcontententry/Tbstrategy）；key 可配（int 优先、退化 string），结果显示行总数 + 命中行字段 dump，未命中/异常分列；选择与输入经 EditorPrefs 持久化；结果可写日志（tag `Smoke`）
3. **产物核对区**：`ConfigService.TableDataFiles` 逐项 File.Exists，缺失标红（预检不依赖运行时）
4. **删 bytes 报错路径**（手动）：删 `RawFile/Config/tbstrategy.bytes` → Play → 启动链停机 ProcedureError，面板状态区可读 LastError（Console 有 `配置文件加载失败:...tbstrategy.bytes`）
5. **FakeNetworkService 装配**（M0 遗留，M3 随 Play 链路执行）：实现 INetworkService 的假件注册进容器 → 进 Tickables → Tick 被调、stats 可读、OnStateChanged 可收

**dotnet test 基线**：**65 用例全绿**（M1 51 + 设置件 14；本里程碑 Core 改动：Procedure.cs 泛型化、Setting.cs 拆分）。

---

## 6.5 玩法层决策：挂载回收契约与队友单机边界（2026-09-10 定）

### ① 挂载型对象回收契约 = **谁挂谁清**

Buff / 装备 / 光环 / 定时器等挂载物，**由挂载方负责摘除与回收**；宿主（角色、队友实体）只维护一个**生命周期容器**，回收时逆序执行容器内已登记的注销委托（与 `SubscriptionBag` 同款语义）。

- **挂 = 交出注销委托**：挂载方必须提供 `Action` 注销；宿主不遍历别人的挂载、不做"帮别人清理"的兜底
- **宿主回收后容器必须为空**——执行完仍非空 = 有人挂了却不注销，Debug 告警（设计方案 §163"漏了不报错、只表现为缓慢内存增长"的隐患由此可见）
- 与 §7.3"返回注销委托"红线同源；该容器即 **M4 实体壳的"生命周期容器"**——**M4 之前业务层按同一契约自建**，不做两套
- 关闭挂账：设计方案 §163"角色回池时谁清 Buff"由此定案

### ② 队友系统只用于**单机部分**（不进 Sim）

- 不进 `SimWorldState`：不做确定性、不进固定容量槽位数组、不参与快照与 checksum（"id 不用引用 / 槽位数组"是联机纪律，单机队友**不受此约束**）
- 内部**可用引用与 `List<Teammate>`**；AI/行为使用可取消 UniTask，禁止 C# 原生 Unity 协程
- **跨边界红线（必须守）**：队友一旦与 Sim 侧实体（联机战斗对象）交互，一律走 **id**，由玩法层做 id ↔ 引用转换；**禁止把 Sim 实体引用存进单机队友结构**——否则污染确定性，M7 接入时无处可查
- 时间原语待 M4 调度器/序列执行（业务禁止裸调 UniTask 时间原语，纪律不变）
- 编队/好感度持久化走 `FileSys`；内容热更走 Lua（M3+）

---

## 7. M2 完成判定

- [x] gen.bat 跑通三路产物（cs/bin/lua，四表）
- [x] C# 强类型查表就绪（**实测** `Tbitem.Get(1001)` = 道具1/10）
- [x] 注册表三件套产物就绪（消费在 M3/M4）
- [x] 删 .bytes → LoadAsync 报错明确（**实测**：7 帧内停机 ProcedureError，报错带 location）
- [x] 流程链 Launch → Preload → Main 通（Seal 时序契约生效；**实测 8 帧**）
- [x] M0 遗留三件：API 面/行数对账（附录 A/B）+ FakeNetworkService 落位说明（§6）
- [x] Unity 侧 Play 冒烟执行（MCP 步进帧实测，0 Error；§实施记录）

---

## 附录 A：API 面对账（对照设计方案 §1.1 验收口径：公开 API 不超清单）

| 件 | 公开面 | 判定 |
| --- | --- | --- |
| 事件中心 | Subscribe/Publish/PublishQueued/StrictMode（+ITickable/IModuleStats） | ✅ 合同内 |
| 引用池 | Acquire/Release/Add/Remove/SetMaxSize/GetAllInfos/DefaultMaxSize | ✅ 合同内（GetAllInfos 为 Debug HUD 数据源，清单含） |
| FSM+Procedure | Start/ChangeState/CurrentState/CurrentStateTime/Fail；数据字典已删（具名 Owner 字段） | ✅ 合同内（M0 指导已记删字典） |
| 时钟 | TimeScale/Paused/Now/ScaledDelta + IWorldClock/IUIClock 标记 + IWallClock + MaxDelta | ✅ 合同内（MaxDelta 为实现回写，M0 指导 1b 记录） |
| FileSys | 契约清单 + 异步逃生口（M0 指导 §8 增补批准） | ✅ 合同内 |
| SettingService（机制） | Load/Save/SaveIfDirty/Get*/Set*/Remove/Has | ✅ 合同内（存储引擎，留 Core） |
| Setting\<T\>（机制） | Get/Set（键/默认值/clamp 单源） | ✅ 合同内（Core/IO/Setting.cs） |
| **GameSettings（业务）** | 具名清单：MasterVolume/Quality/Language/Fullscreen | ✅ **2026-09-10 从 Core 迁至 LiteGame**（机制留框架、清单属业务；键/默认值/范围是游戏内容） |
| DI | Register*/Resolve/Seal/Tickables/Stats | ✅ 合同内（**Validate 已删**——M1 指导 §1 决策记录） |
| Log | Info/Warning/Error/Fatal/SetHelper/SetOutputLevel/ErrorCount/Recent | ✅ 合同内 |
| M2 新件 | AssetService(InitAsync/LoadAssetAsync/LoadRawFileBytes/Text) / IConfigService / SceneService / Bridge.Bind+Data / **ObjectPool\<T\>（Core 件，随本次交付）** | ✅ 手册 M2 步骤 5/6/8 清单内（ObjectPool 为 2026-09-10 决策修订新增） |

## 附录 B：行数对账（软约束；实际 vs §1.1 预算，超 20% 写明原因）

| 件 | 预算 | 实际 | 偏差 |
| --- | --- | --- | --- |
| 装配与驱动（GameEntry） | ~100 | 105 | ✅ |
| 事件中心 | ~180 | 192 | ✅ +7% |
| 引用池 | ~120 | 167 | ⚠️ +39%：Debug 统计字段 + 容量上限机制（M0 指导 §4 设计内增补，DroppedCount/PeakUnused 等） |
| FSM + Procedure | ~220 | 165 | ✅ |
| 时钟（M0 份额） | ~250（三件合计） | 105（时钟+墙钟；调度器/序列执行 M4 交付后并账） | ✅ |
| SettingService | ~80 | 137（另 GameSettings 103 为 M0 指导新增消费面件） | ⚠️ +71%：四类型 Get/Set 全口径 + 八条契约注释；GameSettings 单源纪律件 |
| Log | ~100 | 119 | ✅ +19% |
| FileSys | ~120（设计）/ ~250（M0 指导含异步增补） | 266 | ✅ 对齐 M0 指导版 |
| 横切小工具 | ~290 | 233 已交付（SubscriptionBag 38/ITickable 10/IModuleStats 17/MainThreadDispatcher 44/UniTask 适配 73/M2 增；SafeCall、DevReload 随 M3） | ✅ |
| M2 新件（手册步骤 5/6/8，无单独预算） | — | AssetService 129 + ConfigService 81 + 流程四件 137 + Bridge 44 + SceneService 128 + UniTask 适配 41 + **ObjectPool\<T\> 165 + IPoolable 20** = 745 | 记录在案 |

---

## 附：实施记录

- 2026-09-10：M2 全部代码落地——gen.bat 双输出 + 三件套 xlsx 与注册、AssetService/UniTask 适配、ConfigService（旧件清理）、流程四件 + GameEntry Start 时序、Bridge.Data 骨架、SceneService、link.xml、Editor 冒烟菜单；`Luban.Tables.asmdef` 实体重建（此前只剩 .meta 孤儿）；dotnet 51/51 基线绿。教训一条：bat 注释用 ASCII（UTF-8 中文在 GBK 控制台乱码破坏解析）。
- 2026-09-10（续）：**Play 冒烟实测全绿**（经 MCP 步进帧执行——编辑器失焦被节流，帧泵不转是环境现象非代码问题）。实施中的两个实证决策：
  1. **配置文件走 TextAsset 打包（PackDirectory），不用 RawFileObject/PackRawFile**——YooAsset 3.0.5 模拟清单是单一 BuildBundleType，VirtualAssetBundle 下 RawFileObject 加载恒 null（ABH 路径实测）；混合资产+原生文件的单包方案待 M6 打包配置时定。`LoadRawFileBytesAsync/TextAsync` API 名不变，内部实现 TextAsset。
  2. **Luban 命名空间规则实证**：bean 命名空间 = topModule + xlsx 文件名的模块前缀（`#cfg.uiform.xlsx` → `cfg.cfg`，撞车）；表注册 full_name 去模块前缀 + 文件名去掉 `#cfg.` 前缀后，新表落 `cfg` 命名空间、demo 表保持 `cfg.demo`（Tables.cs 内部引用经命名空间相对解析自洽）。
  - 冒烟结果：Launch→Preload→Main 全通（8 帧）；`Tbitem.Get(1001)` = 道具1/10；三件套可枚举（1/1/1 行）；删 `tbstrategy.bytes` → 7 帧内报错 `配置文件加载失败:...tbstrategy.bytes` 并停机 ProcedureError——报错明确性验收线实测通过。
- 2026-09-10（收尾）：冒烟菜单升级为 **可配置面板**（`ConfigPanel.cs`，`LiteGame/Config/Smoke Panel`）——表清单反射自动发现、key 可配（int/string 双路）、命中行字段 dump、未命中与异常分列、EditorPrefs 持久化、产物核对区。实测：4 表自动发现、命中 `Tbitem/1001` 字段 dump 正确、未命中键提示明确、枚举 1/2/1/1 行、产物 4/4 齐。参考件 `ConfigSmokeMenu.cs` 已删。
- 2026-09-10（收尾 2）：**SceneService 扩展叠加加载**——`LoadSingleAsync/UnloadSingleAsync`（切换）+ `LoadAdditiveAsync/UnloadAdditiveAsync`（并发）双模式；单场景切换清理叠加登记（Unity Single 语义销毁旧场景）；场景收集组（LiteGameScenes）+ Test.unity 进 build settings。Play 实测：叠加加载 3 帧完成（additiveCount=1、sceneCount 2）；重复请求 Warning + 计数不变（去重守卫生效）；卸载 0 帧完成、未加载 location 卸载 no-op 不抛；叠加后再单场景切换 → 叠加登记清零、场景数回落 1（清理逻辑正确）。注意事项：叠加场景各自 AudioListener 会触发 Unity 警告，属场景制作者责任（已写入类契约）。
- 2026-09-10（收尾 3）：**配置面板新增场景服务测试操作**——场景区：场景下拉（EditorBuildSettings 清单）+ 可编辑 location、四个操作按钮（单场景加载/叠加加载/卸载单场景/卸载叠加）、忙碌守卫 + 结果落面板、状态行（单场景名/叠加数/叠加列表）、Play 中 0.5s 自动刷新；`SceneService` 补 `AdditiveLocations` 只读视图；LiteGame.Editor asmdef 加 UniTask 引用（编辑器异步操作用）。实测（Play 步进帧）：场景清单正确列出 Boot/Test、状态文案随单场景加载/卸载正确变化、叠加加载 3 帧完成、去重 Warning 生效、OnGUI Layout 渲染无异常。
  - **实测中发现并修复的真实隐患**：叠加加载的场景若含 `GameEntry`（本工程 Test/Boot 均挂），`DontDestroyOnLoad + Awake` 会二次引导并替换静态容器——表现为"面板 resolve 到的服务 ≠ 加载用的服务"。已在 `GameEntry.Awake` 加重复引导守卫（Warning + 销毁重复件 + `_active` 标志使 Start/Update 不参与）。**守卫 Play 实测通过**（2026-09-10 补验）：叠加含 GameEntry 的场景后容器身份不变、Console 出现 `[GameEntry] 检测到重复 GameEntry（场景 Test 误含引导件）——已销毁，保留首个引导`。纪律：除启动场景外任何场景不得挂 GameEntry（守卫是兜底，不是许可）。
- 2026-09-10（收尾 4）：**设置边界修订——GameSettings 从框架迁至业务**。判据：机制留 Core、清单属业务。`Core/IO/GameSettings.cs` 拆分为：① `Core/IO/Setting.cs`（仅 `Setting<T>` 强类型值对象，机制）；② `LiteGame/Scripts/Runtime/Setting/GameSettings.cs`（具名清单 volume/quality/lang/fullscreen = 游戏内容，键/默认值/clamp 在此单源）。GameEntry 零改动（同命名空间解析）。**顺带补齐设置件零测试缺口**：新增 `SettingTests`（7 例：默认值/四类型往返/clamp 双向/坏值 Warning/float 无损/不支持类型抛）+ `SettingServiceTests`（7 例：首启静默/落盘往返/脏标记二分/Remove 语义/坏值 Warning/空键抛）——dotnet 基线 51 → **65 用例全绿**。Play 实测：容器可解析 `LiteGame.GameSettings`，MasterVolume 默认 0.8。
- 2026-09-10（收尾 5）：**层次边界修订（框架/业务划分检查结论落地）**：
  1. **流程 owner 泛型化**：Core 的 `ProcedureOwner`（sealed，含零使用业务字段 `RetryBoot`）删除——改为 Core 定义 `IProcedureOwner { Exception LastError { get; set; } }` + `ProcedureBase<TOwner> where TOwner : IProcedureOwner`，具体 owner 落业务件 `LiteGame/Scripts/Runtime/Procedure/ProcedureOwner.cs`。业务字段（联机后的 roomId/frameNo 等）此后加在业务 owner，**不回填框架**。业务侧 4 个流程类改 `: ProcedureBase<ProcedureOwner>`（同命名空间解析，其余调用点零改动）。dotnet 65/65 全绿 + Play 实测（8 帧到 ProcedureMain、owner=LiteGame.ProcedureOwner、RetryBoot 字段已不存在、配置 Loaded）。
  2. **静态门面豁免名单登记**：`AssetService` 登记为第 ④ 位（§2 已列现行名单；框架 Core 三位 = Log/ReferencePool/FileSys）。设计方案旧文档不回写，以本节为准。
  3. 同批修正：`M0SelfTestRunner` 命名空间错位（LiteGame.Runtime 程序集里写 namespace LiteFramework → 改 LiteGame）、`LiteGame.DevHUD` asmdef 改 `includePlatforms: ["Editor"]`（兑现"打包剥离"承诺）。
- 2026-09-10（收尾 6）：**对象池决策修订**——设计方案 §1.1 原"直接使用 UnityEngine.Pool.ObjectPool + IModuleStats 包装（~20 行），不自研"**作废**，改**自研两层**：① Core 机制件 `ObjectPool<T>`（`Acquire/Release/Preload/Clear/Trim` + onGet/onRelease/onDestroy 三回调 + `maxIdle` 容量上限；池满策略钉死"归还即销毁不淘汰"；重复归还 Debug 抛（三宏并集）；统计原生接 `IModuleStats`，口径与 ReferencePool 一致）；② Unity 层 `GameObjectPool`（**包 Core 池做底层**不自研第二套池逻辑：prefab 维度 + 加载竞态表 + 池根 DontDestroyOnLoad）。理由：官方池 Debug 校验面只在 Editor（框架要 DEVELOPMENT_BUILD 也开）、统计口径与 ReferencePool 两套、stats 要再包一层 API 面重复、工具类调用不走装配纪律。**代价留痕**：~180 行自研代码自担 bug。**① `ObjectPool<T>` 已交付**（`Core/Pool/ObjectPool.cs`，~145 行）：16 个 xUnit 用例（往返复用/三回调/池满销毁/重复归还抛/Preload 自动上调/Trim/Clear/峰值/onRelease 异常销毁不入池/onGet 传播/Snapshot 口径）——dotnet 基线 65 → **81 用例全绿**。两处口径实证（与 ReferencePool 对齐）：`DroppedCount` 只记超限丢弃，onRelease 异常销毁不计数；`Trim/Clear` 只清空闲、在用对象按契约归还时自然走完生命周期。
**`IPoolable` 可选自生命周期接口（同日追加，2026-09-10 定）**：`OnSpawn/OnDespawn` = **对象自身的生命周期**（自清理/解除引用/逆序执行生命周期容器——谁挂谁清的执行点）；构造回调 = **池策略**（SetActive/挪池根）。顺序钉死：Acquire `onGet → OnSpawn`；Release `OnDespawn → onRelease → 满则销毁`；Trim/Clear 只走 onDestroy 不二次 OnDespawn。异常保护同 onRelease（对象作者代码炸 → Log.Error + 销毁不入池）；OnSpawn 炸 → 销毁 + **账目回滚**（不留幽灵占用）+ 异常传播。不实现接口完全合法（GameObject 不实现也不堵 M4 GameObjectPool；`IReference` 是纯数据层的强制接口，层次不同不冲突）。带参初始化不走接口——Acquire 后调用方赋值。用例 16 → **20 个，基线 85 全绿**。
**池的持有纪律（2026-09-10 定）**：**机制统一、实例分散**——`ObjectPool<T>` 是通用机制类，池实例由各系统自持、生命周期随宿主（清理时机是策略，归业务）；**不做全局池管理器**（按 key 存取 = 服务定位器换皮；"何时清哪些池"进框架即策略越界；全局 `ReferencePool` 已覆盖无回调纯数据对象这一半）。两条补丁：①跨系统共享同类池由**装配点单份创建 + 构造注入**，禁止各自 new（两份空闲列表互不相通）；②可观测性靠注册即发现——池 `RegisterInstance` 进 DI 或宿主 `IModuleStats` 汇总池的 `Snapshot`，禁止 HUD 盲区。入场线：出现"运行时动态创建大量同质池、需统一内存预算"再考虑池注册表（只做汇总统计 + 全局 Trim，不做存取转发）。**② GameObjectPool 留 M4** 随实体壳（依赖 AssetService 真实消费节奏）；挂载契约衔接：`onRelease` 回调里逆序执行宿主生命周期容器（谁挂谁清，§6.5）。
