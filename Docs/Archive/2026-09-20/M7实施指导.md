# M7 实施指导：数值与确定性纪律（float 版）

> 依据：《状态同步实施方案》**§9 里程碑（M7 定义与验收）**、**§2 数值与确定性纪律**（§2.1 IEEE 边界 / §2.2 其余纪律）、**§11 开工前检查清单**（第 1/3/4/5 项属 M7）；顺序依据《框架实施手册》§七（先联机）与《联机同步策略设计》§4。
> 定位：**联机线第一件**。产出 `LiteSim.Core` 程序集（零依赖纯 C#）+ 查表生成链 + lint 纪律件；它是 M8（Sim 层）的地基。
> 工期（§9 预估）：**2–3 天**；预算 ~1100 行（生产码 ~640 / 生成器与 lint ~270 / 测试 ~350）。

---

## 0. 现状盘点（开工前核对）

| 项 | 状态 |
| --- | --- |
| 框架层 | ✅ M0–M4 收官（dotnet 基线 **94 绿**；`Tests/LiteFramework.Core.Tests` + `Tests/Tests.slnx`） |
| `LiteSim.Core` | ❌ **不存在**（本里程碑创建） |
| `LiteNet` | ❌ 不存在（M10；§11 第 6/7 项属 M10 前置，M7 不做） |
| 双轨编译样板 | ✅ `Assets/LiteFramework/LiteFramework.Core.csproj`（net8.0 / LangVersion 9 / `EnableDefaultCompileItems=false` / 显式 include） |
| 生成链先例 | ✅ `Luban/gen.bat`（三遍：C#+bin / Lua / LuaKeys）+ Editor 菜单生成器先例（`WidgetPrefabBuilder`） |
| 版本控制 | ⚠️ **工程无 git**：生成物入库、删除不可恢复（操作前备份/先算冲突） |

---

## 1. 前置决策（开工前定案）

| # | 决策 | 定案 | 理由 |
| --- | --- | --- | --- |
| 1 | 程序集分层 | 新建 `LiteSim.Core`（`noEngineReferences: true`、`references: []`）+ `LiteSim.Core.Editor`（生成器/lint，`includePlatforms: [Editor]`） | 与 `LiteFramework.Core` 同款二分法；**物理隔离是纪律的执行者**（§1） |
| 2 | 物理落位 | `Assets/LiteSim/Core/Scripts/`、`Assets/LiteSim/Core/Scripts/Editor/`；测试放 `Tests/LiteSim.Core.Tests`（与既有测试并列） | 数值层与被测骨架分离（Unity 侧不引测试） |
| 3 | 双轨编译 | `Assets/LiteSim/Core/LiteSim.Core.csproj`（net8.0 / LangVersion 9 / `IsPackable=false` / include `Scripts/**`） | dotnet 侧可单测（框架层先例） |
| 4 | 状态数值类型 | **状态只用 `float` + 整型**；**禁 `double` 进状态**（double 运算跨平台亦有差异，且无收益） | §2.1 |
| 5 | 表规格 | **sin/cos 表长 4096（float） + 象限归约 + 线性插值**；`tan = sin/cos`（不建表） | 精度/体积/生成时间平衡（精度验收 ≤ 1e-5） |
| 6 | atan2 方案 | **octant 归约 + 同一张表查 + 线性插值**（不引入 CORDIC） | 复用一张表、实现短、精度足够 |
| 7 | 生成器形态 | **Editor 菜单产码 → `SimTrigTables.cs` 入库**；生成期可用 `System.Math`（double）填表，**运行期严禁**（lint 抓） | §2.1 硬规则 |
| 8 | lint 形态 | **Editor 菜单扫描器**（源码文本扫描 + 规则集），不上 Roslyn analyzer | 成本/收益；CI 化时再升级（§7 边界） |
| 9 | 单位约定 | **米 / 秒**；角度 = 弧度；y 轴 2.5D（§3.5）——写进 `SimMath`/`SimTrig` 文件头注释 | §11-4 |
| 10 | 容差比较 | `SimMath.NearlyEqual(a,b)`：绝对容差 **1e-6**；Sim 程序集内 float `==` 一律改容差（lint 抓） | §2.1 配套纪律② |
| 11 | SimRng | **Xorshift64\***（`ulong` 状态）；`NextUInt32 / NextFloat01 / NextRange(int,float)`；随机状态进快照（M8 接入） | §2.2 |

---

## 2. 分步实施（逐件施工图）

### 2.0 程序集骨架（0.5h）

```
Assets/LiteSim/Core/
├── LiteSim.Core.asmdef              # noEngineReferences:true, references:[]
├── LiteSim.Core.csproj              # 双轨编译（抄 LiteFramework.Core.csproj 属性段）
└── Scripts/
    ├── SimMath.cs
    ├── SimTrig.cs                   # 查表 + 归约 + 插值（不填表）
    ├── SimTrigTables.cs             # ← 生成物（入库，勿手改）
    ├── SimVector3.cs
    ├── SimRng.cs
    └── Editor/
        ├── LiteSim.Core.Editor.asmdef   # includePlatforms:[Editor]，引 LiteSim.Core
        ├── SimTrigTableGenerator.cs     # 菜单：LiteSim/生成 SimTrig 查表
        └── SimDeterminismLinter.cs      # 菜单：LiteSim/纪律扫描（lint）
Tests/LiteSim.Core.Tests/             # xUnit；并入 Tests/Tests.slnx
    ├── LiteSim.Core.Tests.csproj
    ├── SimTrigTests.cs
    ├── SimVector3Tests.cs
    ├── SimRngTests.cs
    ├── IeeeBoundaryTests.cs
    └── LintRuleTests.cs
```

**验收**：dotnet 侧 `dotnet test` 能编到新项目；Unity 侧 asmdef 编译零错误、`LiteSim.Core` 不在 HybridCLR 热更程序集内。

### 2.1 `SimMath`（~80 行）

**API 契约**：
```csharp
public static class SimMath
{
    public const float Dt = 1f / 60f;               // 逻辑帧步长（与 SimConfig 同源）
    public const float Tolerance = 1e-6f;

    public static bool NearlyEqual(float a, float b);          // |a-b| <= Tolerance（绝对）
    public static float Clamp(float v, float min, float max);
    public static float Clamp01(float v);
    public static float Lerp(float a, float b, float t);       // 禁 Mathf（本程序集无 UnityEngine）
    public static float Abs / Min / Max (float);
    public static int   Abs(int);
}
```
**要点**：文件头注释写死**单位约定（米/秒、弧度）与 IEEE 边界纪律**（禁超越函数、禁 FMA 写法、禁精度依赖比较）——§11-4 验收项。
**用例**：`NearlyEqual` 边界（恰等于容差/超出）、`Clamp01` 极端值、`Lerp` 端点。

### 2.2 `SimTrig` + 生成器（~150 + ~120 行）

**生成器（Editor）**：
- 菜单 `LiteSim/生成 SimTrig 查表` → 产出 `SimTrigTables.cs`（**入库**）
- 表内容：`SinTable[4096]`（覆盖 `[0, π/2]`，用 `Math.Sin` 填 —— **仅生成期允许**）；表头注释标注"生成物，勿手改"+ 生成参数
- 幂等：重复执行产出逐字节一致（排序/格式化固定）

**运行期 `SimTrig`（查表 + 归约 + 插值）**：
```csharp
public static class SimTrig
{
    public const int TableSize = 4096;
    public static float Sin(float radians);      // 象限归约 → 查表 → 线性插值
    public static float Cos(float radians);      // Sin(radians + π/2)
    public static float Atan2(float y, float x); // octant 归约 → 查表 → 插值
    public static float Tan(float radians);      // Sin/Cos（分母容差保护）
}
```
**要点**：
- 归约只用**加减法与整数运算**（IEEE 基本运算 ✓）；插值 `a + (b-a)*t` ✓
- **边界保护**：`NaN/Inf` 输入 → 返回 0 并 Debug 断言（Debug 三宏）
- 不在运行期出现任何 `Math.`/`MathF.` 超越函数（lint 验收项）

**用例**：①精度：与 `Math.Sin` 逐点比对（采样 10k 点，**|误差| ≤ 1e-5**）②象限边界（0/π/2/π/3π/2/2π、负角、大角 1e5 rad）③`Atan2` 四象限 + 轴上（含 0,0）④`Tan` 接近 π/2 的行为 ⑤**重现性**：同一输入序列两次调用结果逐位一致

### 2.3 `SimVector3`（~180 行）

**API 契约**：`struct SimVector3 { float X, Y, Z; }` + 运算符（`+ - * float /`）+ `Dot/Cross/Length/LengthSquared/Normalized`（除以零保护）+ `Distance` + `ToString`
**要点**：`Length` 用 `(float)Math.Sqrt`？**→ 用 `SimMath` 内的 `Sqrt` 包装（`MathF.Sqrt` 或 `(float)Math.Sqrt` 均可，sqrt 是 IEEE 基本运算 ✓）**；`Normalized` 在长度 < 容差时返回零向量（禁 NaN 扩散）
**用例**：运算符一致性、`Dot/Cross` 已知值、`Normalized` 零向量、`Length` 与勾股精度、结构体**无装箱**（对比 `object` 装箱的分配用例）

### 2.4 `SimRng`（~80 行）

```csharp
public struct SimRng
{
    public ulong State;                                  // 进快照（M8）
    public SimRng(ulong seed);
    public uint  NextUInt32();
    public float NextFloat01();                          // (NextUInt32() >> 8) * (1f/16777216f)
    public int   NextRange(int minInclusive, int maxExclusive);
    public float NextRange(float min, float max);
}
```
**要点**：仅整数/位运算 + **乘法换算**（无除法精度依赖）；`NextFloat01` 用 24 位尾数构造（不引入 `double`）；序列稳定 = "同种子同序列"用例
**用例**：固定种子前 8 个值**硬编码期望**（回归锚点）；均匀性粗检（仅 sanity，不做统计强断言）；`NextRange` 边界与分布无越界

### 2.5 纪律 lint 扫描器（~150 行）

菜单 `LiteSim/纪律扫描`：文本扫描目标目录（默认 `Assets/LiteSim/`，可配），规则集：

| # | 规则 | 形态 | 生效范围 |
| --- | --- | --- | --- |
| R1 | **禁超越函数**：`Math.Sin/Cos/Tan/Atan2/Exp/Log/Pow`、`MathF.*` 同名 | 源码扫描（排除 `SimTrigTables.cs` 与 `Editor/`） | **M7 起硬规则** |
| R2 | **禁 FMA 写法**：`MathF.FusedMultiplyAdd`、`System.Math.FusedMultiplyAdd` | 同上 | M7 起 |
| R3 | **禁 float 精度比较**：`==`/`!=` 两侧含 float 变量（白名单：`SimMath.NearlyEqual`、与常量 0 的明确比较需注释豁免） | 同上 | M7 起 |
| R4 | **禁确定性容器/遍历**：`Dictionary` 遍历（`foreach` over dict）、`System.Linq`、不稳定 `Sort` | 同上 | **M8 起**（先把规则写进扫描器，命中即报） |

**输出**：逐条 `文件:行:规则:代码`，退出码非零（供将来 CI）——**M7 的验收演示 = R1~R3 各造一个负例被抓住**。
**用例**：规则集自测（对样例文本断言命中/漏报；豁免注释生效）。

### 2.6 IEEE 边界用例集（~120 行，属测试项目）

- **基础运算逐位一致**：对一批确定输入（含负数、极小/极大、`+0/-0`、次正规数）在 .NET 侧算 `+ - * /` 与 `Sqrt` → 结果 `BitConverter.SingleToInt32Bits` **逐位**记录为**基线文件**（入库）
- **Unity 侧对账**：Play/编辑态用同一批输入跑同一套运算 → 与基线**逐位比较**（等价于 §9 验收②"加乘除/sqrt 在 .NET 与 Unity 下一致"）
- **运算链 checksum**：`SimVector3` 混合运算 10k 步 + `SimRng` 序列 → 汇总 `uint` checksum 与基线一致（等价于 §9 验收③）

> 说明：Unity 侧对账**不引入 Play 断言框架**（数值层无引擎依赖）；用一次性 Editor 菜单/脚本跑，结果与基线文件比对（脚本落 `LiteSim.Core.Editor`，产物入 `Tests/LiteSim.Core.Tests/Baselines/`）。

---

## 3. 测试与自测策略

| 层 | 手段 |
| --- | --- |
| dotnet 单测 | `Tests/LiteSim.Core.Tests`（xUnit）+ 并入 `Tests/Tests.slnx`；`dotnet test Tests/Tests.slnx`（基线 **94 → 94+N**，N 记入完成判定） |
| Unity 侧编译 | asmdef 编译零错误 + asmdef 字段核对（`noEngineReferences:true` / `references:[]` / 不在热更程序集） |
| **跨运行时对账** | 基线文件 + Editor 菜单"跑对账"（逐位比较，非容差） |
| lint 自测 | 规则集负例（R1~R3 各一）+ 正例（`SimTrig` 本身不被误报） |
| 无 Play 断言 | 数值层不进运行时（保持零引擎依赖） |

**红线**：任何"容差比较通过就当跨运行时一致"的写法都是错的——跨运行时必须**逐位**；容差只用于**游戏逻辑**的数值判断（`NearlyEqual`）。

---

## 4. 完成判定（对账 §9 验收）

- [ ] **查表精度用例全通**：`SimTrig` 采样 10k 点，|err| ≤ 1e-5（对 `Math.Sin` 参考）
- [ ] **IEEE 边界用例**：`+ - * /`、`Sqrt` 在 .NET 8 与 Unity 结果**逐位一致**（基线文件比对通过）
- [ ] **lint 抓到直调**：`Math.Sin/Cos/Atan2` 负例被 R1 命中（附演示输出）
- [ ] **同序列重跑一致**：`SimRng` 固定种子前 8 值硬编码锚点 + 运算链 checksum 与基线一致
- [ ] `SimTrigTables.cs` 生成菜单就绪、生成物入库、**重复生成产出逐字节一致**
- [ ] `SimMath`/`SimTrig` 文件头含单位约定与 IEEE 纪律注释
- [ ] `LiteSim.Core.asmdef`：`noEngineReferences:true`、`references:[]`；不在 HybridCLR 热更程序集
- [ ] dotnet 基线更新并记录（94 → 94+N）
- [ ] 行数对账（超 20% 写明原因）

---

## 5. 预算

| 件 | 预算（行） |
| --- | --- |
| `SimMath` | ~80 |
| `SimTrig`（查表/归约/插值） | ~150 |
| `SimTrigTables.cs`（生成物，不计入人工） | 4096+ 行数据 |
| `SimTrigTableGenerator`（Editor） | ~120 |
| `SimVector3` | ~180 |
| `SimRng` | ~80 |
| 纪律 lint 扫描器（Editor） | ~150 |
| 测试（5 个文件） | ~350 |
| 骨架（asmdef/csproj） | ~40 |
| **合计（人工）** | **~1150** |

---

## 6. 决策记录（本轮定案）

| # | 决策 | 备注 |
| --- | --- | --- |
| ① | 表长 **4096 + 线性插值**（sin/cos 共用一张 + 象限归约；tan 不建表） | 精度/体积/生成时间平衡 |
| ② | `Atan2` = **octant 归约 + 查表 + 插值**（不引入 CORDIC） | 复用一张表 |
| ③ | 生成器用 `double` 填表（**仅生成期**），运行期只做查表 + IEEE 基本运算插值 | §2.1 硬规则 |
| ④ | 测试全在 **dotnet 项目**；跨运行时一致性用**逐位比较 + 基线文件**，不用容差 | 容差只服务游戏逻辑 |
| ⑤ | lint 用 **Editor 菜单文本扫描器**（不上 Roslyn analyzer）；R4（容器/遍历）**M8 起**生效 | 成本/收益，CI 化时升级 |
| ⑥ | 状态类型只用 `float` + 整型，**禁 `double` 进状态** | 跨平台差异 + 无收益 |
| ⑦ | 容差比较统一 `SimMath.NearlyEqual`（1e-6 绝对容差） | 一处定义，全 Sim 引用 |

---

## 7. 边界与风险

- **与显式拒绝清单一致**：不引入 ECS/fantasy 类依赖；`LiteSim.Core` 保持 `references: []`（§1 分层纪律）
- **不预做 M8 的事**：本里程碑**不建** `SimWorldState`/系统/checksum 全量（M8）、不建快照回滚（M9）、不 vendor kcp2k（M10）
- **风险：表长与精度**——若 4096 精度不达标，先加插值阶数（线性→二次）再考虑加表长；**不加表长优先**（体积/生成时间）
- **风险：Unity 侧对账脚本**依赖编辑器菜单（非 CI）——记为技术债：CI 具备条件时把对账搬进 `dotnet test` + IL2CPP 真机跑（§测试开发方案）
- **风险：无 git**——生成物与基线文件一旦覆盖不可恢复，生成器必须幂等、基线文件更新前先备份

---

## 附：实施记录

### 2026-09-15 — M7 完成

**交付**（`Assets/LiteSim/Core/`；骨架 = asmdef/csproj/props）

| 件 | 行数 | 说明 |
| --- | --- | --- |
| `Scripts/SimMath.cs` | 77 | Dt=1/60、Tolerance=1e-6、NearlyEqual/Clamp/Lerp/Sqrt 包装；文件头写死单位与 IEEE 纪律 |
| `Scripts/SimTrig.cs` | 163 | 查表 + 象限归约 + 线性插值；Atan2 八分圆归约**反查同一张表**；Tan 分母容差保护 |
| `Scripts/SimTrigTables.cs` | 528（生成物） | `SinTable[4096]`，8 值/行；G9 + InvariantCulture |
| `Scripts/SimVector3.cs` | 101 | 运算符 + Dot/Cross/Length/Normalized（零向量保护） |
| `Scripts/SimRng.cs` | 54 | Xorshift64*；`ulong State` |
| `Scripts/SimDeterminismLint.cs` | 232 | **纯规则引擎**（R1~R4），零依赖、dotnet 可测 |
| `Scripts/Editor/SimTrigTableGenerator.cs` | 68 | 菜单「LiteSim/生成 SimTrig 查表」 |
| `Scripts/Editor/SimDeterminismLinter.cs` | 89 | 菜单「LiteSim/纪律扫描」+ CI 入口（`RunCli`） |
| 骨架 | — | `LiteSim.Core.asmdef`（`noEngineReferences:true` / `references:[]`）、`LiteSim.Core.csproj`、Editor asmdef、`Directory.Build.props`（产物重定向 `.dotnet/`） |

**测试**：`Tests/LiteSim.Core.Tests`（xUnit，并入 `Tests/Tests.slnx`）+ 基线 `Baselines/IeeeBaseline.txt`
SimTrigTests 163 ／ SimVector3Tests 80 ／ SimRngTests 117 ／ IeeeBoundaryTests 61 ／ LintRuleTests 114 ／ IeeeBaselineSpec 91

**实测**
- `dotnet test Tests/Tests.slnx`：**114 → 154 绿**（LiteFramework 114 + LiteSim 40），0 失败
- 查表精度：Sin/Cos 对 `Math.Sin/Cos` 采样 10k 点，|err| ≤ 1e-5（§9 验收①）
- Atan2：四象限 / 轴上 / 全周 sweep，|err| ≤ 1e-4
- SimRng：`seed=12345` 前 8 值硬编码锚点 + 运算链 checksum `2896875742u` 与基线一致（§9 验收③）
- lint 演示（§2.5）：R1/R2/R3 各造一负例**全部命中**；真实源码（R4 开启）**0 误报**（§9 验收②演示）

**坑位**
1. **生成物行尾漏逗号** → 数组初始化器非法。生成器改为行尾 `,\n`（末元素尾逗号 C# 合法）。
2. **规则扫到注释**：`SimMath` 文件头写了 `FusedMultiplyAdd` 被 R2 误报 → 扫描器加**去注释**（字符串/字符字面量感知 + 块注释跨行状态）。
3. **规则引擎落位偏离指导**：指导把 lint 全放 `LiteSim.Core.Editor`；但 Editor 程序集引 `UnityEditor`，dotnet 测试够不到规则集 → 改为**引擎（`LiteSim.Core`，零依赖）+ 菜单（Editor）**二分，规则集才能单测。引擎文件对扫描器自身豁免（规则里含禁用字面串）。
4. **Atan2 全周 sweep 用例**：`Atan2` 值域 `(-π, π]`，跨 π 需按 2π 归一——测试 bug，非实现 bug。

**对账（§5 预算）**
- 人工合计 **1410 行**（生产/工具 784 + 测试 626），预算 ~1150 → **+23%**（超 20%）。主因：① 测试写得更全（626 vs 350，+79%）；② lint 拆引擎+菜单并加去注释（321 vs 150）。
- 生成物改 8 值/行：**528 行**（指导预判 "4096+ 行"），体积更小。

**未做（守边界）**：不建 `SimWorldState`/系统/checksum 全量（M8）、不建快照回滚（M9）、不 vendor kcp2k（M10）。

#### 2026-09-15 追加：纪律件迁出 LiteSim（统一引擎）

`SimDeterminismLint.cs`（规则引擎）与 `Editor/SimDeterminismLinter.cs`（菜单）**已移出本里程碑的 `LiteSim.Core`**，并入独立零依赖程序集 **`Assets/Tools/DisciplineScanner`**：

- 规则扩到 **R1~R6**（新增 R6 禁原生 Unity 协程），并支持**多根规则集**（`Assets/LiteSim` = R1~R5；`LiteFramework/Scripts/Core` = R5；`LiteFramework/Scripts/Unity` + `LiteGame/Scripts/Runtime` = R6）
- 菜单改名：`LiteSim/纪律扫描` → **`Tools/纪律扫描`**；CI 入口 `-executeMethod Tools.DisciplineScan.Editor.DisciplineScanMenu.RunCli`
- 引擎不再寄居 Sim 程序集（原先要管框架规则属分层错位）；同时收敛了此前散在测试里的正则 / 去注释 / 项目根定位
- 依据：《测试开发方案》§7.3 ②。测试改为 `Tests/LiteFramework.Core.Tests/DisciplineScannerTests.cs`


### 2026-09-18 — 跨运行时对账落地与一处**底噪发现**

**落地**（《待办总览》§5-28）：
- 探针**下沉到 `LiteSim.Core/IeeeProbe`**：IEEE 边界样本（24 值）、`Sqrt` 逐值、相邻对 `Add/Sub/Mul/Div`、10k 步运算链 checksum —— **单一来源**，.NET 侧（`IeeeBaselineSpec` 现为薄委托）与 Unity 侧（`LiteSim.Core.Editor/IeeeBaselineChecker`）共用；两侧各写一份必然漂移，探针本身就成了不确定性来源。
- Unity 侧入口：菜单「LiteSim/对账 IEEE 基线」+ `RunCli`；并做成 **EditMode 用例**（`Assets/Tests/EditMode`），进 L2 门禁（`unity command run_tests mode=EditMode`，编辑器无需关闭）。

**发现（重要）**：Unity(2022.3 / Mono) 与 .NET 8 在 **10k 步运算链**上 checksum 不一致：

| | 值 |
| --- | --- |
| 逐值 + 相邻对行（116 行） | **全部逐位一致** ✓ |
| `Chain`（10k 步） | .NET **2896875742** / Unity **3683559206** ❌ |

**根因假设**：`SimMath.Sqrt` = `(float)Math.Sqrt(double)`——.NET 8 的 `Math.Sqrt` 正确舍入，**Mono 的实现可能不是**（ulp 级差异），经 10k 步累积放大。M7 当时写"只用 IEEE 基本运算 + sqrt（IEEE 要求正确舍入）"的推论，**在 Mono 上不成立**——这是本轮最有价值的发现。

**处置**：门禁**分层**——逐值行=硬判据（守住基本运算路径），`Chain`=观测项（记录不阻断）；定案（接受底噪 / 自研 software sqrt 消除源头）记入《待办总览》§5-**33**，输入是 M10 对跑实测的和解率。

### 2026-09-18（晚）— 定案 B 落地与**真因修正**

**真因修正（推翻上一段的"根因假设"）**：上段推断"BCL `Math.Sqrt` 的 Mono 实现非正确舍入"——**不准确**。逐步定位（Unity eval 逐分量对账 + 双精度参考精算）锁定：

| | 值 |
| --- | --- |
| `x*x + y*y + z*z`（逐步舍入，IEEE 无融合） | **1075435771** ← .NET 8 的行为 |
| `x*x + y*y + z*z`（单次舍入，= FMA 融合语义） | **1075435770** ← Unity(Mono) 的行为 |
| 拆成局部变量 `px=x*x; py=y*y; pz=z*z; px+py+pz` | **1075435770** ← 拆变量**不能**阻止 Mono 融合 |

⇒ **Mono 的 JIT 把 `a*b + c*d` 形态自动融合成 FMA**（单次舍入），.NET 8 严格按 IEEE 逐步舍入 → 同一份源码差 1 ulp。逐值行（单个 Sqrt/Add/Sub/Mul/Div）全同恰好证明 BCL sqrt 本身两侧一致。

**修法两刀**：
1. **`SimMath.Sqrt` 改自研 software sqrt**（纯整数位运算 + 正确舍入：归一化取 S∈[2^23,2^25) → 整数逐位开方求 V=⌊sqrt(S·2^(2·shift+2))⌋ → 精确半点比较进位 → 组装指数）——防御性消除对 BCL 数学的依赖，与查表三角函数同策：**自己实现唯一实现**。10 万随机位型 + 2 万完美平方邻域 + 全指数范围幂次 逐位验证 = BCL ✓。
2. **融合安全算术件**（`SimMath.MulAdd2/MulAdd3/MulSub2/MulAddSub3/MulAdd/MulSub`）：双精度累积 + 单次舍入。原理：float×float 的积在 double 里**精确**（24+24 ≤ 53 位）→ 任何融合都等于不融合，融合敏感性被彻底消除。`SimVector3.Dot/Cross/LengthSquared` 与 `ShootingSystem` 射线求交已全部改造。**纪律**：Sim 内出现 `a*b + c*d`、`a*b ± c` 形态一律走它。

**结果**：Chain **两侧逐位一致**（.NET = Unity = 3683559206 ✓）；基线重录（`[baseline]`，Chain 从 2896875742 → 3683559206，SimChecksum 恰未受影响）；BuildHash 重生成。Chain 从"观测项"**升为硬判据**（EditMode 用例直跑断言，一旦有人绕过融合安全件写裸 `a*b+c*d` 立刻红）。

**调试方法论坑位**（Unity 增量编译）：
1. **改 .cs 后 Unity 的编译器服务器可能不重建**（时间戳+大小缓存；Content 改了但大小恰好没变 → 仍用旧解析结果）。对策：追加一行注释改变文件大小，或 `eval` 调 `RequestScriptCompilation(CleanBuildCache)`。
2. **eval 的参数是位置参数**（`unity command eval 'code...'`），不是 `code=...`。
